/* Auto-generated from navigation.css.jinja2 -- do not edit by hand. */

/* ==========================================================================
   Shared navigation styles (applied to all form factors)
   ========================================================================== */

.m3-navigation {
  position: relative;
}

.m3-navigation-drawer__brand {
  display: block;
  padding: 16px 12px 8px;
  font-family: var(--md-sys-typography-title-font, inherit);
  font-size: var(--md-sys-typography-title-size);
  font-weight: var(--md-sys-typography-title-weight);
  color: var(--md-sys-color-on-surface);
}

.m3-navigation-drawer__divider {
  height: 1px;
  margin: 8px 12px;
  background-color: var(--md-sys-color-outline);
}

/* ==========================================================================
   DESKTOP: Navigation drawer (>=1024px) -- FF-01
   ========================================================================== */

.m3-navigation-drawer {
  display: none;
  width: 280px;
  padding: 8px 0;
}

.m3-navigation-drawer__items {
  list-style: none;
  padding: 0;
  margin: 0;
}

.m3-navigation-drawer__item {
  margin: 2px 8px;
}

.m3-navigation-drawer__link {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 12px;
  text-decoration: none;
  color: var(--md-sys-color-on-surface);
  border-radius: var(--md-sys-shape-extra-small);
}

.m3-navigation-drawer__link:hover {
  background-color: var(--md-sys-color-surface-variant);
}

.m3-navigation-drawer__icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 24px;
  height: 24px;
}

.m3-navigation-drawer__label {
  flex: 1;
  font-size: var(--md-sys-typography-body-size);
}

.m3-navigation-drawer__badge {
  background-color: var(--md-sys-color-primary);
  color: var(--md-sys-color-on-primary);
  padding: 2px 6px;
  border-radius: var(--md-sys-shape-extra-small);
  font-size: var(--md-sys-typography-label-size);
  font-weight: var(--md-sys-typography-label-weight);
}

.m3-navigation-drawer__subitems {
  list-style: none;
  padding-left: 48px;
  margin: 2px 0;
}

.m3-navigation-drawer__subitem {
  margin: 2px 8px;
}

.m3-navigation-drawer__sublink {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 6px 12px;
  text-decoration: none;
  color: var(--md-sys-color-on-surface-variant);
  border-radius: var(--md-sys-shape-extra-small);
}

.m3-navigation-drawer__sublink:hover {
  background-color: var(--md-sys-color-surface-variant);
}

.m3-navigation-drawer__subicon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 20px;
  height: 20px;
}

.m3-navigation-drawer__sublabel {
  font-size: var(--md-sys-typography-body-size);
}

/* ==========================================================================
   TABLET: Navigation rail (600-1023px) -- FF-02
   ========================================================================== */

.m3-navigation-rail {
  display: none;
  width: 80px;
  padding: 8px 0;
}

.m3-navigation-rail__items {
  list-style: none;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 8px 0;
  margin: 0;
}

.m3-navigation-rail__item {
  width: 100%;
}

.m3-navigation-rail__link {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 6px 0;
  text-decoration: none;
  width: 100%;
  color: var(--md-sys-color-on-surface-variant);
}

.m3-navigation-rail__link:hover {
  color: var(--md-sys-color-on-surface);
}

.m3-navigation-rail__icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 24px;
  height: 24px;
}

.m3-navigation-rail__badge {
  background-color: var(--md-sys-color-primary);
  color: var(--md-sys-color-on-primary);
  padding: 1px 4px;
  border-radius: var(--md-sys-shape-extra-small);
  font-size: var(--md-sys-typography-label-size);
  font-weight: var(--md-sys-typography-label-weight);
  position: absolute;
  top: 2px;
  right: 8px;
}

/* ==========================================================================
   MOBILE: Bottom navigation bar (<600px) -- FF-03
   ========================================================================== */

.m3-navigation-bar {
  display: none;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: var(--md-sys-color-surface);
  border-top: 1px solid var(--md-sys-color-outline);
}

.m3-navigation-bar__items {
  list-style: none;
  display: flex;
  justify-content: space-around;
  padding: 0;
  margin: 0;
}

.m3-navigation-bar__item {
  flex: 1;
  max-width: 120px;
}

.m3-navigation-bar__link {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  padding: 8px 0;
  text-decoration: none;
  color: var(--md-sys-color-on-surface-variant);
}

.m3-navigation-bar__link:hover {
  color: var(--md-sys-color-on-surface);
}

.m3-navigation-bar__icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 24px;
  height: 24px;
}

.m3-navigation-bar__label {
  font-size: var(--md-sys-typography-label-size);
}

.m3-navigation-bar__badge {
  background-color: var(--md-sys-color-primary);
  color: var(--md-sys-color-on-primary);
  padding: 1px 4px;
  border-radius: var(--md-sys-shape-extra-small);
  font-size: var(--md-sys-typography-label-size);
  font-weight: var(--md-sys-typography-label-weight);
}

/* ==========================================================================
   RESPONSIVE: Show/hide by breakpoint
   ========================================================================== */

@media (min-width: 1024px) {
  .m3-navigation-drawer { display: block; }
  .m3-navigation-rail { display: none; }
  .m3-navigation-bar { display: none; }
}

@media (min-width: 600px) and (max-width: 1023px) {
  .m3-navigation-drawer { display: none; }
  .m3-navigation-rail { display: flex; }
  .m3-navigation-bar { display: none; }
}

@media (max-width: 599px) {
  .m3-navigation-drawer { display: none; }
  .m3-navigation-rail { display: none; }
  .m3-navigation-bar { display: flex; }
}
