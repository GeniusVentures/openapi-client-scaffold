---
phase: 08-supporting-atoms-table-cells-light-palette
plan: 06
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05"]
files_modified:
  - lib/frontend_scaffold.dart
  - example/lib/main.dart
autonomous: true
requirements: [WIDG-40, WIDG-41, WIDG-42]

must_haves:
  truths:
    - "ScaffoldChip, ScaffoldChipGroup, ScaffoldDisclosure, ScaffoldTraceList, TraceItem, and ScaffoldComposer are all reachable from package:frontend_scaffold/frontend_scaffold.dart"
    - "All four new demo tiles appear in example/lib/main.dart's HomePage ListView"
    - "The full flutter test suite passes after the barrel + registration"
    - "dart analyze --fatal-infos is clean across the package (lib/, test/, example/)"
  artifacts:
    - path: "lib/frontend_scaffold.dart"
      provides: "Barrel export extended with the five new atoms in alphabetical position"
      contains: "scaffold_chip"
    - path: "example/lib/main.dart"
      provides: "Demo registrations for disclosure, trace_list, chip, composer"
      contains: "ChipDemo"
  key_links:
    - from: "example/lib/main.dart"
      to: "example/lib/demos/*.dart"
      via: "Import + _DemoTile registration"
      pattern: "disclosure_demo|trace_list_demo|chip_demo|composer_demo"
---

<objective>
Close out Phase 8 by wiring the new atoms into the public surface: append the five barrel exports to `lib/frontend_scaffold.dart` in alphabetical position and register the four new demos in `example/lib/main.dart`, then run the full test + analyze gate to prove the v1.1 shipping bar holds across the package.

Purpose: Satisfies the "tests + demo + barrel export" v1.1 shipping bar for all Phase 8 atoms. Single plan touches the barrel and main.dart to avoid the same-wave file conflicts that would arise if plans 08-03/04/05 each modified them independently.

Output: Modified `lib/frontend_scaffold.dart` (five new export lines, alphabetically positioned) and `example/lib/main.dart` (four new demo imports + four new `_DemoTile` entries). Full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-CONTEXT.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md

@lib/frontend_scaffold.dart
@example/lib/main.dart
@example/lib/demos/disclosure_demo.dart
@example/lib/demos/trace_list_demo.dart
@example/lib/demos/chip_demo.dart
@example/lib/demos/composer_demo.dart

<interfaces>
<!-- Insertion positions in the barrel (alphabetical within components/). Extracted from codebase. -->

lib/frontend_scaffold.dart — existing anchors:
```dart
export 'components/scaffold_card_state.dart';           // existing line 25
export 'components/scaffold_color_swatch.dart';         // existing line 26
export 'components/scaffold_dashed_border.dart';        // existing line 27
export 'components/scaffold_disabled_overlay.dart';     // existing line 28
export 'components/scaffold_drag_handle.dart';          // existing line 29
...
export 'components/scaffold_touch_target.dart';         // existing line 68
export 'components/wallet_connect_sheet.dart';          // existing line 69
```

New lines to insert:
- `export 'components/scaffold_chip.dart';` and `export 'components/scaffold_chip_group.dart';` — between lines 25 and 26 (after scaffold_card_state, before scaffold_color_swatch)
- `export 'components/scaffold_composer.dart';` — between lines 26 and 27 (after scaffold_color_swatch, before scaffold_dashed_border)
- `export 'components/scaffold_disclosure.dart';` — between lines 28 and 29 (after scaffold_disabled_overlay, before scaffold_drag_handle)
- `export 'components/scaffold_trace_list.dart';` — between lines 68 and 69 (after scaffold_touch_target, before wallet_connect_sheet)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append five new exports to lib/frontend_scaffold.dart in alphabetical position</name>
  <files>lib/frontend_scaffold.dart</files>
  <read_first>
    - lib/frontend_scaffold.dart (the file being modified — see the existing export list and the insertion anchors documented in the interfaces block above)
    - lib/components/scaffold_chip.dart (verify the file exists and the class is `ScaffoldChip`)
    - lib/components/scaffold_chip_group.dart (verify the file exists and the class is `ScaffoldChipGroup`)
    - lib/components/scaffold_composer.dart (verify the file exists and the class is `ScaffoldComposer`)
    - lib/components/scaffold_disclosure.dart (verify the file exists and the class is `ScaffoldDisclosure`)
    - lib/components/scaffold_trace_list.dart (verify the file exists and exports `ScaffoldTraceList` and `TraceItem`)
  </read_first>
  <action>Edit `lib/frontend_scaffold.dart` to insert five new export lines in alphabetical position within the existing `components/` block. (1) After the existing `export 'components/scaffold_card_state.dart';` line, insert `export 'components/scaffold_chip.dart';` and `export 'components/scaffold_chip_group.dart';` on consecutive lines. (2) After the existing `export 'components/scaffold_color_swatch.dart';` line, insert `export 'components/scaffold_composer.dart';`. (3) After the existing `export 'components/scaffold_disabled_overlay.dart';` line, insert `export 'components/scaffold_disclosure.dart';`. (4) After the existing `export 'components/scaffold_touch_target.dart';` line, insert `export 'components/scaffold_trace_list.dart';`. Do NOT reorder existing exports. Do NOT modify the library-doc header. Run `dart analyze --fatal-infos lib/frontend_scaffold.dart` to confirm the barrel compiles cleanly with the new exports.</action>
  <verify>
    <automated>dart analyze --fatal-infos lib/frontend_scaffold.dart &amp;&amp; [ "$(grep -c "scaffold_chip\.dart\|scaffold_chip_group\.dart\|scaffold_composer\.dart\|scaffold_disclosure\.dart\|scaffold_trace_list\.dart" lib/frontend_scaffold.dart)" -eq 5 ]</automated>
  </verify>
  <done>Five new export lines exist in alphabetical position; `dart analyze --fatal-infos lib/frontend_scaffold.dart` exits zero.</done>
  <acceptance_criteria>
    - `grep -c "^export 'components/scaffold_chip.dart';" lib/frontend_scaffold.dart` returns 1
    - `grep -c "^export 'components/scaffold_chip_group.dart';" lib/frontend_scaffold.dart` returns 1
    - `grep -c "^export 'components/scaffold_composer.dart';" lib/frontend_scaffold.dart` returns 1
    - `grep -c "^export 'components/scaffold_disclosure.dart';" lib/frontend_scaffold.dart` returns 1
    - `grep -c "^export 'components/scaffold_trace_list.dart';" lib/frontend_scaffold.dart` returns 1
    - The chip export appears between `scaffold_card_state.dart` and `scaffold_color_swatch.dart` in the file
    - The composer export appears between `scaffold_color_swatch.dart` and `scaffold_dashed_border.dart`
    - The disclosure export appears between `scaffold_disabled_overlay.dart` and `scaffold_drag_handle.dart`
    - The trace_list export appears between `scaffold_touch_target.dart` and `wallet_connect_sheet.dart`
    - `dart analyze --fatal-infos lib/frontend_scaffold.dart` exits zero
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register the four new demos in example/lib/main.dart</name>
  <files>example/lib/main.dart</files>
  <read_first>
    - example/lib/main.dart (the file being modified — see the existing import block at lines 4-17 and the existing _DemoTile entries in the ListView)
    - example/lib/demos/disclosure_demo.dart (verify the demo class is `ScaffoldDisclosureDemo`)
    - example/lib/demos/trace_list_demo.dart (verify the demo class is `ScaffoldTraceListDemo`)
    - example/lib/demos/chip_demo.dart (verify the demo class is `ScaffoldChipDemo`)
    - example/lib/demos/composer_demo.dart (verify the demo class is `ScaffoldComposerDemo`)
  </read_first>
  <action>Edit `example/lib/main.dart`. (1) Add four new imports at the end of the existing import block (after `import 'demos/kitchen_sink_demo.dart';`): `import 'demos/chip_demo.dart';`, `import 'demos/composer_demo.dart';`, `import 'demos/disclosure_demo.dart';`, `import 'demos/trace_list_demo.dart';`. (2) In the `HomePage.build` `ListView.children` list, after the existing `_DemoTile(title: 'Wallet connect sheet', ...)` entry and before the closing `],` of the children list, append four new `_DemoTile` entries in this order: a "Chip / ChipGroup" tile with subtitle 'Pill pressable atom + selection group' building `const ScaffoldChipDemo()`; a "Composer" tile with subtitle 'Text composition area with badge/action slots' building `const ScaffoldComposerDemo()`; a "Disclosure" tile with subtitle 'Expand/collapse row with AnimatedSize + reduced-motion' building `const ScaffoldDisclosureDemo()`; a "Trace list" tile with subtitle 'Ordered disclosure items + optional group header' building `const ScaffoldTraceListDemo()`. Run `dart analyze --fatal-infos example/lib/main.dart` to confirm the file compiles cleanly with the new registrations.</action>
  <verify>
    <automated>dart analyze --fatal-infos example/lib/main.dart &amp;&amp; [ "$(grep -c "chip_demo\|composer_demo\|disclosure_demo\|trace_list_demo" example/lib/main.dart)" -eq 4 ] &amp;&amp; [ "$(grep -c "ScaffoldChipDemo\|ScaffoldComposerDemo\|ScaffoldDisclosureDemo\|ScaffoldTraceListDemo" example/lib/main.dart)" -eq 4 ]</automated>
  </verify>
  <done>Four new imports and four new `_DemoTile` entries exist; `dart analyze --fatal-infos example/lib/main.dart` exits zero.</done>
  <acceptance_criteria>
    - `grep -c "^import 'demos/chip_demo.dart';" example/lib/main.dart` returns 1
    - `grep -c "^import 'demos/composer_demo.dart';" example/lib/main.dart` returns 1
    - `grep -c "^import 'demos/disclosure_demo.dart';" example/lib/main.dart` returns 1
    - `grep -c "^import 'demos/trace_list_demo.dart';" example/lib/main.dart` returns 1
    - `grep -c "ScaffoldChipDemo()" example/lib/main.dart` returns 1
    - `grep -c "ScaffoldComposerDemo()" example/lib/main.dart` returns 1
    - `grep -c "ScaffoldDisclosureDemo()" example/lib/main.dart` returns 1
    - `grep -c "ScaffoldTraceListDemo()" example/lib/main.dart` returns 1
    - `dart analyze --fatal-infos example/lib/main.dart` exits zero
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run the full Phase 8 verification gate</name>
  <files></files>
  <read_first>
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-CONTEXT.md (the v1.1 shipping bar — "tests + demo + barrel export for its widgets, dart analyze --fatal-infos + flutter test gates green")
    - .planning/workstreams/scaffold/STATE.md (the "All v1.1 widgets consume only Theme.of(context)" constraint)
  </read_first>
  <action>Run the full verification gate from the repository root (`/Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold`). (1) `flutter test` — full suite MUST pass with zero failures. (2) `dart analyze --fatal-infos` — full package MUST be clean (lib/, test/, example/). (3) `grep -rn "Colors\.white\|Colors\.black\|Colors\.grey" lib/components/scaffold_chip.dart lib/components/scaffold_chip_group.dart lib/components/scaffold_composer.dart lib/components/scaffold_disclosure.dart lib/components/scaffold_trace_list.dart lib/components/scaffold_badge.dart` — MUST return no matches in the new files, and MUST return no matches in `scaffold_badge.dart` (WIDG-46 remediation confirmed). (4) `grep -rnE "EdgeInsets\.(all|symmetric|only)\([0-9]" lib/components/scaffold_chip.dart lib/components/scaffold_chip_group.dart lib/components/scaffold_composer.dart lib/components/scaffold_disclosure.dart lib/components/scaffold_trace_list.dart` — MUST return no matches (all spacing via dimens tokens). If any gate fails, fix the offending file (do not patch the test to pass — fix the implementation) and re-run the gate.</action>
  <verify>
    <automated>flutter test &amp;&amp; dart analyze --fatal-infos &amp;&amp; ! grep -rn "Colors\.white\|Colors\.black\|Colors\.grey" lib/components/scaffold_chip.dart lib/components/scaffold_chip_group.dart lib/components/scaffold_composer.dart lib/components/scaffold_disclosure.dart lib/components/scaffold_trace_list.dart lib/components/scaffold_badge.dart &amp;&amp; ! grep -rnE "EdgeInsets\.(all|symmetric|only)\([0-9]" lib/components/scaffold_chip.dart lib/components/scaffold_chip_group.dart lib/components/scaffold_composer.dart lib/components/scaffold_disclosure.dart lib/components/scaffold_trace_list.dart</automated>
  </verify>
  <done>Full `flutter test` suite green, full `dart analyze --fatal-infos` clean, no hardcoded colors in any new component or in the remediated badge, no hardcoded EdgeInsets dims in any new component.</done>
  <acceptance_criteria>
    - `flutter test` exits zero
    - `dart analyze --fatal-infos` exits zero
    - `grep -n "Colors\.white" lib/components/scaffold_badge.dart` returns no matches (WIDG-46 remediation holds)
    - `grep -rn "Colors\." lib/components/scaffold_chip.dart lib/components/scaffold_chip_group.dart lib/components/scaffold_composer.dart lib/components/scaffold_disclosure.dart lib/components/scaffold_trace_list.dart` returns no matches
    - `grep -rnE "EdgeInsets\.(all|symmetric|only)\([0-9]" lib/components/scaffold_chip.dart lib/components/scaffold_chip_group.dart lib/components/scaffold_composer.dart lib/components/scaffold_disclosure.dart lib/components/scaffold_trace_list.dart` returns no matches
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package → consumer apps | the barrel export widens the public API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | Barrel export surface | accept | Adding exports does not change atom behavior; CI drift check on templates remains the guard |
| T-08-06-02 | Information Disclosure | Demo registrations | accept | Demo data is placeholder strings — no PII, no credentials |
</threat_model>

<verification>
- `flutter test` exits zero.
- `dart analyze --fatal-infos` exits zero.
- `lib/frontend_scaffold.dart` exports all five new atoms in alphabetical position.
- `example/lib/main.dart` registers all four new demos.
- No hardcoded colors or dims in any new component.
- `lib/components/scaffold_badge.dart` has zero `Colors.white` matches.
</verification>

<success_criteria>
- Every Phase 8 atom (chip, chip group, disclosure, trace list, composer) is reachable from the package barrel export.
- Every Phase 8 demo (chip, composer, disclosure, trace list) is registered and reachable from the example app's HomePage.
- The full test + analyze gates are green across the package.
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-06-SUMMARY.md` when done.
</output>
