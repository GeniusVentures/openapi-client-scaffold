---
phase: 08-supporting-atoms-table-cells-light-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/scaffold_badge.dart
  - test/components/scaffold_badge_test.dart
  - test/components/scaffold_palette_token_test.dart
autonomous: true
requirements: [WIDG-46]

must_haves:
  truths:
    - "ScaffoldBadge label text and icon glyph colors pass WCAG AA 4.5:1 against every BadgeVariant fill in both defaultPalette and lightPalette"
    - "No hardcoded Colors.white remains in scaffold_badge.dart"
    - "lightPalette provides a non-null value for every token consumed by shipped scaffold widgets"
  artifacts:
    - path: "lib/components/scaffold_badge.dart"
      provides: "On-status color resolution helper + replaced hardcoded Colors.white at the label style and icon glyph sites"
      contains: "_resolveOnStatusColor"
    - path: "test/components/scaffold_badge_test.dart"
      provides: "Assertions for on-status color under both palettes for bright fills (lightGreenPrimary, statusSuccess, statusWarningText) and dark fills (statusError, blue500)"
      contains: "lightPalette"
    - path: "test/components/scaffold_palette_token_test.dart"
      provides: "Token coverage assertions for lightPalette across all tokens consumed by shipped widgets"
      contains: "lightPalette"
  key_links:
    - from: "lib/components/scaffold_badge.dart"
      to: "lib/theme/scaffold_palette.dart"
      via: "context.palette lookups plus the new _resolveOnStatusColor helper"
      pattern: "_resolveOnStatusColor"
---

<objective>
Close the WIDG-46 light-palette gap by remediating the `ScaffoldBadge` hardcoded `Colors.white` at lines 105 and 140 of `lib/components/scaffold_badge.dart` and by extending the existing palette-token test to assert light-palette coverage for every token consumed by shipped scaffold widgets.

Purpose: Implements D-10 / D-11 from 08-CONTEXT.md. Unblocks downstream Wave 1-3 plans by ensuring the badge atom they compose is WCAG-AA-correct under both palettes, and locks the "no consumer overrides under `ThemeData.light()`" contract in a test.

Output:
- `lib/components/scaffold_badge.dart` with `_resolveOnStatusColor(Color fill)` helper; the two `Colors.white` sites replaced.
- `test/components/scaffold_badge_test.dart` extended with on-status-color assertions for bright and dark fills under both `defaultPalette` and `lightPalette`.
- `test/components/scaffold_palette_token_test.dart` extended to assert `lightPalette` provides non-null, light-appropriate values for every token consumed by shipped widgets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-CONTEXT.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md

@lib/components/scaffold_badge.dart
@lib/theme/scaffold_palette.dart
@lib/theme/scaffold_theme.dart
@test/components/scaffold_badge_test.dart
@test/components/scaffold_palette_token_test.dart

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/theme/scaffold_palette.dart:
```dart
class ScaffoldPalette extends ThemeExtension<ScaffoldPalette> {
  // Tokens consumed by Phase 8 widgets (see 08-CONTEXT.md "Canonical References"):
  //   surfaceElevated, deepBlueCardColor, lightGreenPrimary,
  //   textPrimary, textSecondary, borderSubtle, focusRingColor,
  //   statusSuccess, statusError, statusWarningText, blue500, …
  static const ScaffoldPalette defaultPalette = ScaffoldPalette(...);  // dark
  static const ScaffoldPalette lightPalette  = ScaffoldPalette(...);   // light
}
```

From lib/theme/scaffold_theme.dart:
```dart
extension ScaffoldThemeX on BuildContext {
  ScaffoldPalette get palette;
  ScaffoldDimens  get dimens;
}
const List<ThemeExtension<dynamic>> scaffoldThemeExtensions = <ThemeExtension<dynamic>>[
  ScaffoldPalette.defaultPalette,
  ScaffoldDimens.defaultDimens,
];
```

From lib/components/scaffold_badge.dart (current — lines 105 and 140 hardcode Colors.white):
```dart
// Line ~105 (label style site):
final TextStyle? labelStyle = textTheme.labelSmall?.copyWith(
  color: Colors.white,  // ← WIDG-46 remediation target
);
// Line ~140 (icon glyph site):
child: Icon(icon, size: 16, color: Colors.white),  // ← WIDG-46 remediation target
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing on-status-color tests for ScaffoldBadge under both palettes</name>
  <files>test/components/scaffold_badge_test.dart</files>
  <read_first>
    - test/components/scaffold_badge_test.dart (current test file — see existing pump helper, variant coverage, and theme wiring)
    - lib/components/scaffold_badge.dart (current implementation — confirms the two `Colors.white` sites and how `badgeColor`/`variant` resolve to a fill)
    - lib/theme/scaffold_palette.dart (token names — confirms `lightPalette` token values for `lightGreenPrimary` `#00EAAE`, `statusError` `#D13438`, `blue500`)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (section "lib/components/scaffold_badge.dart (MODIFY — WIDG-46 on-status color)" — the resolved on-status contract)
  </read_first>
  <behavior>
    - Test 1: Pumping a `ScaffoldBadge` whose resolved fill is `palette.lightGreenPrimary` under `defaultPalette` renders its label text with the dark on-status color (`Color(0xFF17191E)`).
    - Test 2: Pumping a `ScaffoldBadge` whose resolved fill is `palette.lightGreenPrimary` under `lightPalette` renders its label text with the dark on-status color.
    - Test 3: Pumping a `ScaffoldBadge` whose resolved fill is `palette.statusError` under `defaultPalette` renders its label text with the light on-status color (`Color(0xFFFFFFFF)`).
    - Test 4: Pumping a `ScaffoldBadge` whose resolved fill is `palette.statusError` under `lightPalette` renders its label text with the light on-status color.
    - Test 5: For an icon-only `ScaffoldBadge` whose resolved fill is `palette.lightGreenPrimary`, the rendered `Icon` widget's `color` equals the dark on-status color (icon glyph site covered).
    - Test 6: For an icon-only `ScaffoldBadge` whose resolved fill is `palette.statusError`, the rendered `Icon` widget's `color` equals the light on-status color.
  </behavior>
  <action>Extend `test/components/scaffold_badge_test.dart` with the six tests above. Locate the existing pump helper and add a `_pumpLight(WidgetTester, Widget)` companion that wraps the child in `MaterialApp(theme: ThemeData.light().copyWith(extensions: const <ThemeExtension<dynamic>>[ScaffoldPalette.lightPalette, ScaffoldDimens.defaultDimens]))`. Each test pumps a badge (constructed the same way the existing tests construct it — same variant / `badgeColor` overrides), then locates the rendered `Text` widget for the label (or the `Icon` widget for the icon-only case) and asserts on its `style.color` (or `Icon.color`). Run `flutter test test/components/scaffold_badge_test.dart` — all six new tests MUST fail because the implementation still hardcodes `Colors.white`.</action>
  <verify>
    <automated>flutter test test/components/scaffold_badge_test.dart 2>&1 | grep -E "Some tests failed" &amp;&amp; echo "RED confirmed"</automated>
  </verify>
  <done>All six new tests exist in the file, the file compiles, and `flutter test` reports them as failing (RED state).</done>
  <acceptance_criteria>
    - `grep -c "_pumpLight" test/components/scaffold_badge_test.dart` returns ≥ 1
    - `grep -c "lightPalette" test/components/scaffold_badge_test.dart` returns ≥ 1
    - `grep -c "Color(0xFF17191E)" test/components/scaffold_badge_test.dart` returns ≥ 1
    - `grep -c "Color(0xFFFFFFFF)" test/components/scaffold_badge_test.dart` returns ≥ 1
    - `flutter test test/components/scaffold_badge_test.dart` exits non-zero with the six new tests failing
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _resolveOnStatusColor in ScaffoldBadge and replace both Colors.white sites</name>
  <files>lib/components/scaffold_badge.dart</files>
  <read_first>
    - lib/components/scaffold_badge.dart (the file being modified — see lines 105 and 140 for the two `Colors.white` sites and the surrounding fill-resolution logic)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (the "lib/components/scaffold_badge.dart (MODIFY)" section — specifies the helper name, threshold constant, and the two fixed on-status color constants)
    - test/components/scaffold_badge_test.dart (the failing tests from Task 1 — they are the contract)
  </read_first>
  <behavior>
    - Test 1..6 from Task 1 flip from RED to GREEN.
    - All pre-existing tests in `test/components/scaffold_badge_test.dart` continue to pass.
    - `dart analyze --fatal-infos lib/components/scaffold_badge.dart` is clean.
  </behavior>
  <action>In `lib/components/scaffold_badge.dart`, add a private static helper `_resolveOnStatusColor(Color fill)` that returns `_kOnStatusDark` (`Color(0xFF17191E)`) when `fill.computeLuminance() > _kOnStatusLuminanceThreshold` and `_kOnStatusLight` (`Color(0xFFFFFFFF)`) otherwise. Declare both colors and the threshold as `static const` members on the badge class: `_kOnStatusLuminanceThreshold = 0.40`, `_kOnStatusDark = Color(0xFF17191E)`, `_kOnStatusLight = Color(0xFFFFFFFF)`. Replace the `Colors.white` at the label-style site (currently line 105) with `_resolveOnStatusColor(resolvedBadgeColor)` where `resolvedBadgeColor` is the local variable already used by the badge to resolve its fill (per D-10 — resolution lives inside `ScaffoldBadge`, not in consumers). Replace the `Colors.white` at the icon-glyph site (currently line 140) with the same helper call. Do NOT introduce any new palette tokens and do NOT modify `scaffold_palette.dart`. Re-run `flutter test test/components/scaffold_badge_test.dart` — all tests (existing + the six new ones) MUST pass.</action>
  <verify>
    <automated>flutter test test/components/scaffold_badge_test.dart &amp;&amp; dart analyze --fatal-infos lib/components/scaffold_badge.dart &amp;&amp; ! grep -n "Colors\.white" lib/components/scaffold_badge.dart</automated>
  </verify>
  <done>All badge tests pass, `dart analyze --fatal-infos` is clean, and `grep -n "Colors\.white" lib/components/scaffold_badge.dart` returns no matches.</done>
  <acceptance_criteria>
    - `grep -c "_resolveOnStatusColor" lib/components/scaffold_badge.dart` returns ≥ 3 (declaration + two call sites)
    - `grep -c "_kOnStatusLuminanceThreshold" lib/components/scaffold_badge.dart` returns ≥ 1
    - `grep -n "Colors\.white" lib/components/scaffold_badge.dart` returns no matches
    - `flutter test test/components/scaffold_badge_test.dart` exits zero
    - `dart analyze --fatal-infos lib/components/scaffold_badge.dart` exits zero
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend palette-token test to assert lightPalette coverage for tokens consumed by shipped widgets</name>
  <files>test/components/scaffold_palette_token_test.dart</files>
  <read_first>
    - test/components/scaffold_palette_token_test.dart (the file being modified — see the existing per-token assertion pattern)
    - lib/theme/scaffold_palette.dart (the full token list on `ScaffoldPalette` and the existing `lightPalette` definition at lines ~132-155)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md (the "Color" section — lists the tokens consumed by Phase 8 widgets and the light-palette contract)
  </read_first>
  <action>Add a new test `lightPalette covers all tokens consumed by shipped widgets` to `test/components/scaffold_palette_token_test.dart`. Mirror the file's existing assertion style. Reference `ScaffoldPalette.lightPalette` as a `const` value and emit one `expect(..., isNotNull)` per token consumed by shipped widgets per the UI-SPEC Color section: `surfaceElevated`, `deepBlueCardColor`, `lightGreenPrimary`, `textPrimary`, `textSecondary`, `borderSubtle`, `focusRingColor`, `statusSuccess`, `statusError`, `statusWarningText`, `blue500`. Additionally assert the surface flip contract: `lightPalette.surfaceElevated` is not equal to `defaultPalette.surfaceElevated`, and `lightPalette.textPrimary` is not equal to `defaultPalette.textPrimary`. Do NOT modify `lib/theme/scaffold_palette.dart` — this is a verification test only; if a gap is discovered (missing token or null value), stop and report rather than patching the palette.</action>
  <verify>
    <automated>flutter test test/components/scaffold_palette_token_test.dart</automated>
  </verify>
  <done>The new test exists, the file compiles, and `flutter test test/components/scaffold_palette_token_test.dart` exits zero.</done>
  <acceptance_criteria>
    - `grep -c "lightPalette covers all tokens consumed by shipped widgets" test/components/scaffold_palette_token_test.dart` returns ≥ 1
    - `grep -c "lightPalette" test/components/scaffold_palette_token_test.dart` returns ≥ 11 (one per token assertion, plus the test name)
    - `grep -c "isNot(equals(" test/components/scaffold_palette_token_test.dart` returns ≥ 2 (surface flip + text flip)
    - `flutter test test/components/scaffold_palette_token_test.dart` exits zero
    - `dart analyze --fatal-infos test/components/scaffold_palette_token_test.dart` exits zero
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → ScaffoldBadge | consumer supplies badge label string, icon, and `badgeColor` override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | ScaffoldBadge fill resolution | accept | Consumer-supplied `badgeColor` is an in-process API value, not a network trust boundary; badge atom holds no secrets and stores no state |
| T-08-01-02 | Information Disclosure | Test fixtures | accept | No PII, no credentials — test data is placeholder strings |
| T-08-01-03 | Denial of Service | Luminance computation | accept | O(1) arithmetic on a `Color` — no DoS surface |
</threat_model>

<verification>
- `flutter test test/components/scaffold_badge_test.dart` exits zero.
- `flutter test test/components/scaffold_palette_token_test.dart` exits zero.
- `dart analyze --fatal-infos lib/components/scaffold_badge.dart test/components/scaffold_badge_test.dart test/components/scaffold_palette_token_test.dart` exits zero.
- `grep -n "Colors\.white" lib/components/scaffold_badge.dart` returns no matches.
</verification>

<success_criteria>
- ScaffoldBadge renders WCAG-AA-correct label and icon colors against bright and dark fills under both `defaultPalette` and `lightPalette`.
- The `lightPalette` token-coverage contract is asserted in a test that runs on every CI invocation.
- No new palette tokens introduced; no changes to `lib/theme/scaffold_palette.dart`.
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-01-SUMMARY.md` when done.
</output>
