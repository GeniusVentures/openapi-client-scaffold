---
phase: 08-supporting-atoms-table-cells-light-palette
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/components/data_table.dart.jinja2
  - test/components/data_table_cell_builder_test.dart
autonomous: true
requirements: [WIDG-43]

must_haves:
  truths:
    - "DataColumnConfig accepts an optional cellBuilder of type Widget Function(BuildContext, T item)?"
    - "When cellBuilder is null, the cell renders Text(item.accessor?.toString() ?? '') — existing behavior preserved"
    - "When cellBuilder is non-null, the cell renders cellBuilder(context, item) inside the default DataCell padding"
    - "Sort behavior is driven by accessor regardless of cellBuilder presence"
    - "data_table.dart.jinja2 line 245 labelLarge w600 column-header style is unchanged"
  artifacts:
    - path: "templates/components/data_table.dart.jinja2"
      provides: "DataColumnConfig gains cellBuilder field; cell render path branches on cellBuilder presence"
      contains: "cellBuilder"
    - path: "test/components/data_table_cell_builder_test.dart"
      provides: "Widget test proving the cellBuilder contract on a hand-written DataColumnConfig-shaped class that mirrors the template's generated output"
      contains: "cellBuilder"
  key_links:
    - from: "templates/components/data_table.dart.jinja2"
      to: "DataColumnConfig class"
      via: "Additive field declaration preserving constructor backward compatibility"
      pattern: "cellBuilder"
---

<objective>
Extend `templates/components/data_table.dart.jinja2` so `DataColumnConfig<T>` gains an optional `cellBuilder: Widget Function(BuildContext, T item)?` field, and branch the cell render path on its presence — without forking the table and without changing the locked column-header typography at template line 245.

Purpose: Implements D-08 / D-09 from 08-CONTEXT.md. Consumers can render custom cells (badges, chips, status indicators, custom layouts) inside the existing `DataCell` wrapper without copying the table widget.

Output:
- `templates/components/data_table.dart.jinja2` — `DataColumnConfig` constructor + fields extended; cell render `for col in columns` loop branches on `cellBuilder` presence; the `data_table_vars.json` fixture is NOT modified (per D-09 fallback — the fixture format cannot express a function value, so the contract is verified in the widget test).
- `test/components/data_table_cell_builder_test.dart` — new widget test that hand-writes a `DataColumnConfig`-shaped class matching the new template signature, then proves the runtime branch (null → string cell; non-null → builder output).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-CONTEXT.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md

@templates/components/data_table.dart.jinja2
@templates/components/data_table_vars.json

<interfaces>
<!-- Existing template contract (locked anchors). Extracted from codebase. -->

From templates/components/data_table.dart.jinja2 (lines 50-71, current DataColumnConfig):
```dart
class DataColumnConfig {
  const DataColumnConfig({
    required this.label,
    required this.accessor,
    this.sortable = true,
  });

  final String label;
  final String accessor;
  final bool sortable;
}
```

From templates/components/data_table.dart.jinja2 (line 245 — LOCKED, do NOT modify):
```dart
style: textTheme.labelLarge?.copyWith(fontWeight: FontWeight.w600),
```

From templates/components/data_table.dart.jinja2 (lines 307-311 — cell render path, the modification site):
```jinja2
{% for col in columns %}
  DataCell(
    Text(item.{{ col.accessor }}?.toString() ?? ''),
  ),
{% endfor %}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing widget test for the DataColumnConfig cellBuilder contract</name>
  <files>test/components/data_table_cell_builder_test.dart</files>
  <read_first>
    - templates/components/data_table.dart.jinja2 (read lines 50-71 for the existing DataColumnConfig shape and lines 295-315 for the cell render path — the test hand-writes a class that mirrors the post-change template output)
    - test/components/scaffold_badge_test.dart (pattern analog for the pump helper shape — uses `MaterialApp(theme: ThemeData(extensions: scaffoldThemeExtensions), home: Scaffold(body: Center(child: child)))`)
    - lib/theme/scaffold_theme.dart (for the `scaffoldThemeExtensions` constant consumed by the pump helper)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (section "templates/components/data_table.dart.jinja2 (MODIFY — add cellBuilder)" — specifies the post-change field signature)
  </read_first>
  <behavior>
    - Test 1: When a `DataColumnConfig` (hand-written test-double class matching the new template signature) is constructed with `cellBuilder: null`, a host `DataTable` rendering via the same branch shape as the template renders `Text(item.name)` — i.e. the string fallback fires.
    - Test 2: When `cellBuilder` is non-null and returns a `Container` with a unique `Key('custom-cell')`, the rendered tree contains the `Key('custom-cell')` widget and does NOT contain the string-fallback `Text`.
    - Test 3: The custom builder is invoked with the same `BuildContext` and item the template would supply — verified by capturing the arguments inside the builder and asserting on the captured item reference.
  </behavior>
  <action>Create `test/components/data_table_cell_builder_test.dart`. At the top of the file, hand-write a private `_SampleItem` class with an `Object? name` field and a private `_DataColumnConfig` class that mirrors the POST-CHANGE template `DataColumnConfig` shape — fields `String label`, `String accessor`, `bool sortable = true`, and the new `Widget Function(BuildContext, _SampleItem)? cellBuilder`. Then write a `_CellHost` StatelessWidget that, given a `_DataColumnConfig` and an item, returns a `DataCell` whose child is `config.cellBuilder != null ? Builder(builder: (BuildContext ctx) => config.cellBuilder!(ctx, item)) : Text(item.name?.toString() ?? '')`. This mirrors the post-change template render path exactly. Write the three tests in the behavior block. Run `flutter test test/components/data_table_cell_builder_test.dart` — the file MUST NOT compile against the existing template (because the template does not yet declare `cellBuilder`), but the test-double file itself compiles and passes, since the contract is hand-written. The "RED" state for this task is that the corresponding field does NOT yet exist in `templates/components/data_table.dart.jinja2` — confirm by `grep -c "cellBuilder" templates/components/data_table.dart.jinja2` returning 0 BEFORE Task 2 runs.</action>
  <verify>
    <automated>flutter test test/components/data_table_cell_builder_test.dart &amp;&amp; [ "$(grep -c 'cellBuilder' templates/components/data_table.dart.jinja2)" -eq 0 ]</automated>
  </verify>
  <done>The new test file exists and its tests pass against the hand-written `_DataColumnConfig`. `grep -c "cellBuilder" templates/components/data_table.dart.jinja2` returns 0 — the template does not yet declare the field (pre-Task-2 state).</done>
  <acceptance_criteria>
    - `test/components/data_table_cell_builder_test.dart` exists
    - `grep -c "cellBuilder" test/components/data_table_cell_builder_test.dart` returns ≥ 4 (field declaration + three tests)
    - `grep -c "custom-cell" test/components/data_table_cell_builder_test.dart` returns ≥ 1
    - `flutter test test/components/data_table_cell_builder_test.dart` exits zero
    - `grep -c "cellBuilder" templates/components/data_table.dart.jinja2` returns 0 (template not yet modified — RED state)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend DataColumnConfig and the cell render path in data_table.dart.jinja2</name>
  <files>templates/components/data_table.dart.jinja2</files>
  <read_first>
    - templates/components/data_table.dart.jinja2 (the file being modified — read lines 50-71 for the DataColumnConfig block to extend and lines 295-315 for the cell render path to branch)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (the "templates/components/data_table.dart.jinja2 (MODIFY — add cellBuilder)" section — specifies the exact additive change and the fallback contract)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md (the "DataColumnConfig&lt;T&gt;.cellBuilder" widget contract — signature, default behavior, sort contract)
  </read_first>
  <action>In `templates/components/data_table.dart.jinja2`, make the following additive changes. (1) Extend the `DataColumnConfig` constructor at line 57 to add `this.cellBuilder,` as an optional named parameter after `this.sortable = true,`. (2) After the existing `final bool sortable;` field, add a new field declaration: a doc comment explaining that when non-null the cell renders `cellBuilder(context, item)` inside the default `DataCell`, that custom builders MUST NOT add their own outer padding (they inherit the DataTable default), and that sort behavior is driven by `accessor` regardless of `cellBuilder` presence; then the field `final Widget Function(BuildContext context, {{ data_type_name }} item)? cellBuilder;`. (3) Replace the cell render path at lines 307-311 with a Jinja `{% if col.cellBuilder is defined and col.cellBuilder %}` branch: when the fixture marks the column as cellBuilder-exercising, emit `DataCell(Builder(builder: (BuildContext cellContext) => widget.columns[{{ loop.index0 }}].cellBuilder!(cellContext, item)))`; otherwise emit the existing `DataCell(Text(item.{{ col.accessor }}?.toString() ?? ''))` unchanged. Because the existing fixture at `templates/components/data_table_vars.json` does not set `cellBuilder` on any column, the `{% else %}` branch is what the existing fixture will generate — preserving generated-output backward compatibility. Do NOT touch line 245 (`labelLarge` w600 column headers — locked). Do NOT modify `templates/components/data_table_vars.json` (per D-09 fallback). After editing, verify the template remains syntactically valid Jinja by `grep -c "cellBuilder" templates/components/data_table.dart.jinja2` returning ≥ 3 and the `{% for col in columns %}` / `{% endfor %}` block structure being intact.</action>
  <verify>
    <automated>grep -c "cellBuilder" templates/components/data_table.dart.jinja2 | grep -v '^0$' &amp;&amp; grep -c "labelLarge?.copyWith(fontWeight: FontWeight.w600)" templates/components/data_table.dart.jinja2 &amp;&amp; grep -c "accessor" templates/components/data_table.dart.jinja2</automated>
  </verify>
  <done>`templates/components/data_table.dart.jinja2` declares `cellBuilder` in `DataColumnConfig`, branches the cell render path on its presence, and the locked `labelLarge` w600 column-header style at line 245 is unchanged.</done>
  <acceptance_criteria>
    - `grep -c "cellBuilder" templates/components/data_table.dart.jinja2` returns ≥ 3 (constructor param + field declaration + render branch)
    - `grep -c "labelLarge?.copyWith(fontWeight: FontWeight.w600)" templates/components/data_table.dart.jinja2` returns ≥ 1 (locked header preserved)
    - `grep -c "accessor" templates/components/data_table.dart.jinja2` returns ≥ 3 (existing accessor contract intact)
    - `templates/components/data_table_vars.json` is byte-identical to its pre-plan state (D-09 fallback — fixture not modified)
    - The Jinja `{% if %}`/`{% else %}`/`{% endif %}` block in the cell render path is balanced (no orphan tags)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → cellBuilder callback | consumer supplies an arbitrary render closure executed per row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | cellBuilder closure execution | accept | In-process Dart closure — not a network trust boundary; consumer code already runs in the same isolate |
| T-08-02-02 | Denial of Service | Custom builder returning a huge widget tree per row | accept | Consumer responsibility; scaffold does not police consumer-supplied builders — the contract documents "MUST NOT add their own outer padding" but resource bounds are out of scope for an in-process callback |
| T-08-02-03 | Information Disclosure | Test fixtures | accept | No PII, no credentials — placeholder strings only |
</threat_model>

<verification>
- `flutter test test/components/data_table_cell_builder_test.dart` exits zero.
- `dart analyze --fatal-infos test/components/data_table_cell_builder_test.dart` exits zero.
- `grep -c "cellBuilder" templates/components/data_table.dart.jinja2` returns ≥ 3.
- `templates/components/data_table_vars.json` is byte-identical to its pre-plan state.
- Line 245 locked `labelLarge` w600 column-header style is unchanged.
</verification>

<success_criteria>
- Consumers can pass a `cellBuilder` on `DataColumnConfig<T>` and the generated data table renders that custom widget per cell without forking the table.
- The existing fixture continues to generate the string-cell path — backward compatible.
- The cellBuilder contract is asserted in a widget test that runs on every CI invocation.
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-02-SUMMARY.md` when done.
</output>
