---
phase: 08-supporting-atoms-table-cells-light-palette
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/scaffold_disclosure.dart
  - lib/components/scaffold_trace_list.dart
  - test/components/scaffold_disclosure_test.dart
  - test/components/scaffold_trace_list_test.dart
  - example/lib/demos/disclosure_demo.dart
  - example/lib/demos/trace_list_demo.dart
autonomous: true
requirements: [WIDG-41]

must_haves:
  truths:
    - "ScaffoldDisclosure expands and collapses its body via AnimatedSize honoring ScaffoldMotion reduced-motion"
    - "ScaffoldDisclosure supports both controlled (expanded + onExpandedChanged) and uncontrolled (initiallyExpanded) modes"
    - "ScaffoldDisclosure chevron rotates 0° to 90° on expand and tints lightGreenPrimary when expanded AND highlightWhenExpanded is true"
    - "ScaffoldTraceList renders an ordered List<TraceItem> as disclosure rows with space8 separation and an optional groupHeader"
    - "ScaffoldTraceList renders SizedBox.shrink() when items is empty"
    - "Both widgets render correctly under ThemeData.light() with lightPalette"
  artifacts:
    - path: "lib/components/scaffold_disclosure.dart"
      provides: "ScaffoldDisclosure atom — pressable header + AnimatedSize body, controlled/uncontrolled"
      exports: ["ScaffoldDisclosure"]
    - path: "lib/components/scaffold_trace_list.dart"
      provides: "ScaffoldTraceList atom — ordered disclosure list with typed TraceItem model"
      exports: ["ScaffoldTraceList", "TraceItem"]
    - path: "test/components/scaffold_disclosure_test.dart"
      provides: "Widget tests for the disclosure contract across both palettes and both state modes"
    - path: "test/components/scaffold_trace_list_test.dart"
      provides: "Widget tests for trace-list ordering, empty state, and group header"
    - path: "example/lib/demos/disclosure_demo.dart"
      provides: "Demo exercising controlled/uncontrolled/highlight variants"
    - path: "example/lib/demos/trace_list_demo.dart"
      provides: "Demo exercising ordered trace items + group header"
  key_links:
    - from: "lib/components/scaffold_disclosure.dart"
      to: "lib/components/scaffold_motion.dart"
      via: "ScaffoldMotion.of(context).reducedMotion gates AnimatedSize duration"
      pattern: "ScaffoldMotion\\.of\\(context\\)\\.reducedMotion"
    - from: "lib/components/scaffold_trace_list.dart"
      to: "lib/components/scaffold_disclosure.dart"
      via: "Each TraceItem rendered as a ScaffoldDisclosure"
      pattern: "ScaffoldDisclosure\\("
---

<objective>
Ship `ScaffoldDisclosure` and `ScaffoldTraceList` — generic expand/collapse row atom and ordered trace-of-disclosures atom — each with widget tests across both palettes and a demo in `example/lib/demos/`.

Purpose: Implements D-04 / D-05 from 08-CONTEXT.md and WIDG-41. Unlocks "Thinking traces" and any consumer that needs an accessible expand/collapse pattern without forking ExpansionTile.

Output: Two new atoms in `lib/components/`, two new test files in `test/components/`, two new demo files in `example/lib/demos/`. Barrel export and demo registration in `example/lib/main.dart` are deferred to plan 08-06 (final wave) to avoid same-wave file conflicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-CONTEXT.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md

@lib/components/scaffold_pressable.dart
@lib/components/scaffold_motion.dart
@lib/components/scaffold_status_indicator.dart
@lib/components/scaffold_search_bar.dart
@lib/theme/scaffold_theme.dart
@lib/theme/scaffold_dimens.dart
@test/components/scaffold_badge_test.dart
@example/lib/demos/media_card_demo.dart

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/components/scaffold_motion.dart:
```dart
class ScaffoldMotion extends InheritedWidget {
  static ScaffoldMotionData of(BuildContext context);
}
class ScaffoldMotionData {
  final bool reducedMotion;
}
class ScaffoldMotionDurations {
  static const Duration short;   // 150ms
  static const Duration medium;  // 300ms
}
class ScaffoldMotionCurves {
  static const Curve standard;
}
```

From lib/components/scaffold_pressable.dart:
```dart
class ScaffoldPressable extends StatelessWidget {
  const ScaffoldPressable({
    required this.child,
    this.onPressed,
    this.onLongPress,
    this.disabled = false,
    this.semanticLabel,
    ...
  });
  // Internally wraps child in: ScaffoldTouchTarget → Stack(state layer)
  //   → MouseRegion/GestureDetector → ScaffoldFocusOutline → Focus
  //   → Semantics(button: true). Disabled wraps in ScaffoldDisabledOverlay.
}
```

From lib/components/scaffold_status_indicator.dart:
```dart
enum StatusVariant { success, warning, error, info, neutral }
class ScaffoldStatusIndicator extends StatelessWidget {
  const ScaffoldStatusIndicator({required this.status, this.dotSize = 8, ...});
  final StatusVariant status;
  final double dotSize;
}
```

From lib/theme/scaffold_dimens.dart:
```dart
class ScaffoldDimens extends ThemeExtension<ScaffoldDimens> {
  final double space2;   // 4
  final double space4;   // 8
  final double space6;   // 12
  final double space8;   // 16
  final double space12;  // 24
  ...
}
```

From lib/theme/scaffold_theme.dart:
```dart
extension ScaffoldThemeX on BuildContext {
  ScaffoldPalette get palette;
  ScaffoldDimens get dimens;
}
const List<ThemeExtension<dynamic>> scaffoldThemeExtensions;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing widget tests for ScaffoldDisclosure + ScaffoldTraceList</name>
  <files>test/components/scaffold_disclosure_test.dart, test/components/scaffold_trace_list_test.dart</files>
  <read_first>
    - test/components/scaffold_badge_test.dart (pattern analog for pump helper shape and assertion style)
    - test/components/media_controls_test.dart (pattern analog for callback-assertion tests with transient state)
    - lib/components/scaffold_motion.dart (ScaffoldMotion.of API — used to override reducedMotion in tests)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (sections "scaffold_disclosure.dart" and "scaffold_trace_list.dart" — the locked contract)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md (the "ScaffoldDisclosure" and "ScaffoldTraceList" widget contracts — exact paddings, gaps, animation contract)
  </read_first>
  <behavior>
    Disclosure tests (test/components/scaffold_disclosure_test.dart):
    - Test 1: Uncontrolled disclosure with `initiallyExpanded: false` initially hides body; tapping the header reveals it.
    - Test 2: Uncontrolled disclosure with `initiallyExpanded: true` initially shows body; tapping hides it.
    - Test 3: Controlled disclosure with `expanded: false` does NOT expand on tap; it only fires `onExpandedChanged(true)`. The parent must update `expanded` for the UI to change.
    - Test 4: `onExpandedChanged` fires exactly once per tap with the new (negated) state.
    - Test 5: Chevron `AnimatedRotation` `turns` is 0.0 when collapsed and 0.25 when expanded.
    - Test 6: Chevron color is `palette.textSecondary` when collapsed; when expanded AND `highlightWhenExpanded: true`, chevron color is `palette.lightGreenPrimary`; when expanded AND `highlightWhenExpanded: false`, chevron color stays `palette.textSecondary`.
    - Test 7: Semantics node reports `expanded: true/false` matching state and `label: title`.
    - Test 8: Renders under `ThemeData.light()` + `lightPalette` without exception and body indent is `EdgeInsets.only(left: dimens.space6, top: dimens.space4)`.

    Trace-list tests (test/components/scaffold_trace_list_test.dart):
    - Test 9: Empty `items` list renders `SizedBox.shrink()`.
    - Test 10: Renders items in the supplied order — first item's title appears before second item's title in the widget tree.
    - Test 11: `groupHeader` text renders with `textTheme.titleSmall` style and 24px top padding.
    - Test 12: Items are separated vertically by `dimens.space8` (16px).
    - Test 13: Renders under `lightPalette` without exception.
  </behavior>
  <action>Create both test files. Each file defines a `_pump(WidgetTester, Widget)` helper that wraps the child in `MaterialApp(theme: ThemeData(extensions: scaffoldThemeExtensions), home: Scaffold(body: Center(child: child)))` and a `_pumpLight` companion that uses `ThemeData.light().copyWith(extensions: const <ThemeExtension<dynamic>>[ScaffoldPalette.lightPalette, ScaffoldDimens.defaultDimens])`. Write the thirteen tests above against the (not-yet-existing) `ScaffoldDisclosure` and `ScaffoldTraceList` classes — importing from `package:frontend_scaffold/components/scaffold_disclosure.dart` and `package:frontend_scaffold/components/scaffold_trace_list.dart`. Run `flutter test test/components/scaffold_disclosure_test.dart test/components/scaffold_trace_list_test.dart` — the run MUST fail because the imported files do not yet exist (RED state).</action>
  <verify>
    <automated>flutter test test/components/scaffold_disclosure_test.dart test/components/scaffold_trace_list_test.dart 2>&amp;1 | grep -E "(Error|Could not find|Target of URI)" &amp;&amp; echo "RED confirmed"</automated>
  </verify>
  <done>Both test files exist, contain the thirteen tests, and `flutter test` reports compile/import errors because the implementation files do not yet exist (RED state).</done>
  <acceptance_criteria>
    - `test/components/scaffold_disclosure_test.dart` exists
    - `test/components/scaffold_trace_list_test.dart` exists
    - `grep -c "testWidgets" test/components/scaffold_disclosure_test.dart` returns ≥ 8
    - `grep -c "testWidgets" test/components/scaffold_trace_list_test.dart` returns ≥ 5
    - `flutter test test/components/scaffold_disclosure_test.dart test/components/scaffold_trace_list_test.dart` exits non-zero (RED — implementations do not exist)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ScaffoldDisclosure with controlled/uncontrolled state, AnimatedSize body, and reduced-motion gating</name>
  <files>lib/components/scaffold_disclosure.dart</files>
  <read_first>
    - lib/components/scaffold_disclosure.dart (will not exist — file is being created; verify by `ls` before reading the analogs below)
    - lib/components/scaffold_search_bar.dart (lines 309-335 — the existing AnimatedSize + reduced-motion + ScaffoldSurface pattern this widget mirrors)
    - lib/components/scaffold_pressable.dart (full composition stack — the header uses this)
    - lib/components/scaffold_motion.dart (ScaffoldMotion.of, ScaffoldMotionDurations, ScaffoldMotionCurves)
    - lib/theme/scaffold_theme.dart (context.palette / context.dimens accessors)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (section "scaffold_disclosure.dart" — the full implementation shape)
    - test/components/scaffold_disclosure_test.dart (the failing tests from Task 1 — they are the contract)
  </read_first>
  <behavior>
    - All eight disclosure tests from Task 1 flip from RED to GREEN.
    - `dart analyze --fatal-infos lib/components/scaffold_disclosure.dart` is clean.
  </behavior>
  <action>Create `lib/components/scaffold_disclosure.dart` as a StatefulWidget. Open with the standard library-doc header (`/// ScaffoldDisclosure — M3 expand/collapse row atom.` plus a paragraph describing composition of ScaffoldPressable + AnimatedSize + ScaffoldMotion; then `library;`). Constructor takes `required String title`, `required Widget body`, optional `bool? expanded`, `bool initiallyExpanded = false`, `ValueChanged<bool>? onExpandedChanged`, `bool highlightWhenExpanded = false`. Assert in the constructor that not both `expanded` and `initiallyExpanded != false` are supplied (the controlled/uncontrolled conflict case). Private state holds `late bool _internalExpanded = widget.initiallyExpanded;` and exposes `bool get _effectiveExpanded => widget.expanded ?? _internalExpanded;`. The `build` method resolves `palette`, `dimens`, `textTheme`, and `reducedMotion = ScaffoldMotion.of(context).reducedMotion`. The header is a `ScaffoldPressable` whose `onPressed` calls a `_toggle()` that updates internal state when uncontrolled and always fires `widget.onExpandedChanged?.call(next)`. The header child is a `Row` with `Expanded(child: Text(title, style: textTheme.labelMedium))`, `SizedBox(width: dimens.space4)`, and an `AnimatedRotation` wrapping `Icon(Icons.chevron_right, size: 24, color: _effectiveExpanded && widget.highlightWhenExpanded ? palette.lightGreenPrimary : palette.textSecondary)`. The `AnimatedRotation` uses `turns: _effectiveExpanded ? 0.25 : 0.0`, duration `reducedMotion ? Duration.zero : ScaffoldMotionDurations.short`, curve `ScaffoldMotionCurves.standard`. The body is wrapped in `AnimatedSize(duration: reducedMotion ? Duration.zero : ScaffoldMotionDurations.medium, curve: ScaffoldMotionCurves.standard, child: _effectiveExpanded ? Padding(padding: EdgeInsets.only(left: dimens.space6, top: dimens.space4), child: widget.body) : const SizedBox.shrink())`. Wrap the whole `Column` in `Semantics(expanded: _effectiveExpanded, label: widget.title, child: ...)`. Re-run the disclosure tests — all eight MUST pass.</action>
  <verify>
    <automated>flutter test test/components/scaffold_disclosure_test.dart &amp;&amp; dart analyze --fatal-infos lib/components/scaffold_disclosure.dart</automated>
  </verify>
  <done>All eight disclosure tests pass; `dart analyze --fatal-infos` is clean; the file consumes only `Theme.of(context)` via `context.palette`/`context.dimens` (no hardcoded colors or dims).</done>
  <acceptance_criteria>
    - `lib/components/scaffold_disclosure.dart` exists
    - `grep -c "ScaffoldMotion.of(context).reducedMotion" lib/components/scaffold_disclosure.dart` returns ≥ 1
    - `grep -c "AnimatedSize" lib/components/scaffold_disclosure.dart` returns ≥ 1
    - `grep -c "AnimatedRotation" lib/components/scaffold_disclosure.dart` returns ≥ 1
    - `grep -c "ScaffoldPressable" lib/components/scaffold_disclosure.dart` returns ≥ 1
    - `grep -nE "Colors\.(white|black|grey|blue|red)" lib/components/scaffold_disclosure.dart` returns no matches
    - `grep -nE "EdgeInsets\.(all|symmetric|only)\([0-9]" lib/components/scaffold_disclosure.dart` returns no matches (no hardcoded dims)
    - `flutter test test/components/scaffold_disclosure_test.dart` exits zero
    - `dart analyze --fatal-infos lib/components/scaffold_disclosure.dart` exits zero
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ScaffoldTraceList with TraceItem typed model</name>
  <files>lib/components/scaffold_trace_list.dart</files>
  <read_first>
    - lib/components/scaffold_trace_list.dart (will not exist — file is being created)
    - lib/components/scaffold_disclosure.dart (the per-item atom this list composes — just created in Task 2)
    - lib/components/scaffold_state_view.dart (typed model → widget switch pattern)
    - lib/components/scaffold_status_indicator.dart (StatusVariant enum consumed by TraceItem)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (section "scaffold_trace_list.dart" — the typed model + layout pattern)
    - test/components/scaffold_trace_list_test.dart (the failing tests from Task 1 — they are the contract)
  </read_first>
  <behavior>
    - All five trace-list tests from Task 1 flip from RED to GREEN.
    - `dart analyze --fatal-infos lib/components/scaffold_trace_list.dart` is clean.
  </behavior>
  <action>Create `lib/components/scaffold_trace_list.dart`. Open with the standard library-doc header (`/// ScaffoldTraceList — M3 ordered trace of disclosure rows.` plus a paragraph; then `library;`). Define an immutable `TraceItem` class with fields `required String title`, `required Widget body`, optional `StatusVariant? status`, `bool initiallyExpanded = false`. Define `ScaffoldTraceList` as a StatelessWidget taking `required List<TraceItem> items` and optional `String? groupHeader`. In `build`, return `const SizedBox.shrink()` when `items.isEmpty`. Otherwise build a `Column(crossAxisAlignment: CrossAxisAlignment.start, mainAxisSize: MainAxisSize.min, children: ...)`. If `groupHeader != null`, prepend a `Padding(padding: EdgeInsets.only(top: dimens.space12), child: Text(groupHeader!, style: Theme.of(context).textTheme.titleSmall))` followed by `SizedBox(height: dimens.space8)`. Then for each item index `i`, if `i > 0` add `SizedBox(height: dimens.space8)`, then add `_buildItem(context, items[i])`. The `_buildItem` method returns a `ScaffoldDisclosure(title: item.title, body: item.body, initiallyExpanded: item.initiallyExpanded)`. If `item.status != null`, compose the title as a `Row` containing `ScaffoldStatusIndicator(status: item.status!, dotSize: 8)` + `SizedBox(width: dimens.space2)` + `Text(item.title)` — but because `ScaffoldDisclosure.title` is typed `String`, instead pass a synthesized title string and render the status indicator via a custom leading slot ONLY if the disclosure atom exposes one. Per the UI-SPEC "Status slot" row ("Optional `ScaffoldStatusIndicator` per item, leading, before title"), the cleanest composition is to keep `ScaffoldDisclosure.title: String` and have `ScaffoldTraceList` render the status indicator OUTSIDE the disclosure title — do not modify `ScaffoldDisclosure`. The simplest valid composition: when `item.status != null`, wrap the `ScaffoldDisclosure` in a `Row` where the leading child is `ScaffoldStatusIndicator(status: item.status!, dotSize: 8)` followed by `SizedBox(width: dimens.space4)` and `Expanded(child: disclosure)`. This keeps `ScaffoldDisclosure` untouched and satisfies the UI-SPEC leading-status contract at the trace-list level. Re-run the trace-list tests — all five MUST pass.</action>
  <verify>
    <automated>flutter test test/components/scaffold_trace_list_test.dart &amp;&amp; dart analyze --fatal-infos lib/components/scaffold_trace_list.dart</automated>
  </verify>
  <done>All five trace-list tests pass; `dart analyze --fatal-infos` is clean; the file consumes only `Theme.of(context)` via `context.palette`/`context.dimens`.</done>
  <acceptance_criteria>
    - `lib/components/scaffold_trace_list.dart` exists
    - `grep -c "class TraceItem" lib/components/scaffold_trace_list.dart` returns ≥ 1
    - `grep -c "class ScaffoldTraceList" lib/components/scaffold_trace_list.dart` returns ≥ 1
    - `grep -c "ScaffoldDisclosure(" lib/components/scaffold_trace_list.dart` returns ≥ 1
    - `grep -c "SizedBox.shrink" lib/components/scaffold_trace_list.dart` returns ≥ 1 (empty state)
    - `grep -nE "Colors\.(white|black|grey|blue|red)" lib/components/scaffold_trace_list.dart` returns no matches
    - `flutter test test/components/scaffold_trace_list_test.dart` exits zero
    - `dart analyze --fatal-infos lib/components/scaffold_trace_list.dart` exits zero
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Author disclosure_demo.dart and trace_list_demo.dart</name>
  <files>example/lib/demos/disclosure_demo.dart, example/lib/demos/trace_list_demo.dart</files>
  <read_first>
    - example/lib/demos/media_card_demo.dart (pattern analog — sectioned vertical layout with Text headers + SizedBox separators using dimens)
    - example/lib/demos/wallet_connect_sheet_demo.dart (pattern analog for interactive demos with local state)
    - lib/components/scaffold_disclosure.dart (the widget being demonstrated)
    - lib/components/scaffold_trace_list.dart (the widget being demonstrated)
    - lib/theme/scaffold_theme.dart (context.dimens accessor for separator spacing)
  </read_first>
  <action>Create `example/lib/demos/disclosure_demo.dart` exposing a `ScaffoldDisclosureDemo` StatelessWidget. Render a `Scaffold(appBar: AppBar(title: const Text('ScaffoldDisclosure')), body: SingleChildScrollView(padding: EdgeInsets.all(dimens.itemSpacing), child: Column(...)))`. Sections: (1) "Uncontrolled (collapsed by default)" — `ScaffoldDisclosure(title: 'What is the scaffold?', body: Text('A shared widget library.', style: TextStyle(color: palette.textSecondary)))`; (2) "Uncontrolled (expanded by default)" — same with `initiallyExpanded: true`; (3) "Highlight when expanded" — same with `highlightWhenExpanded: true`; (4) "Controlled" — a `StatefulBuilder` wrapping a `bool _expanded` local that drives `expanded:` and `onExpandedChanged:`. Import from `package:frontend_scaffold/components/scaffold_disclosure.dart` directly (barrel export lands in plan 08-06). Create `example/lib/demos/trace_list_demo.dart` exposing a `ScaffoldTraceListDemo` StatelessWidget. Render a similar single-scroll Column with a "Simple trace" section containing a `ScaffoldTraceList(items: [TraceItem(title: 'Step 1: Load', body: Text('Loaded 42 items.')), TraceItem(title: 'Step 2: Filter', body: Text('Filtered to 7.'), status: StatusVariant.success), TraceItem(title: 'Step 3: Render', body: Text('Rendered.'), status: StatusVariant.info)])`, a "With group header" section with `groupHeader: 'Pipeline'`, and an "Empty state" section rendering `ScaffoldTraceList(items: const [])` (which must produce zero height). Import from `package:frontend_scaffold/components/scaffold_trace_list.dart` and `package:frontend_scaffold/components/scaffold_status_indicator.dart` directly. Do NOT register the demos in `example/lib/main.dart` — that lands in plan 08-06 to avoid same-wave file conflicts.</action>
  <verify>
    <automated>dart analyze --fatal-infos example/lib/demos/disclosure_demo.dart example/lib/demos/trace_list_demo.dart</automated>
  </verify>
  <done>Both demo files exist, compile cleanly under `dart analyze --fatal-infos`, and are NOT yet registered in `example/lib/main.dart`.</done>
  <acceptance_criteria>
    - `example/lib/demos/disclosure_demo.dart` exists
    - `example/lib/demos/trace_list_demo.dart` exists
    - `grep -c "ScaffoldDisclosureDemo" example/lib/demos/disclosure_demo.dart` returns ≥ 1
    - `grep -c "ScaffoldTraceListDemo" example/lib/demos/trace_list_demo.dart` returns ≥ 1
    - `grep -c "ScaffoldDisclosure(" example/lib/demos/disclosure_demo.dart` returns ≥ 4 (four sections)
    - `grep -c "ScaffoldTraceList(" example/lib/demos/trace_list_demo.dart` returns ≥ 3 (three sections)
    - `dart analyze --fatal-infos example/lib/demos/disclosure_demo.dart example/lib/demos/trace_list_demo.dart` exits zero
    - `grep -c "disclosure_demo\|trace_list_demo" example/lib/main.dart` returns 0 (registration deferred to plan 08-06)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → ScaffoldDisclosure/ScaffoldTraceList | consumer supplies title strings, body widgets, and callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | TraceItem list ordering | accept | In-process API contract — consumer controls ordering by construction |
| T-08-03-02 | Denial of Service | Huge items list | accept | Consumer responsibility — scaffold does not virtualize; UI-SPEC "Empty state → SizedBox.shrink()" bounds the zero case |
| T-08-03-03 | Information Disclosure | Semantics labels | accept | Consumer-supplied title strings are announced to screen readers — no PII handling in atom |
| T-08-03-04 | Elevation of Privilege | None — atoms hold no secrets/state | accept | Stateless render + private transient state only (D-03) — no privilege surface |
</threat_model>

<verification>
- `flutter test test/components/scaffold_disclosure_test.dart test/components/scaffold_trace_list_test.dart` exits zero.
- `dart analyze --fatal-infos lib/components/scaffold_disclosure.dart lib/components/scaffold_trace_list.dart test/components/scaffold_disclosure_test.dart test/components/scaffold_trace_list_test.dart example/lib/demos/disclosure_demo.dart example/lib/demos/trace_list_demo.dart` exits zero.
- No hardcoded colors or dims in either new component file.
- Both demo files exist and are NOT yet registered in `example/lib/main.dart` (deferred to plan 08-06).
</verification>

<success_criteria>
- `ScaffoldDisclosure` expands/collapses a row generically in both controlled and uncontrolled modes, honors reduced-motion, and renders correctly under both palettes.
- `ScaffoldTraceList` renders an ordered list of disclosure items with optional group header and empty-state shrink.
- Both atoms have widget tests across both palettes and a demo file (demo registration deferred to plan 08-06).
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-03-SUMMARY.md` when done.
</output>
