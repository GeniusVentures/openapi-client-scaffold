---
phase: 11-verification-coverage-gate
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - README.md
autonomous: true
requirements:
  - WIDG-44
  - WIDG-45
must_haves:
  truths:
    - "README tells the reader how to run the demo (cd example && flutter run -d macos / -d chrome)"
    - "README shows a 26-entry component gallery with one embedded image per demo"
    - "README states the correct test count (454) and demo count (26) — no stale 214/one-per-family claims"
    - "README contains the 19-row WIDG-45 coverage table mapping every Beautiful UI component to shipped scaffold atoms"
    - "README's repository layout tree includes the new images/ directory"
  artifacts:
    - path: "README.md"
      provides: "Package-root reader-facing documentation: demo-run instructions, component gallery, WIDG-45 coverage proof, corrected counts"
      contains: "## Component gallery"
  key_links:
    - from: "README.md"
      to: "images/*.png"
      via: "26 markdown image embeds ![alt](images/<name>.png) — package-root-relative paths so GitHub renders them"
      pattern: "!\\[.*\\]\\(images/"
    - from: "README.md"
      to: ".planning/atoms-v1.2-additions.md"
      via: "the 19-row WIDG-45 table reproduces the 7+8+4 split verbatim from 11-RESEARCH.md §WIDG-45 19-Component Coverage Proof"
      pattern: "primitive-enabled"
---

<objective>
Restructure `README.md` per D-01 so a reader of the package can (a) run the
demo, (b) see all 26 components as embedded images, and (c) verify the
19-component Beautiful UI coverage claim (WIDG-45) without leaving the
README.

Purpose: The README is the package's public face. Today it says "214 tests"
(stale — actual is 454) and "one demo screen per widget family" (vague —
actual is 26 demos). It has no gallery, no images, and no WIDG-45 proof. This
plan closes all three gaps with surgical edits that preserve the existing
Part 1 / Part 2 structure.

Output: Updated `README.md` with corrected counts, an expanded demo section,
a 26-entry component gallery, the 19-row WIDG-45 coverage table, and an
updated repository layout tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/REQUIREMENTS.md
@.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-CONTEXT.md
@.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-RESEARCH.md
@.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-PATTERNS.md

@README.md
@.planning/atoms-v1.2-additions.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct stale counts + expand Demo app section</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 1–100 (Part 1 structure: what-is, What's-in-it, Demo app, Develop — locate the exact edit sites)
    - README.md lines 280–319 (Repository layout tree — locate the second test-count edit at line 300)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-PATTERNS.md (§ "README.md (MODIFIED)" Patterns 1, 2, 5 — the verbatim diff shapes)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-RESEARCH.md (§ "README Restructuring" → "Current State (VERIFIED)" for the staleness claims and § "Minimal Surgical Edits" for the edit content)
  </read_first>
  <action>
    Make three surgical edits to `README.md`. Do NOT restructure headings,
    do NOT touch Part 2, do NOT touch the "What's in it" table, do NOT
    change the "~70 widgets" claim at lines 5, 53, 297 (still accurate).

    **Edit A — Demo app section expansion (lines 73–79).** The current
    section reads:
    > `example/` is a runnable gallery with one demo screen per widget family.

    Replace that single line with:
    > `example/` is a runnable gallery with 26 demo screens covering every
    > widget family and the v1.2 atoms. The component gallery below shows a
    > captured screenshot of each demo.

    Keep the existing `## Demo app` heading and the existing `cd example &&
    flutter run -d macos    # or -d chrome` code block above it unchanged
    (this satisfies D-01's "how to run the demo" requirement).

    **Edit B — Test count corrections (two places).**
    - Line 86: change `flutter test                  # 214 tests` to
      `flutter test                  # 454 tests`.
    - Line 300: change `├── test/                           ← 214 widget +
      token tests` to `├── test/                           ← 454 widget +
      token tests`.

    **Edit C — Repository layout tree.** Insert one line into the tree
    between `├── example/                        ← runnable demo gallery`
    (line 303) and `├── templates/` (line 304):
    ```
    ├── images/                         ← per-demo screenshots (gallery above)
    ```
    Match the existing tree's column alignment (the `←` should line up with
    the other rows).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && ! grep -q "214 tests\|214 widget" README.md && grep -q "454 tests" README.md && grep -q "454 widget + token tests" README.md && grep -q "26 demo screens" README.md && grep -q "^├── images/" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "214 tests\|214 widget" README.md` returns `0` (no stale counts remain).
    - `grep -c "454 tests" README.md` returns `1` (line 86).
    - `grep -c "454 widget + token tests" README.md` returns `1` (line 300).
    - `grep -c "26 demo screens" README.md` returns `1`.
    - `grep -c "one demo screen per widget family" README.md` returns `0` (stale phrasing gone).
    - `grep -c "^├── images/" README.md` returns `1` (tree row added).
    - `grep -c "cd example && flutter run -d macos" README.md` returns `1` (run instruction preserved verbatim).
    - `grep -c "## Demo app" README.md` returns `1` (heading unchanged).
    - `wc -l README.md` returns between `320` and `325` (started at 319; +2 from Edit A's extra line, +1 from Edit C's tree row).
  </acceptance_criteria>
  <done>
    README no longer claims 214 tests or "one demo per family"; the demo
    section directs readers to the gallery below; the layout tree lists
    `images/`; the run-the-demo command is preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Component gallery section (26 embedded images)</name>
  <files>README.md</files>
  <read_first>
    - README.md (after Task 1 — locate the line immediately after the updated `## Demo app` section so the gallery inserts cleanly before `## Develop`)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-PATTERNS.md (§ "README.md (MODIFIED)" Pattern 3 — the per-entry block shape)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-RESEARCH.md (§ "Image-Capture Harness" → "Naming Convention" table for the 26 (title, filename) pairs)
    - example/lib/main.dart (the `_DemoTile` registration order — the gallery MUST follow this order so the visual flow matches the demo app)
    - images/ (verify all 26 PNGs from Plan 11-02 are on disk before embedding — `ls images/ | wc -l` must return 26)
  </read_first>
  <action>
    Insert a new `## Component gallery` section IMMEDIATELY AFTER the
    updated `## Demo app` section and BEFORE `## Develop`. The section
    contains 26 entries in the same order as `example/lib/main.dart`'s
    `_DemoTile` registry.

    Each entry has this exact three-line shape:

    ```
    ### {Demo Title}
    ![{Demo Title} demo](images/{filename}.png)
    {One-line description.}
    ```

    Followed by a blank line before the next entry.

    The 26 (Title, filename, description) tuples in order:

    1.  `ActionButton` / `action_button` / "Enabled, disabled, and rotate-animation states."
    2.  `StringButton` / `string_button` / "Keypad-style button that emits its string value."
    3.  `TextEntryFieldWidget` / `text_entry_field` / "M3 text field with scaffold styling."
    4.  `Loading` / `loading` / "Loading indicators and skeleton states."
    5.  `Toast` / `toast` / "Transient notification toasts."
    6.  `BottomDrawer / ResponsiveDrawer` / `bottom_drawer` / "Modal bottom drawer that becomes a side drawer on wide screens."
    7.  `Animations` / `animations` / "ScaffoldAnimatedDisplay variants: fade, pulse, scale, shake, rotate."
    8.  `AppScreenView / DesktopBodyContainer` / `page_chrome` / "Page-chrome scaffolding for app screens."
    9.  `ResponsiveGrid` / `responsive_grid` / "Breakpoint-aware responsive grid layout."
    10. `Tracer` / `tracer` / "Trace/debug overlay."
    11. `Kitchen Sink` / `kitchen_sink` / "All atoms on one screen."
    12. `Media card` / `media_card` / "Media thumbnail + metadata card consuming ScaffoldBadge slots."
    13. `Media controls` / `media_controls` / "Playback controls built from ScaffoldPressable + ScaffoldTouchTarget + ScaffoldSlider."
    14. `Wallet connect sheet` / `wallet_connect_sheet` / "Reown session presentation sheet."
    15. `Chip / ChipGroup` / `chip` / "Pressable token atoms and chip-group selection."
    16. `Composer` / `composer` / "Text-entry area with action and badge slots."
    17. `Disclosure` / `disclosure` / "Generic expand/collapse row."
    18. `Trace list` / `trace_list` / "Ordered list of disclosure items."
    19. `Streaming rich text` / `streaming_rich_text` / "Incremental rich text with citations and action slots."
    20. `Code block` / `code_block` / "Syntax-highlighted code with line numbers and copy."
    21. `Selection actions` / `selection_actions` / "Anchored toolbar over selectable content."
    22. `Markdown to spans` / `markdown_to_spans` / "Markdown source → typed span model."
    23. `Light syntax tokenizer` / `light_syntax_tokenizer` / "Minimal syntax tokenizer for code block highlighting."
    24. `Chart` / `chart` / "Neutral-series chart via fl_chart."
    25. `Chart Scrubber` / `chart_scrubber` / "Point-selection/scrubbing composing with the chart."
    26. `Chart Range Selector` / `chart_range_selector` / "Drag-range selection + consumer-policy zoom."

    Every image path MUST be `images/<name>.png` (package-root-relative, no
    leading slash) so GitHub renders them. Filenames MUST match the files
    on disk from Plan 11-02 exactly.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && grep -q "^## Component gallery$" README.md && [ "$(grep -c '^!\[.*\](images/' README.md)" = "26" ] && [ "$(grep -c '^### ' README.md)" -ge "26" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Component gallery$" README.md` returns `1`.
    - `grep -c "^!\[.*\](images/" README.md` returns `26` (one embed per demo).
    - Every filename embedded in the README exists on disk: for each `images/<name>.png` referenced, `test -f <name>` succeeds. Spot-check `images/action_button.png`, `images/chip.png`, `images/chart_range_selector.png`.
    - The gallery section appears BEFORE `## Develop` and AFTER `## Demo app` — verify with `awk '/^## Demo app/,/^## Develop/' README.md | grep -c "^## Component gallery$"` returning `1`.
    - No image path starts with `/` or `./` — `grep -E '^!\[.*\]\((/|\./)' README.md` returns zero matches.
    - Gallery order matches the demo registry: the `###` headings in the gallery appear in the same relative order as the 26 titles above (spot-check first, middle, last: `### ActionButton` precedes `### Kitchen Sink` precedes `### Chart Range Selector`).
  </acceptance_criteria>
  <done>
    README contains a 26-entry component gallery with embedded images that
    resolve on disk, in the same order as the demo registry, inserted
    between "Demo app" and "Develop".
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Coverage section with the 19-row WIDG-45 table</name>
  <files>README.md</files>
  <read_first>
    - .planning/atoms-v1.2-additions.md (the AUTHORITATIVE 19-component source — confirm the 7 ready / 8 thin / 4 primitive-enabled split before typing the table)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-RESEARCH.md (§ "WIDG-45 19-Component Coverage Proof" — the verbatim 19-row table; copy it EXACTLY, do not paraphrase)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-PATTERNS.md (§ "README.md (MODIFIED)" Pattern 4 + Pitfall 5 warning about table drift)
    - README.md (after Tasks 1–2 — locate the insertion point immediately after the Component gallery)
  </read_first>
  <action>
    Insert a new `## Coverage` section IMMEDIATELY AFTER the `## Component
    gallery` section and BEFORE `## Develop`. The section opens with a
    one-paragraph preamble stating the 19-component Beautiful UI yardstick
    and the 7 ready / 8 thin / 4 primitive-enabled split, then contains
    the 19-row table from 11-RESEARCH.md §WIDG-45 19-Component Coverage
    Proof VERBATIM.

    Suggested preamble (one sentence, then a sub-tally line):
    > Every component in the Beautiful UI yardstick is composable from
    > shipped scaffold atoms. Of the 19 components, **7 are ready** (existing
    > atoms compose them with no new primitives), **8 are thin** (compose
    > from existing atoms plus a small amount of consumer wiring), and **4
    > are primitive-enabled** (unlocked by the new v1.2 primitives).

    The 19-row table follows. Columns: `#` | `Beautiful UI Component` |
    `Coverage Tier` | `Shipped Scaffold Atoms That Compose It`. Rows in
    this exact order with this exact content (per 11-RESEARCH.md):

    1.  Loading State | Compose (8-thin) | `ScaffoldSkeleton` + `ScaffoldAnimatedDisplay*` (7 variants) + `ScaffoldFormattedValueDuration` + `ScaffoldSurface`
    2.  Thinking | Compose (8-thin) | `ScaffoldSurface` + `ScaffoldStatusIndicator` + `ScaffoldAnimatedDisplay*` + `ScaffoldDisclosure` (v1.2) + `ScaffoldTraceList` (v1.2)
    3.  Streaming Text | Add primitive (4-primitive) | `ScaffoldStreamingRichText` (v1.2) + `ScaffoldStreamingCopyButton` + `ScaffoldLiveRegion` (a11y announcements)
    4.  Approval Card | Ready (7-ready) | `ScaffoldCard` + `ScaffoldSelectionIndicatorRadio` + `TextEntryFieldWidget` + `ActionButton`
    5.  Tool Chips | Compose (8-thin) | `ScaffoldChip` (v1.2) + `ScaffoldChipGroup` (v1.2) + `ScaffoldBadge` + `ScaffoldStatusIndicator` + `ScaffoldPressable`
    6.  Task Rows | Ready (7-ready) | `ScaffoldStatusIndicator` + `ScaffoldFormattedValue*` + `ScaffoldAnimatedDisplay*` + `ScaffoldSurface`
    7.  Chat | Compose (8-thin) | `ScaffoldCard` + `ScaffoldAnimatedDisplay*` + `ScaffoldStateView` + `TextEntryFieldWidget` + `ScaffoldComposer` (v1.2) + `ScaffoldStreamingRichText` + `ScaffoldTraceList`
    8.  Prompt Bar | Compose (8-thin) | `TextEntryFieldWidget` + `ActionButton` + `ScaffoldBadge` + `ScaffoldPressable` + `ScaffoldComposer` (v1.2) + `ScaffoldChip` (v1.2)
    9.  Recommendation Card | Ready (7-ready) | `ScaffoldCard` + `ScaffoldFormattedValuePercentage` + `ScaffoldStatusIndicator` + `ActionButton`
    10. Context Cards | Ready (7-ready) | `ScaffoldCard` + `ScaffoldBadge` + `ScaffoldFormattedValueNumber` + `ScaffoldImagePlaceholder*` (4 variants)
    11. Diff Table | Compose (8-thin) | `templates/components/data_table.dart.jinja2` + `DataColumnConfig.cellBuilder` (v1.2 Phase 8 extension)
    12. Records Table | Compose (8-thin) | `data_table.dart.jinja2` + `cellBuilder` + `ScaffoldChip` (tags) + `ScaffoldStatusIndicator` (status cells)
    13. Filter Table | Compose (8-thin) | `data_table.dart.jinja2` + `ScaffoldChipGroup` (v1.2) + consumer Cubit deriving visible rows
    14. Sidebar Nav | Ready (7-ready) | `templates/components/navigation.dart.jinja2` (Material drawer + nested groups + badges) + `ScaffoldSearchBar`
    15. Search | Ready (7-ready) | `ScaffoldSearchBar` (live callbacks, grouped results, loading, filter actions) + `ScaffoldStateView` (empty state)
    16. Insight Cards | Add primitive (4-primitive) | `ScaffoldChart` (v1.2) + `ScaffoldChartScrubber` (v1.2) + `ScaffoldChartRangeSelector` (v1.2) + `ScaffoldCard`
    17. Code Block | Add primitive (4-primitive) | `ScaffoldCodeBlock` (v1.2) + `ScaffoldOverflowFade` + `ScaffoldSurface`
    18. Fine-tune Card | Ready (7-ready) | `ScaffoldCard` + `ScaffoldNumericInput` + `ScaffoldColorSwatch` + `ScaffoldSelectionIndicatorToggle` + `StringButton`
    19. Selection Actions | Add primitive (4-primitive) | `ScaffoldSelectionActions` (v1.2) + `ScaffoldSelectionCopyAction` + `ScaffoldComposer`

    Format the table as a standard GitHub-flavored Markdown table with
    leading pipes. Use backticks around every atom/class name as shown.
    Tally check before committing: rows 4, 6, 9, 10, 14, 15, 18 are
    7-ready; rows 1, 2, 5, 7, 8, 11, 12, 13 are 8-thin; rows 3, 16, 17,
    19 are 4-primitive — total 19.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && grep -q "^## Coverage$" README.md && [ "$(grep -cE "^\| [0-9]+ \|" README.md)" = "19" ] && [ "$(grep -c "7-ready\|8-thin\|4-primitive" README.md)" -ge "19" ] && grep -q "ScaffoldStreamingRichText" README.md && grep -q "ScaffoldChartRangeSelector" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Coverage$" README.md` returns `1`.
    - `grep -cE "^\| [0-9]+ \|" README.md` returns `19` (19 numbered table rows).
    - `grep -c "Ready (7-ready)" README.md` returns `7`.
    - `grep -c "Compose (8-thin)" README.md` returns `8`.
    - `grep -c "Add primitive (4-primitive)" README.md` returns `4`.
    - Every v1.2 atom name appears at least once in the table: `grep -c "ScaffoldChip\|ScaffoldChipGroup\|ScaffoldDisclosure\|ScaffoldTraceList\|ScaffoldComposer\|ScaffoldStreamingRichText\|ScaffoldCodeBlock\|ScaffoldSelectionActions\|ScaffoldChart\|ScaffoldChartScrubber\|ScaffoldChartRangeSelector" README.md` returns at least `11` (each appears at least once; some appear multiple times).
    - The Coverage section appears AFTER `## Component gallery` and BEFORE `## Develop` — `awk '/^## Component gallery/,/^## Develop/' README.md | grep -c "^## Coverage$"` returns `1`.
    - The table renders as valid GFM — every table row has matching leading and trailing pipes: `grep -E "^\| " README.md | grep -vc " \|$"` returns `0`.
  </acceptance_criteria>
  <done>
    README contains the 19-row WIDG-45 coverage table with the verified
    7+8+4 split, every v1.2 atom named at least once, valid GFM table
    syntax, and the section sits between the gallery and Develop.
  </done>
</task>

</tasks>

<verification>
Run the full phase-gate sweep after all three tasks land:

1. `dart analyze --fatal-infos` → `No issues found!`
2. `flutter test` → `+454: All tests passed!` with zero skips
3. `grep -rn "skip: true" test/` → empty
4. `ls images/ | wc -l` → `26`
5. `grep -c "^!\[.*\](images/" README.md` → `26`
6. `grep -cE "^\| [0-9]+ \|" README.md` → `19`
7. `grep -c "214 tests\|one demo screen per widget family" README.md` → `0`

All seven must be green for the phase to be done.
</verification>

<success_criteria>
- README's stale "214 tests" and "one demo screen per widget family" claims are gone, replaced with "454 tests" and "26 demo screens".
- The 26-entry component gallery embeds every PNG from `images/` and renders on GitHub.
- The 19-row WIDG-45 coverage table matches `.planning/atoms-v1.2-additions.md` (7+8+4 = 19).
- Repository layout tree includes `images/`.
- Part 2 (codegen toolkit), "What's in it" table, and "~70 widgets" claims are untouched.
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-03-SUMMARY.md` when done. Include:
- The line counts before/after each task (README.md line count progression).
- The verbatim outputs of the seven phase-gate commands listed in `<verification>`.
- A confirmation that the WIDG-45 table was copied verbatim from 11-RESEARCH.md (no paraphrasing).
</output>
