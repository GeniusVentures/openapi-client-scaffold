---
phase: 11-verification-coverage-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/components/scaffold_selection_actions_test.dart
autonomous: true
requirements:
  - WIDG-44
must_haves:
  truths:
    - "The permanently-skipped long-press SelectionArea test is deleted from the suite"
    - "The test suite runs with zero skips (no `skip: true` anywhere under test/)"
    - "The full test suite still passes (454 passing, 0 skipped)"
    - "`dart analyze --fatal-infos` remains clean (no orphaned helpers/imports)"
  artifacts:
    - path: "test/components/scaffold_selection_actions_test.dart"
      provides: "SelectionActions test suite without the redundant skipped smoke test"
      min_lines: 880
  key_links:
    - from: "test/components/scaffold_selection_actions_test.dart"
      to: "remaining 18 non-skipped tests"
      via: "_pump helper at line 12 and _defaultToolbarBuilder at line 37 — both retained"
      pattern: "_pump\\("
---

<objective>
Close out the only known verification debt in the v1.2 atom suite: delete the
permanently-skipped long-press `SelectionArea` smoke test (D-06) so the package
test suite runs **zero-skip**. Research verified this is a pure deletion — the
helpers and imports stay used by the remaining 17 tests; no cleanup needed.

Purpose: A permanently-skipped test is green-with-asterisk noise. The skipped
test is redundant — 18 non-skipped tests in the same file already cover
`onSelectionChanged` via the deterministic `debugSimulateSelection` hook (25
references). D-06 directs deletion, not unskip-and-fix.

Output: `test/components/scaffold_selection_actions_test.dart` reduced from
929 lines to 887 lines (42 lines removed: lines 334–375 inclusive). Suite runs
with zero skips and the same 454 passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/REQUIREMENTS.md
@.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-CONTEXT.md
@.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-RESEARCH.md
@.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-PATTERNS.md

@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the skipped SelectionArea smoke test (D-06)</name>
  <files>test/components/scaffold_selection_actions_test.dart</files>
  <read_first>
    - test/components/scaffold_selection_actions_test.dart (read lines 1–50 for imports + `_pump`/`_pumpLight`/`_defaultToolbarBuilder` helpers; read lines 320–390 to see the exact block being deleted and the test that follows it at line 377 "Test 18 — the toolbar paints on-screen, centered above the selection")
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-PATTERNS.md (§ "test/components/scaffold_selection_actions_test.dart — MODIFIED — test deletion" for the verbatim diff shape)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-RESEARCH.md (§ "Skipped Test Deletion (D-06) — VERIFIED" for the no-cleanup-needed proof)
  </read_first>
  <action>
    Per D-06, delete exactly lines 334–375 (inclusive) of
    `test/components/scaffold_selection_actions_test.dart` — the comment block
    starting `// Test 11 — SMOKE: real SelectionArea path via longPress + drag.`
    (line 334) through the closing `}, skip: true); // Framework-flaky — see
    comment above.` (line 375). This is 42 lines total: 16 lines of comment
    plus the 26-line `testWidgets(...)` body and its trailing `skip: true`
    argument. Also delete the single blank line at 376 so the prior test's
    closing brace (currently at line 332) is followed by exactly one blank
    line before the next test comment at line 377 (`// Test 18 — the toolbar
    paints on-screen, centered above the selection.`).

    Do NOT touch anything else in the file. Specifically:
    - Leave all 9 imports at lines 1–10 in place — `TestGesture`,
      `startGesture`, `gesture.moveBy`, `gesture.up` are still used by 5 other
      gesture-based tests at lines 456, 459, 483, 585, 588, 594, 675, 678,
      684, 762, 765, 769, 848, 851, 857.
    - Leave `_pump` (line 12), `_pumpLight` (line 21), and
      `_defaultToolbarBuilder` (line 37) in place — used by the remaining 17
      tests.
    - Do not renumber the remaining tests' inline comments ("Test 18", etc.)
      — those are stable identifiers, not sequence counters.

    After the edit, the file MUST be 887 lines (929 − 42). Confirm with
    `wc -l test/components/scaffold_selection_actions_test.dart`.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && wc -l test/components/scaffold_selection_actions_test.dart | awk '{print $1}' | grep -qx 887 && dart analyze --fatal-infos 2>&1 | grep -q "No issues found"</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l test/components/scaffold_selection_actions_test.dart` returns `887` (was 929).
    - `grep -n "skip: true" test/components/scaffold_selection_actions_test.dart` returns zero matches.
    - `grep -n "Test 11 — SMOKE" test/components/scaffold_selection_actions_test.dart` returns zero matches (comment block gone).
    - `grep -n "Test 18 — the toolbar paints" test/components/scaffold_selection_actions_test.dart` returns exactly one match (the next test survived).
    - `dart analyze --fatal-infos` from the package root prints `No issues found!`.
    - `grep -c "_pump(" test/components/scaffold_selection_actions_test.dart` is unchanged (helpers intact, no `unused_element` analyzer hint).
  </acceptance_criteria>
  <done>
    The skipped smoke test and its orphaned comment block are gone, the file
    is exactly 887 lines, and the analyzer is clean with no new
    `unused_element`/`unused_import` warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite + zero-skip sweep gate</name>
  <files>test/components/scaffold_selection_actions_test.dart</files>
  <read_first>
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-VALIDATION.md (§ Per-Task Verification Map rows 11-01-01 and 11-01-02 for the gate commands)
    - .planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-RESEARCH.md (§ "Per-Atom Sweep Matrix" and § "Verification commands to re-run as gate evidence" for the expected outputs)
  </read_first>
  <action>
    Run the WIDG-44 sweep's gate commands from the package root and capture
    their outputs for the phase VERIFICATION artifact:

    1. `dart analyze --fatal-infos` — must print `No issues found!`.
    2. `flutter test` — must report `+454: All tests passed!` with NO `~N`
       skip counter (the `~1` skip must be gone after Task 1).
    3. `grep -rn "skip: true" test/` — must return zero lines.
    4. Per-atom sweep re-verification (D-03 v1.2 scope): for each of the 11
       v1.2 atoms (chip, chip_group, disclosure, trace_list, composer,
       streaming_rich_text, code_block, selection_actions, chart,
       chart_scrubber, chart_range_selector), confirm:
       - `lib/components/scaffold_<name>.dart` exists
       - `test/components/scaffold_<name>_test.dart` exists
       - the corresponding demo file exists under `example/lib/demos/`
       - `lib/frontend_scaffold.dart` exports it (use the grep from
         11-PATTERNS.md § "lib/frontend_scaffold.dart (VERIFY only)"):
         `grep -E "scaffold_(chip|chip_group|disclosure|trace_list|composer|streaming_rich_text|streaming_rich_text_cubit|streaming_rich_text_state|code_block|selection_actions|chart|chart_scrubber|chart_range_selector|chart_renderer)\.dart" lib/frontend_scaffold.dart`
         — expect 14 hits at lines 26, 27, 28, 29, 30, 31, 33, 36, 69, 78,
         79, 80, 83, 108.

    Research has already verified the sweep is green (zero gaps). This task
    re-runs the gates to confirm Task 1's deletion didn't regress anything
    and to capture evidence. If the sweep DOES surface a gap (a missing
    test, demo, or export), per D-05 the gap is fixed inline in this plan —
    append a follow-up task to this plan rather than deferring. Research
    predicts this will not happen.

    Record all four command outputs verbatim — they go into the phase
    `11-VERIFICATION.md` at phase close (not this plan's responsibility to
    author; the execute-plan summary captures them).
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && dart analyze --fatal-infos 2>&1 | grep -q "No issues found" && flutter test 2>&1 | tail -5 | grep -E "\+454: All tests passed" && ! grep -rn "skip: true" test/ 2>/dev/null | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `dart analyze --fatal-infos` prints `No issues found!`.
    - `flutter test` final line matches `+454: All tests passed!` (or `+454 ~0:` — the key is no `~N` for N≥1).
    - `grep -rn "skip: true" test/` returns zero matches across the entire test tree.
    - `grep -E "scaffold_(chip|chip_group|disclosure|trace_list|composer|streaming_rich_text|streaming_rich_text_cubit|streaming_rich_text_state|code_block|selection_actions|chart|chart_scrubber|chart_range_selector|chart_renderer)\.dart" lib/frontend_scaffold.dart | wc -l` returns `14`.
    - `find test -name '*_test.dart' | wc -l` returns `56` (no test file accidentally deleted).
    - `find example/lib/demos -name '*.dart' | wc -l` returns `26` (all demos intact).
  </acceptance_criteria>
  <done>
    All four sweep gates are green: analyzer clean, 454 tests passing with
    zero skips, no `skip: true` anywhere under `test/`, and all 14 v1.2
    exports present in the barrel. The WIDG-44 sweep evidence is captured.
  </done>
</task>

</tasks>

<verification>
Phase-gate alignment: this plan satisfies the WIDG-44 "tests pass + zero
skips" criterion and the "per-atom sweep reports green" criterion for the
test/barrel sides. The demo-side sweep evidence is captured by Plan 11-02
(capture harness iterating the registry proves every demo is pumpable). The
README documentation is Plan 11-03's responsibility.
</verification>

<success_criteria>
- `test/components/scaffold_selection_actions_test.dart` is 887 lines.
- `grep -rn "skip: true" test/` is empty.
- `flutter test` reports `+454: All tests passed!` with zero skips.
- `dart analyze --fatal-infos` is clean.
- All 11 v1.2 atoms verified to have lib + test + demo + barrel export (D-03
  scope; v1.1 atoms NOT re-swept per D-03).
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/11-verification-coverage-gate/11-01-SUMMARY.md` when done. Include:
- The verbatim outputs of `dart analyze --fatal-infos`, `flutter test` (final summary line), `grep -rn "skip: true" test/`, and the barrel-export grep — these become evidence rows in `11-VERIFICATION.md`.
- The file-line count before/after (929 → 887).
- Any D-05 inline gaps found and fixed (research predicts zero; if any surfaced, document the fix).
</output>
