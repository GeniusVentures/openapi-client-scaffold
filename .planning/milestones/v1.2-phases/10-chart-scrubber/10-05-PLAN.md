---
phase: 10-chart-scrubber
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - lib/frontend_scaffold.dart
  - example/lib/demos/chart_demo.dart
  - example/lib/demos/chart_scrubber_demo.dart
  - example/lib/main.dart
autonomous: false
requirements: [WIDG-35, WIDG-36]

must_haves:
  truths:
    - "lib/frontend_scaffold.dart barrel exports components/scaffold_chart.dart, components/scaffold_chart_scrubber.dart, utils/chart_geometry.dart, utils/scaffold_chart_renderer.dart (the support part is exported so consumers can compose with it if they need to replace the renderer)"
    - "example/lib/demos/chart_demo.dart demonstrates ScaffoldChart with: framed chart (plotHeight >= 220) AND axis-free chart (plotHeight < 220), custom xLabelFormatter (epoch-second to time string), lineColor override, empty-series state"
    - "example/lib/demos/chart_scrubber_demo.dart demonstrates ScaffoldChartScrubber with: tap/drag selection updating an external readout, keyboard ArrowLeft/Right navigation, Escape clearing, PointerExit clearing, live-region announcement of the selected value"
    - "example/lib/main.dart registers both demos in the demo index"
    - "Demos use ONLY existing scaffold atoms + theme tokens — no new pubspec deps in example/, no hardcoded colors"
    - "dart analyze --fatal-infos is clean across the whole package"
    - "flutter test passes the full suite (new + existing tests)"
    - "Human UAT confirms: framed and axis-free charts render correctly under BOTH dark and light palettes, scrub interaction feels responsive, keyboard navigation works, focus ring visible, reduced-motion setting honored"
  artifacts:
    - path: "lib/frontend_scaffold.dart"
      provides: "Barrel export"
      contains: "scaffold_chart"
    - path: "example/lib/demos/chart_demo.dart"
      provides: "ScaffoldChart demo (framed + axis-free + formatter + empty)"
      contains: "ScaffoldChart<"
    - path: "example/lib/demos/chart_scrubber_demo.dart"
      provides: "ScaffoldChartScrubber demo with external readout"
      contains: "ScaffoldChartScrubber<"
    - path: "example/lib/main.dart"
      provides: "Demo registration"
      contains: "chart"
  key_links:
    - from: "lib/frontend_scaffold.dart"
      to: "lib/components/scaffold_chart.dart"
      via: "export"
      pattern: "export 'components/scaffold_chart\\.dart'"
    - from: "lib/frontend_scaffold.dart"
      to: "lib/components/scaffold_chart_scrubber.dart"
      via: "export"
      pattern: "export 'components/scaffold_chart_scrubber\\.dart'"
    - from: "example/lib/demos/chart_demo.dart"
      to: "package:frontend_scaffold/frontend_scaffold.dart"
      via: "import"
      pattern: "package:frontend_scaffold/frontend_scaffold\\.dart"
---

<objective>
Close out Phase 10: export the new atoms + geometry + renderer from the barrel, ship two demos proving the atoms work end-to-end under both dark and light palettes, register demos in the example app, run final gates (analyze + tests), and complete human UAT.

Purpose: Demos are the demonstrability contract (D-07 precedent from Phase 9) — every atom must be visible working in the example app, under both palettes, or the phase is not done.

Output: One barrel edit, two demo files, one example main.dart edit, and a human-UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/phases/10-chart-scrubber/10-CONTEXT.md
@.planning/workstreams/scaffold/phases/10-chart-scrubber/10-UI-SPEC.md
@.planning/workstreams/scaffold/phases/10-chart-scrubber/10-03-SUMMARY.md
@.planning/workstreams/scaffold/phases/10-chart-scrubber/10-04-SUMMARY.md

<interfaces>
<!-- New public exports to add to lib/frontend_scaffold.dart (alphabetical within existing lists): -->
<!-- - export 'components/scaffold_chart.dart'; -->
<!-- - export 'components/scaffold_chart_scrubber.dart'; -->
<!-- - export 'utils/chart_geometry.dart'; -->
<!-- - export 'utils/scaffold_chart_renderer.dart'; -->

<!-- Demo file conventions (from existing demos like streaming_rich_text_demo.dart): -->
<!-- - One StatelessWidget or StatefulWidget per demo file, named e.g. ChartDemo / ChartScrubberDemo -->
<!-- - Top-of-file library doc comment naming the atom being demonstrated -->
<!-- - Uses Theme.of(context) via context.palette / context.dimens — no hardcoded colors -->
<!-- - Demo registration happens in example/lib/main.dart by adding the demo widget to the existing demo list -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel-export the four Phase 10 files from lib/frontend_scaffold.dart</name>
  <files>lib/frontend_scaffold.dart</files>
  <read_first>
    - lib/frontend_scaffold.dart (current state — confirm insertion points preserve alphabetical ordering within the components/ and utils/ groups)
  </read_first>
  <action>
    Add four export lines to `lib/frontend_scaffold.dart`:

    1. `export 'components/scaffold_chart.dart';` — insert after `scaffold_chip_group.dart` and before `scaffold_code_block.dart` (alphabetical: chart < chip < code_block — chart before chip).
    2. `export 'components/scaffold_chart_scrubber.dart';` — immediately after the chart export.
    3. `export 'utils/chart_geometry.dart';` — insert before `utils/light_syntax_tokenizer.dart` (alphabetical: chart_geometry < light_syntax_tokenizer).
    4. `export 'utils/scaffold_chart_renderer.dart';` — insert after `utils/scaffold_rich_spans.dart` and before `utils/streaming_announce_policy.dart` (alphabetical: scaffold_chart_renderer < streaming).

    Do NOT reorder any existing exports. Do NOT add comments. Preserve the file's existing 1-blank-line structure between groups.

    Verify with `dart analyze lib/frontend_scaffold.dart --fatal-infos` — the barrel must remain clean.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && grep -q "export 'components/scaffold_chart.dart';" lib/frontend_scaffold.dart && grep -q "export 'components/scaffold_chart_scrubber.dart';" lib/frontend_scaffold.dart && grep -q "export 'utils/chart_geometry.dart';" lib/frontend_scaffold.dart && grep -q "export 'utils/scaffold_chart_renderer.dart';" lib/frontend_scaffold.dart && dart analyze lib/frontend_scaffold.dart --fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - All four export lines present in lib/frontend_scaffold.dart
    - Alphabetical ordering preserved within each group
    - `dart analyze lib/frontend_scaffold.dart --fatal-infos` exits 0
  </acceptance_criteria>
  <done>Barrel exports wired; consumers can `import 'package:frontend_scaffold/frontend_scaffold.dart'` and get the four new publics</done>
</task>

<task type="auto">
  <name>Task 2: Ship example/lib/demos/chart_demo.dart + chart_scrubber_demo.dart and register in main.dart</name>
  <files>example/lib/demos/chart_demo.dart, example/lib/demos/chart_scrubber_demo.dart, example/lib/main.dart</files>
  <read_first>
    - example/lib/demos/streaming_rich_text_demo.dart (demo file shape — StatelessWidget, library doc, demo registration pattern)
    - example/lib/demos/code_block_demo.dart (theme-aware demo pattern — uses context.palette, no hardcoded colors)
    - example/lib/main.dart (current demo index — find the list where new demos are added)
    - lib/components/scaffold_chart.dart (Plan 03 — atom API)
    - lib/components/scaffold_chart_scrubber.dart (Plan 04 — atom API)
  </read_first>
  <action>
    Create `example/lib/demos/chart_demo.dart`:

    - Library doc: "ScaffoldChart demo — framed + axis-free + formatter + empty state."
    - Widget: `class ChartDemo extends StatelessWidget`.
    - Build: a Scaffold with a scrollable ListView containing 4 sections, each introduced by a Text header (using context.textTheme.titleSmall) and separated by SizedBox(height: dimens.space8):
      1. "Framed chart (plotHeight 280)": SizedBox(height: 280, child: ScaffoldChart<Offset>(series: sampleData, xAccessor: (o) => o.dx, yAccessor: (o) => o.dy)) — sampleData is a static list of 24 Offsets with x = epoch-second-style values (e.g. 1692000000 + i*3600) and y oscillating in a 50-150 range.
      2. "Axis-free chart (plotHeight 96)": SizedBox(height: 96, child: ScaffoldChart<Offset>(...same series...)).
      3. "Custom formatter + line color": plotHeight 220, xLabelFormatter: (x) => DateTime.fromMillisecondsSinceEpoch(x.toInt() * 1000).toIso8601String().substring(11, 16) (HH:mm), lineColor: context.palette.statusSuccess.
      4. "Empty state": SizedBox(height: 220, child: ScaffoldChart<Offset>(series: const [], xAccessor: (o) => o.dx, yAccessor: (o) => o.dy)).

    Create `example/lib/demos/chart_scrubber_demo.dart`:

    - Library doc: "ScaffoldChartScrubber demo — selection + keyboard + PointerExit + live region."
    - Widget: `class ChartScrubberDemo extends StatefulWidget` (state holds the selected Offset?).
    - State: `Offset? _selected; String? _announce;`
    - Build: Scaffold + Column:
      1. A readout row at top: Text(_selected == null ? 'No selection' : 'Value: ${_selected!.dy.toStringAsFixed(2)}', style: context.textTheme.bodyMedium). This is the consumer-rendered external readout per D-05.
      2. SizedBox(height: dimens.space4).
      3. SizedBox(height: 280, child: ScaffoldChartScrubber<Offset>(series: same sampleData, xAccessor: ..., yAccessor: ..., selectedPoint: _selected, onPointSelected: (v) => setState(() { _selected = v; _announce = v == null ? null : 'Value ${v.dy.toStringAsFixed(2)}'; }), announceValue: _announce)).
      4. SizedBox(height: dimens.space4).
      5. A short Text helper (style: context.textTheme.labelSmall, color: palette.textSecondary): 'Tap or drag to scrub. Arrow keys navigate. Enter confirms. Escape or hover-exit clears.'

    Edit `example/lib/main.dart`: register both demos in the existing demo list/index. Add entries following the existing pattern (e.g. `('Chart', (_) => const ChartDemo())` and `('Chart Scrubber', (_) => const ChartScrubberDemo())` — adapt to the actual main.dart structure).

    Style rules for demos: theme tokens only (context.palette, context.dimens, context.textTheme); no hardcoded colors; Allman braces; `final` locals.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && grep -q "class ChartDemo" example/lib/demos/chart_demo.dart && grep -q "class ChartScrubberDemo" example/lib/demos/chart_scrubber_demo.dart && grep -q "ScaffoldChart<Offset>" example/lib/demos/chart_demo.dart && grep -q "ScaffoldChartScrubber<Offset>" example/lib/demos/chart_scrubber_demo.dart && grep -qE "ChartDemo|ChartScrubberDemo" example/lib/main.dart && dart analyze example/ --fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - example/lib/demos/chart_demo.dart exists with 4 sections (framed, axis-free, formatter+color, empty)
    - example/lib/demos/chart_scrubber_demo.dart exists with readout row + scrubber + helper text
    - example/lib/main.dart registers both demos
    - No hardcoded Color(0x...) literals in demo files (grep returns 0 — statusSuccess/palette references only)
    - `dart analyze example/ --fatal-infos` exits 0
  </acceptance_criteria>
  <done>Both demos shipped and registered; example app compiles clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final gates + human UAT of chart + scrubber under both palettes</name>
  <what-built>
    Phase 10 complete: ScaffoldChart<T> (WIDG-35), ScaffoldChartScrubber<T> (WIDG-36), pure chart_geometry support part, isolated fl_chart renderer support part, barrel exports, two demos registered in example app.
  </what-built>
  <how-to-verify>
    1. Run final gates:
       - `cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && dart analyze --fatal-infos` — expect 0 issues across the package.
       - `flutter test` — expect ALL tests passing (existing + new chart tests). Look for "+N: All tests passed!" with N matching the previous count plus ~30 new chart tests.
       - `grep -rln "package:fl_chart" lib/components/` — expect EMPTY output (no fl_chart leakage into base atoms, D-02 gate).
       - `grep -rln "package:fl_chart" lib/utils/` — expect exactly ONE file: `lib/utils/scaffold_chart_renderer.dart`.
    2. Visual UAT — framed chart (dark palette):
       - `cd example && flutter run -d macos` (or your preferred device).
       - Open the "Chart" demo. Confirm the framed chart (plotHeight 280) renders with: right-side Y-axis labels using tabular figures, X-axis labels as a plain Row of Texts at the bottom, horizontal grid lines (subtle), below-bar area gradient fading to transparent at ~62% height, line in `lightGreenPrimary` (#00EAAE on dark).
    3. Visual UAT — axis-free chart:
       - Scroll to the axis-free section (plotHeight 96). Confirm NO Y-axis labels on the right, NO X-axis label row at the bottom, NO grid lines. The line renders mid-plot with label bands reserved above and below.
    4. Visual UAT — empty state:
       - Scroll to "Empty state". Confirm the chart surface renders as an empty padded surface (SizedBox.shrink inside) — no crash, no phantom axes.
    5. Visual UAT — scrubber (dark palette):
       - Open the "Chart Scrubber" demo.
       - Tap and drag on the chart. Confirm: scrub dot (radius 5, strokeWidth 4) follows the finger; vertical rule at the touch position; NO floating tooltip; the external readout at the top updates with "Value: N.NN".
       - Move the pointer off the chart. Confirm the readout clears to "No selection" (PointerExit pattern).
       - Click on the chart to focus it, then press ArrowRight/ArrowLeft. Confirm the selection advances/retreats one point at a time and the readout updates.
       - Press Enter. Confirm onPointSelected fires with the current selection (consumer state unchanged visually — this is a confirmation hook).
       - Press Escape. Confirm the selection clears.
       - Confirm the focus ring (2px, focusRingColor) is visible when the scrubber has keyboard focus.
    6. Visual UAT — light palette:
       - Toggle the example app to light mode (follow the example app's existing theme toggle if present; otherwise override the platform brightness).
       - Repeat sections 2-5. Confirm: all colors switch to the light palette variants, contrast remains readable, no white-on-white or black-on-black text.
    7. Reduced motion:
       - Enable reduced motion in your OS accessibility settings (macOS: System Settings → Accessibility → Display → Reduce motion) and reload the app.
       - Confirm the scrub dot appear/disappear is instant (no 150ms animation).
  </how-to-verify>
  <resume-signal>Type "approved" if all gates pass and the UAT confirms the 3 phase success criteria (neutral contract, scrubber composition, dual-palette rendering). Describe any failures with the specific section number (1-7) and what you observed.</resume-signal>
</task>

</tasks>

<verification>
- `dart analyze --fatal-infos` exits 0 across the package (lib + test + example)
- `flutter test` passes all tests
- Zero fl_chart imports under `lib/components/` (D-02 gate)
- Exactly one fl_chart import under `lib/utils/` (scaffold_chart_renderer.dart)
- Barrel exports present
- Both demos registered
- Human UAT signed off
</verification>

<success_criteria>
Phase 10 success criteria (from ROADMAP) met:
1. `ScaffoldChart` renders a series supplied via the neutral data contract (`series`, `xAccessor`, `yAccessor`) with no domain knowledge in the widget — verified by Plan 03 atom + Plan 05 demo.
2. `ScaffoldChartScrubber` composes with `ScaffoldChart` to expose `selectedPoint` and fire `onPointSelected` on tap/drag — verified by Plan 04 atom + Plan 05 demo.
3. Chart + scrubber render correctly under both dark and light palettes with M3 theme tokens only — verified by Task 3 human UAT.

WIDG-35 and WIDG-36 closed.
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/10-chart-scrubber/10-05-SUMMARY.md` when done
</output>
