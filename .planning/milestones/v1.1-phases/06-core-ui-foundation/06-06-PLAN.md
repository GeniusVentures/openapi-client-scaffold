---
phase: 06-core-ui-foundation
plan: "06"
type: execute
wave: 6
# Exec wave 6/6 → dependency layer Wave 3. The 6 exec waves realize 4 canonical layers: Wave 0 = plans 01+02, Wave 1 = 03+04, Wave 2 = 05, Wave 3 = 06.
depends_on:
  - 06-05
files_modified:
  - templates/components/card.dart.jinja2
  - templates/components/card_cubit.dart.jinja2
  - templates/components/card_state.dart.jinja2
  - templates/components/card_vars.json
  - templates/components/state.dart.jinja2
  - templates/components/state_cubit.dart.jinja2
  - templates/components/state_view_state.dart.jinja2
  - templates/components/state_vars.json
  - templates/components/search_bar.dart.jinja2
  - templates/components/search_bar_cubit.dart.jinja2
  - templates/components/search_bar_state.dart.jinja2
  - templates/components/search_bar_vars.json
  - lib/components/scaffold_card.dart
  - lib/components/scaffold_card_cubit.dart
  - lib/components/scaffold_card_state.dart
  - lib/components/scaffold_state_view.dart
  - lib/components/scaffold_state_view_cubit.dart
  - lib/components/scaffold_state_view_state.dart
  - lib/components/scaffold_search_bar.dart
  - lib/components/scaffold_search_bar_cubit.dart
  - lib/components/scaffold_search_bar_state.dart
  - lib/frontend_scaffold.dart
  - test/components/scaffold_card_test.dart
  - test/components/scaffold_state_view_test.dart
  - test/components/scaffold_search_bar_test.dart
  - example/lib/demos/kitchen_sink_demo.dart
autonomous: true
requirements:
  - WIDG-26
  - WIDG-27
  - WIDG-28
user_setup: []

must_haves:
  truths:
    - "ScaffoldCard is generated from card.dart.jinja2 via engine.py and composes ScaffoldSurface + ScaffoldPressable for elevated/outlined/filled variants with header/body/actions slots — no raw Material Card or GestureDetector"
    - "ScaffoldStateView is generated from state.dart.jinja2 via engine.py and composes ScaffoldSkeleton (loading) + ScaffoldStatusIndicator + ScaffoldPressable (empty/error/unavailable/success) — no raw Material ElevatedButton/OutlinedButton/Card"
    - "ScaffoldSearchBar is generated from search_bar.dart.jinja2 via engine.py and composes ScaffoldSurface + ScaffoldFocusOutline + ScaffoldStatusIndicator + ScaffoldBadge + ScaffoldLiveRegion — no raw Material InputDecoration/ListTile container"
    - "All 3 generated composites keep Cubit (flutter_bloc + hydrated_bloc) for hydrated variant state (cardVariant / stateType / query), matching the existing template architecture and the UI-SPEC 'Cubit not Riverpod' correction"
    - "Generated composites carry the source-schema + generator-version header and are committed; a drift gate regenerates via engine.py and diffs — any difference fails (templates are source-of-truth, generated output is never hand-edited)"
    - "All 3 composites consume only atoms shipped in Plans 01-05, Theme.of(context), and context.palette/dimens — no Riverpod, no GeniusTheme, no app-specific logic (per D-04)"
    - "Kitchen-sink demo renders all 28 atoms + ScaffoldMotion in a scrollable gallery with reducedMotion toggle"
  artifacts:
    - templates/components/card.dart.jinja2 (updated to consume atoms) + card_cubit/state/vars.json companions
    - templates/components/state.dart.jinja2 (updated to consume atoms) + state_cubit/state_view_state/vars.json companions
    - templates/components/search_bar.dart.jinja2 (updated to consume atoms) + search_bar_cubit/state/vars.json companions
    - lib/components/scaffold_card.dart + scaffold_card_cubit.dart + scaffold_card_state.dart
    - lib/components/scaffold_state_view.dart + scaffold_state_view_cubit.dart + scaffold_state_view_state.dart
    - lib/components/scaffold_search_bar.dart + scaffold_search_bar_cubit.dart + scaffold_search_bar_state.dart
    - test/components/scaffold_card_test.dart
    - test/components/scaffold_state_view_test.dart
    - test/components/scaffold_search_bar_test.dart
    - example/lib/demos/kitchen_sink_demo.dart
  key_links:
    - "card.dart.jinja2 → engine.py (Jinja2 StrictUndefined) → lib/components/scaffold_card.dart — drift gate regenerates + diffs"
    - "state.dart.jinja2 → engine.py → lib/components/scaffold_state_view.dart — drift gate regenerates + diffs"
    - "search_bar.dart.jinja2 → engine.py → lib/components/scaffold_search_bar.dart — drift gate regenerates + diffs"
    - "ScaffoldCard → ScaffoldSurface(01)+ScaffoldPressable(03)+ScaffoldElevation(Phase5)+ScaffoldMotion(01)"
    - "ScaffoldStateView → ScaffoldSkeleton(03)+ScaffoldStatusIndicator(03)+ScaffoldPressable(03)"
    - "ScaffoldSearchBar → ScaffoldSurface(01)+ScaffoldPressable(03)+ScaffoldStatusIndicator(03)+ScaffoldBadge(03)+ScaffoldFocusOutline(01)+ScaffoldLiveRegion(02)+ScaffoldMotion(01)"
    - "Kitchen sink → all 28 atoms + ScaffoldMotion from Plans 01-05"
---

<objective>
Update the 3 existing Jinja2 templates (card, state, search_bar) and their _cubit/_state/_vars.json companions to compose the Phase 6 atoms instead of raw Material widgets, then generate the 3 Wave 3 composites (ScaffoldCard, ScaffoldStateView, ScaffoldSearchBar) from those templates via the Jinja2 engine (engine.py) with a CI drift gate. The generated composites keep the Cubit (flutter_bloc + hydrated_bloc) architecture for hydrated variant state. Create the kitchen-sink demo gallery proving all 28 atoms + ScaffoldMotion compose and render correctly.

Purpose: Deliver the consumer-facing composite widgets from the existing template pipeline (not hand-written) and prove the full library works end-to-end. This plan completes Phase 6.

Output: 3 updated template sets, 3 generated composites (each with _cubit/_state), widget tests, kitchen-sink demo, barrel exports, and a drift gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md
@.planning/workstreams/scaffold/ROADMAP.md
@src/scaffold/engine.py
@src/scaffold/design_tokens.json
@src/scaffold/templates/components/card.dart.jinja2
@src/scaffold/templates/components/card_cubit.dart.jinja2
@src/scaffold/templates/components/card_state.dart.jinja2
@src/scaffold/templates/components/card_vars.json
@src/scaffold/templates/components/state.dart.jinja2
@src/scaffold/templates/components/state_cubit.dart.jinja2
@src/scaffold/templates/components/state_view_state.dart.jinja2
@src/scaffold/templates/components/state_vars.json
@src/scaffold/templates/components/search_bar.dart.jinja2
@src/scaffold/templates/components/search_bar_cubit.dart.jinja2
@src/scaffold/templates/components/search_bar_state.dart.jinja2
@src/scaffold/templates/components/search_bar_vars.json
@src/scaffold/lib/theme/scaffold_theme.dart
@src/scaffold/lib/theme/scaffold_palette.dart
@src/scaffold/lib/theme/scaffold_dimens.dart
@src/scaffold/lib/theme/scaffold_elevation.dart
@src/scaffold/lib/components/scaffold_surface.dart
@src/scaffold/lib/components/scaffold_pressable.dart
@src/scaffold/lib/components/scaffold_focus_outline.dart
@src/scaffold/lib/components/scaffold_live_region.dart
@src/scaffold/lib/components/scaffold_skeleton.dart
@src/scaffold/lib/components/scaffold_badge.dart
@src/scaffold/lib/components/scaffold_status_indicator.dart
@src/scaffold/lib/components/scaffold_motion.dart
@src/scaffold/lib/frontend_scaffold.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update card/state/search_bar Jinja2 templates to compose Phase 6 atoms + Scaffold class names</name>
  <files>templates/components/card.dart.jinja2, templates/components/card_cubit.dart.jinja2, templates/components/card_state.dart.jinja2, templates/components/card_vars.json, templates/components/state.dart.jinja2, templates/components/state_cubit.dart.jinja2, templates/components/state_view_state.dart.jinja2, templates/components/state_vars.json, templates/components/search_bar.dart.jinja2, templates/components/search_bar_cubit.dart.jinja2, templates/components/search_bar_state.dart.jinja2, templates/components/search_bar_vars.json</files>
  <read_first>src/scaffold/templates/components/card.dart.jinja2, src/scaffold/templates/components/state.dart.jinja2, src/scaffold/templates/components/search_bar.dart.jinja2, src/scaffold/templates/components/card_cubit.dart.jinja2, src/scaffold/templates/components/state_cubit.dart.jinja2, src/scaffold/templates/components/search_bar_cubit.dart.jinja2, src/scaffold/templates/components/card_state.dart.jinja2, src/scaffold/templates/components/state_view_state.dart.jinja2, src/scaffold/templates/components/search_bar_state.dart.jinja2, src/scaffold/templates/components/card_vars.json, src/scaffold/templates/components/state_vars.json, src/scaffold/templates/components/search_bar_vars.json, src/scaffold/engine.py, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md (Wave 3 composites section), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md</read_first>
  <action>
    Update the 3 existing template sets so the generated composites consume Phase 6 atoms instead of raw Material widgets, while PRESERVING the existing Cubit (flutter_bloc) + hydrated_bloc state architecture and the Jinja2 StrictUndefined contract. Per D-04 (pure composability) and the UI-SPEC Wave 3 contract:

    card.dart.jinja2 — replace the raw Card/Card.outlined/Card.filled + GestureDetector with ScaffoldSurface (variant → surface treatment: elevated = elevation 4 + ScaffoldElevation.card + palette.deepBlueCardColor; outlined = elevation 0 + 1px palette.borderSubtle border; filled = elevation 0 + palette.surfaceElevated) and ScaffoldPressable (when onTap non-null; disabled → ScaffoldDisabledOverlay). Keep the header/body/actions slot wiring and the state.cardVariant runtime selection driven by {{ widget_class_name }}Cubit.

    state.dart.jinja2 — replace the raw Card/Icon/ElevatedButton/OutlinedButton and the `Loading` widget with: loading → ScaffoldSkeleton; empty/error/unavailable/success → ScaffoldStatusIndicator (status + label) + icon + headline (textTheme.headlineSmall) + body (textTheme.bodyLarge) + optional action via ScaffoldPressable (retry/CTA). Keep the BlocConsumer state.stateType switch and the showToast error path (Phase 5 toast_manager remains valid for error toasts).

    search_bar.dart.jinja2 — replace the raw TextField InputDecoration/Card with ScaffoldSurface (dimens.radiusPill pill container) + ScaffoldFocusOutline (focused ring) + ScaffoldStatusIndicator (isLoading) + ScaffoldBadge (resultCount) + ScaffoldLiveRegion (result-count announcement). Keep the TextEditingController + FocusNode + _handleQueryChanged cubit wiring; the grouped-results panel may keep ListTile for results, but the input surface + trailing slots use atoms.

    For all 3: update the _vars.json fixtures so widget_class_name is "ScaffoldCard" / "ScaffoldStateView" / "ScaffoldSearchBar" respectively (matching the ROADMAP/UI-SPEC contract names). Update the hardcoded relative imports inside each .dart.jinja2 (e.g. `import 'card_cubit.dart';` → `import 'scaffold_card_cubit.dart';`, `import 'card_state.dart';` → `import 'scaffold_card_state.dart';`, and the state template's `import 'state_view_state.dart';` → `import 'scaffold_state_view_state.dart';`) so the generated files reference their own _cubit/_state companions. Keep the generated-code header + generator-version comment convention. Do NOT introduce Riverpod, GeniusTheme, or app-specific logic. Templates render with StrictUndefined — do not introduce unguarded undefined variables.
  </action>
  <verify>
    <automated>cd src/scaffold && python3 engine.py --template components/card.dart.jinja2 --output /tmp/06_card_probe.dart --template-dir templates/ --tokens design_tokens.json --vars templates/components/card_vars.json && python3 engine.py --template components/state.dart.jinja2 --output /tmp/06_state_probe.dart --template-dir templates/ --tokens design_tokens.json --vars templates/components/state_vars.json && python3 engine.py --template components/search_bar.dart.jinja2 --output /tmp/06_search_probe.dart --template-dir templates/ --tokens design_tokens.json --vars templates/components/search_bar_vars.json</automated>
  </verify>
  <acceptance_criteria>
    - All 3 templates render via engine.py (Jinja2 StrictUndefined) with zero UndefinedError
    - card.dart.jinja2 references ScaffoldSurface + ScaffoldPressable (grep confirms), not raw Card/GestureDetector
    - state.dart.jinja2 references ScaffoldSkeleton + ScaffoldStatusIndicator + ScaffoldPressable (grep confirms), not raw Card/ElevatedButton/OutlinedButton
    - search_bar.dart.jinja2 references ScaffoldSurface + ScaffoldFocusOutline + ScaffoldStatusIndicator + ScaffoldBadge + ScaffoldLiveRegion (grep confirms)
    - All 3 _vars.json set widget_class_name to ScaffoldCard/ScaffoldStateView/ScaffoldSearchBar (grep confirms)
    - All 3 .dart.jinja2 import statements reference the scaffold_-prefixed _cubit/_state filenames (grep confirms no bare card_cubit/state_cubit/search_bar_cubit imports remain)
  </acceptance_criteria>
  <done>3 template sets updated to compose Phase 6 atoms with Scaffold* class names, render cleanly via engine.py, and preserve the Cubit/hydrated_bloc architecture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate composites via engine.py + drift gate + widget tests + barrel exports</name>
  <files>lib/components/scaffold_card.dart, lib/components/scaffold_card_cubit.dart, lib/components/scaffold_card_state.dart, lib/components/scaffold_state_view.dart, lib/components/scaffold_state_view_cubit.dart, lib/components/scaffold_state_view_state.dart, lib/components/scaffold_search_bar.dart, lib/components/scaffold_search_bar_cubit.dart, lib/components/scaffold_search_bar_state.dart, test/components/scaffold_card_test.dart, test/components/scaffold_state_view_test.dart, test/components/scaffold_search_bar_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/engine.py, src/scaffold/templates/components/card.dart.jinja2, src/scaffold/templates/components/state.dart.jinja2, src/scaffold/templates/components/search_bar.dart.jinja2, src/scaffold/templates/components/card_vars.json, src/scaffold/templates/components/state_vars.json, src/scaffold/templates/components/search_bar_vars.json, src/scaffold/lib/theme/scaffold_theme.dart, src/scaffold/lib/components/scaffold_surface.dart, src/scaffold/lib/components/scaffold_pressable.dart, src/scaffold/lib/components/scaffold_focus_outline.dart, src/scaffold/lib/components/scaffold_live_region.dart, src/scaffold/lib/components/scaffold_skeleton.dart, src/scaffold/lib/components/scaffold_badge.dart, src/scaffold/lib/components/scaffold_status_indicator.dart, src/scaffold/lib/components/scaffold_motion.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldCard(variant: elevated) renders a ScaffoldSurface with ScaffoldElevation.card + palette.deepBlueCardColor
    - Test 2: ScaffoldCard(variant: outlined) renders elevation 0 + 1px palette.borderSubtle border
    - Test 3: ScaffoldCard(variant: filled) renders elevation 0 + palette.surfaceElevated
    - Test 4: ScaffoldCard with onTap wraps in ScaffoldPressable; tap fires onTap
    - Test 5: ScaffoldCard header/body/actions slots render with correct spacing
    - Test 6: ScaffoldStateView(state: loading) renders ScaffoldSkeleton
    - Test 7: ScaffoldStateView(state: error) renders ScaffoldStatusIndicator(error) + headline + retry ScaffoldPressable
    - Test 8: ScaffoldStateView(state: success) renders ScaffoldStatusIndicator(success) + headline
    - Test 9: ScaffoldSearchBar idle renders ScaffoldSurface pill + TextField + hintText
    - Test 10: ScaffoldSearchBar with query shows clear; clear empties query
    - Test 11: ScaffoldSearchBar isLoading shows ScaffoldStatusIndicator; resultCount>0 shows ScaffoldBadge
  </behavior>
  <action>
    Generate the 3 composites FROM the updated templates via the Jinja2 engine — do NOT hand-write the composite Dart files. Run engine.py for each composite (StrictUndefined), passing --tokens design_tokens.json and --vars the component's _vars.json, e.g.:
      python3 engine.py --template components/card.dart.jinja2 --output lib/components/scaffold_card.dart --template-dir templates/ --tokens design_tokens.json --vars templates/components/card_vars.json
    (and the analogous render for state → scaffold_state_view.dart, search_bar → scaffold_search_bar.dart). The _cubit/_state companions render from their own templates with the same vars: card_cubit.dart.jinja2 → scaffold_card_cubit.dart, card_state.dart.jinja2 → scaffold_card_state.dart, state_cubit.dart.jinja2 → scaffold_state_view_cubit.dart, state_view_state.dart.jinja2 → scaffold_state_view_state.dart, search_bar_cubit.dart.jinja2 → scaffold_search_bar_cubit.dart, search_bar_state.dart.jinja2 → scaffold_search_bar_state.dart. The generated composites keep Cubit (flutter_bloc + hydrated_bloc) for hydrated variant state.

    Write the 3 widget tests per the behavior block, exercising the generated composites through their own internal BlocProvider. Initialize/override HydratedBloc storage in the test setup so the generated cubit's hydrate() call is safe under flutter_test. Export all 9 generated files (3 composites + 6 _cubit/_state) from lib/frontend_scaffold.dart. Run dart analyze --fatal-infos across all generated files.

    Drift gate: regenerate each composite via engine.py into a temp file and diff against the committed lib/components/ file — zero difference required (templates are source-of-truth). Wire the same regenerate-and-diff step into CI so any hand-edit to a generated file, or any divergence between template and committed output, fails the build. Generated files carry the source-schema + generator-version header from the templates.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_card_test.dart test/components/scaffold_state_view_test.dart test/components/scaffold_search_bar_test.dart && dart analyze --fatal-infos lib/components/scaffold_card.dart lib/components/scaffold_card_cubit.dart lib/components/scaffold_card_state.dart lib/components/scaffold_state_view.dart lib/components/scaffold_state_view_cubit.dart lib/components/scaffold_state_view_state.dart lib/components/scaffold_search_bar.dart lib/components/scaffold_search_bar_cubit.dart lib/components/scaffold_search_bar_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 3 composites + 6 _cubit/_state files generated from templates via engine.py and present in lib/components/
    - All 11 tests pass; dart analyze --fatal-infos clean across all 9 generated files
    - Generated files carry the source-schema + generator-version header (grep confirms "do not edit by hand")
    - ScaffoldCard composes ScaffoldSurface + ScaffoldPressable (grep confirms imports)
    - ScaffoldStateView composes ScaffoldSkeleton + ScaffoldStatusIndicator + ScaffoldPressable (grep confirms imports)
    - ScaffoldSearchBar composes ScaffoldSurface + ScaffoldFocusOutline + ScaffoldStatusIndicator + ScaffoldBadge + ScaffoldLiveRegion (grep confirms imports)
    - Barrel exports all 9 generated files (grep confirms export lines)
    - Drift gate passes: regenerating each composite via engine.py and diffing against the committed file produces zero difference (templates are source-of-truth)
  </acceptance_criteria>
  <done>3 composites generated from the updated templates with Cubit/hydrated_bloc architecture, tested (11 tests), barrel-exported, drift-gate clean.</done>
</task>

<task type="auto">
  <name>Task 3: Kitchen-sink demo — render all 28 atoms + ScaffoldMotion in scrollable gallery</name>
  <files>example/lib/demos/kitchen_sink_demo.dart</files>
  <read_first>src/scaffold/lib/frontend_scaffold.dart (all 28 atom exports + 3 generated composites), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md (all visual contracts)</read_first>
  <action>
    Create example/lib/demos/kitchen_sink_demo.dart. StatefulWidget wrapping ScaffoldMotion(reducedMotion: _reduced) as ancestor. Scaffold with AppBar("Core UI Foundation — Kitchen Sink") and ListView body. Each section: labeled header + ScaffoldSurface card containing the atom demo. Include a Switch at top to toggle reducedMotion. Render every atom:

    Wave 0: Motion info card (durations/curves table), Surface (colored rect), TouchTarget (small icon in 48px box), FocusOutline (text field with focus), LiveRegion (label), OverflowFade (long text), ScrollEdgeIndicator (scroll list), ResponsiveVisibility (toggle), FormattedValue (number/money/date), ColorSwatch (3 colors)

    Wave 1: Skeleton (animated), Badge (dot/count/icon/text), StatusIndicator (5 colors), SelectionIndicator (radio/checkbox/toggle), ImagePlaceholder (loading/failed), AnimatedDisplay (fade+pulse), Pressable (tap counter), DisabledOverlay (dimmed), DragHandle, ResizeHandle (corner), NumericInput (0-100)

    Wave 2: SelectableSurface (toggle), Draggable (chip+handle), DropTarget (zone), FileInputSurface (drop area)

    Wave 3: ScaffoldCard (elevated/outlined), ScaffoldStateView (loading/empty), ScaffoldSearchBar (interactive) — the generated composites, imported via the barrel.

    The demo must import the frontend_scaffold barrel (or all individual atoms). Add a note in the demo file header on how to wire into the example app's main.dart.
  </action>
  <verify>
    <automated>cd src/scaffold && dart analyze --fatal-infos example/lib/demos/kitchen_sink_demo.dart</automated>
  </verify>
  <acceptance_criteria>
    - kitchen_sink_demo.dart exists and dart analyze --fatal-infos returns zero issues
    - Demo imports all 28 widget atoms + 3 generated composites (grep confirms frontend_scaffold import)
    - Demo renders inside ScaffoldMotion ancestor (grep confirms)
    - Demo includes reducedMotion Switch toggle (grep confirms)
    - Demo groups widgets by wave with labeled sections
    - Demo verifiable by running `cd src/scaffold/example && flutter run` and seeing no red-screen errors
  </acceptance_criteria>
  <done>Kitchen-sink demo created, dart analyze clean, all 28 atoms + ScaffoldMotion + 3 generated composites rendered in gallery. Phase 6 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller text input → SearchBar TextField | SearchBar sanitizes before onQueryChanged callback; no XSS in Flutter. |
| Template + _vars.json → generated Dart | Jinja2 rendering is the trust boundary: a malformed vars/tokens input or a hand-edit to generated output can drift from the template source-of-truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Denial of Service | SearchBar TextEditingController leak | low | mitigate | Controller+FocusNode disposed in widget dispose() |
| T-06-12 | Tampering | Generated composite drift (hand-edited vs template) | low | mitigate | Drift gate: CI regenerates via engine.py and diffs against committed output; any difference fails the build (templates are source-of-truth, generated code never hand-edited) |
</threat_model>

<verification>
- `cd src/scaffold && flutter test test/components/scaffold_card_test.dart test/components/scaffold_state_view_test.dart test/components/scaffold_search_bar_test.dart` — all green (11 tests)
- `cd src/scaffold && dart analyze --fatal-infos lib/components/scaffold_card.dart lib/components/scaffold_card_cubit.dart lib/components/scaffold_card_state.dart lib/components/scaffold_state_view.dart lib/components/scaffold_state_view_cubit.dart lib/components/scaffold_state_view_state.dart lib/components/scaffold_search_bar.dart lib/components/scaffold_search_bar_cubit.dart lib/components/scaffold_search_bar_state.dart example/lib/demos/kitchen_sink_demo.dart` — zero issues
- `cd src/scaffold && flutter test` — full suite confirmation, no regressions from barrel updates
- Grep barrel for all 9 generated-file export lines
- Drift gate: regenerate each composite via engine.py and diff against the committed file — zero difference
</verification>

<success_criteria>
1. ScaffoldCard generated from card.dart.jinja2, composes ScaffoldSurface + ScaffoldPressable, renders elevated/outlined/filled variants; interactive when onTap non-null (D-04)
2. ScaffoldStateView generated from state.dart.jinja2, composes ScaffoldSkeleton + ScaffoldStatusIndicator + ScaffoldPressable across loading/empty/error/unavailable/success (D-02)
3. ScaffoldSearchBar generated from search_bar.dart.jinja2, composes ScaffoldSurface + ScaffoldFocusOutline + ScaffoldStatusIndicator + ScaffoldBadge + ScaffoldLiveRegion (D-02)
4. All 3 composites keep Cubit (flutter_bloc + hydrated_bloc) for hydrated variant state; drift gate regenerates + diffs clean
5. Kitchen-sink demo renders all 28 atoms + ScaffoldMotion + 3 generated composites in scrollable gallery with reducedMotion toggle
6. dart analyze --fatal-infos clean across all generated files; all tests green; barrel exports updated
7. Phase 6 complete — 28 atoms + ScaffoldMotion + 3 generated composites shipped, exported, tested, demoed
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Classes (generated from templates — not hand-written)
- ScaffoldCard (generated StatelessWidget) — lib/components/scaffold_card.dart
- ScaffoldCardCubit (generated Cubit + HydratedMixin) — lib/components/scaffold_card_cubit.dart
- ScaffoldCardState (generated state class) — lib/components/scaffold_card_state.dart
- ScaffoldStateView (generated StatelessWidget) — lib/components/scaffold_state_view.dart
- ScaffoldStateViewCubit (generated Cubit + HydratedMixin) — lib/components/scaffold_state_view_cubit.dart
- ScaffoldStateViewState (generated state class) — lib/components/scaffold_state_view_state.dart
- ScaffoldSearchBar (generated StatefulWidget) — lib/components/scaffold_search_bar.dart
- ScaffoldSearchBarCubit (generated Cubit + HydratedMixin) — lib/components/scaffold_search_bar_cubit.dart
- ScaffoldSearchBarState (generated state class) — lib/components/scaffold_search_bar_state.dart
- KitchenSinkDemo (StatefulWidget) — example/lib/demos/kitchen_sink_demo.dart

### Modified templates (source-of-truth)
- card.dart.jinja2 + card_cubit.dart.jinja2 + card_state.dart.jinja2 + card_vars.json
- state.dart.jinja2 + state_cubit.dart.jinja2 + state_view_state.dart.jinja2 + state_vars.json
- search_bar.dart.jinja2 + search_bar_cubit.dart.jinja2 + search_bar_state.dart.jinja2 + search_bar_vars.json
</parameter>
