---
phase: 08-supporting-atoms-table-cells-light-palette
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/scaffold_chip.dart
  - lib/components/scaffold_chip_group.dart
  - test/components/scaffold_chip_test.dart
  - test/components/scaffold_chip_group_test.dart
  - example/lib/demos/chip_demo.dart
autonomous: true
requirements: [WIDG-40]

must_haves:
  truths:
    - "ScaffoldChip renders a pill-shaped pressable atom with optional leading icon, label, and trailing ScaffoldStatusIndicator"
    - "ScaffoldChip selected state is a 2px lightGreenPrimary BORDER (not fill change)"
    - "ScaffoldChip min hit area is 48px via ScaffoldTouchTarget — not by padding arithmetic"
    - "ScaffoldChip constructor asserts non-null semanticLabel when icon-only (label == null && icon != null)"
    - "ScaffoldChipGroup lays out chips via Wrap with space8 (16px) spacing and space4 (8px) runSpacing"
    - "ScaffoldChipGroup holds NO selection truth — selection is consumer-owned via selected Set<int> + onSelectionChanged callback"
    - "ScaffoldChipGroup supports single-select (radio semantics) and multi-select (group semantics) modes"
    - "ScaffoldChipGroup renders SizedBox.shrink() when chips list is empty"
    - "Both atoms render correctly under ThemeData.light() with lightPalette"
  artifacts:
    - path: "lib/components/scaffold_chip.dart"
      provides: "ScaffoldChip atom — pill pressable with optional icon/label/status slots"
      exports: ["ScaffoldChip"]
    - path: "lib/components/scaffold_chip_group.dart"
      provides: "ScaffoldChipGroup atom — Wrap layout + selection dispatch"
      exports: ["ScaffoldChipGroup"]
    - path: "test/components/scaffold_chip_test.dart"
      provides: "Widget tests for chip contract across both palettes, selected/disabled/icon-only states, and a11y"
    - path: "test/components/scaffold_chip_group_test.dart"
      provides: "Widget tests for selection modes, empty state, layout spacing, and semantics role"
    - path: "example/lib/demos/chip_demo.dart"
      provides: "Demo exercising default/selected/disabled/status/icon-only chips and a chip group"
  key_links:
    - from: "lib/components/scaffold_chip.dart"
      to: "lib/components/scaffold_pressable.dart"
      via: "Chip interaction handled by ScaffoldPressable which supplies ScaffoldTouchTarget internally"
      pattern: "ScaffoldPressable\\("
    - from: "lib/components/scaffold_chip_group.dart"
      to: "lib/components/scaffold_chip.dart"
      via: "ChipGroup wraps each consumer-supplied chip and forwards taps to onSelectionChanged"
      pattern: "ScaffoldChip\\("
---

<objective>
Ship `ScaffoldChip` and `ScaffoldChipGroup` — pill-shaped pressable token atom and selection-management group layout — each with widget tests across both palettes and a demo file.

Purpose: Implements D-01 / D-02 / D-03 from 08-CONTEXT.md and WIDG-40. Unlocks "Tool Chips" and "Filter Table → chip/group" consumer patterns.

Output: Two new atoms in `lib/components/`, two new test files in `test/components/`, one new demo file in `example/lib/demos/`. Barrel export and demo registration in `example/lib/main.dart` are deferred to plan 08-06 to avoid same-wave file conflicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-CONTEXT.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md
@.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md

@lib/components/scaffold_surface.dart
@lib/components/scaffold_pressable.dart
@lib/components/scaffold_touch_target.dart
@lib/components/scaffold_status_indicator.dart
@lib/components/scaffold_disabled_overlay.dart
@lib/components/scaffold_selectable_surface.dart
@lib/components/scaffold_badge.dart
@lib/theme/scaffold_theme.dart
@lib/theme/scaffold_dimens.dart
@test/components/scaffold_badge_test.dart
@test/components/media_controls_test.dart
@example/lib/demos/media_card_demo.dart

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/components/scaffold_surface.dart:
```dart
class ScaffoldSurface extends StatelessWidget {
  const ScaffoldSurface({
    required this.child,
    this.color,
    this.borderRadius,
    this.border,
    this.padding,
    ...
  });
}
```

From lib/components/scaffold_pressable.dart:
```dart
class ScaffoldPressable extends StatelessWidget {
  const ScaffoldPressable({
    required this.child,
    this.onPressed,
    this.onLongPress,
    this.disabled = false,
    this.semanticLabel,
    ...
  });
  // Internally wraps child in: ScaffoldTouchTarget (48x48) → Stack(state layer)
  //   → MouseRegion/GestureDetector → ScaffoldFocusOutline → Focus
  //   → Semantics(button: true). Disabled wraps in ScaffoldDisabledOverlay.
}
```

From lib/components/scaffold_status_indicator.dart:
```dart
enum StatusVariant { success, warning, error, info, neutral }
class ScaffoldStatusIndicator extends StatelessWidget {
  const ScaffoldStatusIndicator({required this.status, this.dotSize = 8, ...});
}
```

From lib/theme/scaffold_dimens.dart:
```dart
class ScaffoldDimens extends ThemeExtension<ScaffoldDimens> {
  final double space2;         // 4
  final double space3;         // 6
  final double space4;         // 8
  final double space8;         // 16
  final double radiusPill;     // 48
  final double minTouchTarget; // 48
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing widget tests for ScaffoldChip + ScaffoldChipGroup</name>
  <files>test/components/scaffold_chip_test.dart, test/components/scaffold_chip_group_test.dart</files>
  <read_first>
    - test/components/scaffold_badge_test.dart (pattern analog for pump helper shape and simple atom assertions)
    - test/components/media_controls_test.dart (pattern analog for callback assertions on a composite)
    - lib/components/scaffold_pressable.dart (the pressable contract — Semantics(button: true), disabled, semanticLabel)
    - lib/theme/scaffold_dimens.dart (token names — space2/space4/space8/radiusPill)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (sections "scaffold_chip.dart" and "scaffold_chip_group.dart" — the locked contract)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-UI-SPEC.md (the "ScaffoldChip" and "ScaffoldChipGroup" widget contracts — paddings, gaps, selection modes)
  </read_first>
  <behavior>
    Chip tests (test/components/scaffold_chip_test.dart):
    - Test 1: Default chip renders label text with `textTheme.labelMedium` style and `palette.deepBlueCardColor` fill.
    - Test 2: Selected chip renders a 2px `palette.lightGreenPrimary` border AND retains the default `palette.deepBlueCardColor` fill (NOT a fill change).
    - Test 3: Tapping the chip fires `onPressed` exactly once.
    - Test 4: Disabled chip does NOT fire `onPressed` on tap and renders the `ScaffoldDisabledOverlay`.
    - Test 5: Icon + label chip renders icon then label with `dimens.space2` (4px) gap.
    - Test 6: Icon + label + status chip renders icon, label, and `ScaffoldStatusIndicator` with `dimens.space2` gaps between each.
    - Test 7: Icon-only chip constructor asserts when `semanticLabel` is null — `expect(() => ScaffoldChip(icon: Icons.add, onPressed: () {}), throwsAssertionError)`.
    - Test 8: Icon-only chip with `semanticLabel: 'Add'` pumps without error and the Semantics node carries that label.
    - Test 9: Semantics node reports `button: true` and `selected: true/false` matching the `selected` parameter.
    - Test 10: Renders under `lightPalette` without exception.

    Chip-group tests (test/components/scaffold_chip_group_test.dart):
    - Test 11: Empty `chips` list renders `SizedBox.shrink()`.
    - Test 12: Single-select mode — tapping chip index 1 calls `onSelectionChanged` with `{1}`; tapping chip index 2 next calls with `{2}` (not `{1, 2}`).
    - Test 13: Multi-select mode — tapping index 1 calls with `{1}`; tapping index 2 next calls with `{1, 2}`; tapping index 1 again calls with `{2}`.
    - Test 14: Tapping an already-selected chip in single-select mode keeps it selected (calls with `{1}` again — consumer decides whether to no-op).
    - Test 15: Selected state propagates — chip at index in `selected` set renders with the selected border.
    - Test 16: Wrap layout — verified by locating a `Wrap` widget with `spacing: dimens.space8` and `runSpacing: dimens.space4`.
    - Test 17: Single-select mode Semantics reports `radiogroup` role; multi-select reports `group` role.
    - Test 18: Renders under `lightPalette` without exception.
  </behavior>
  <action>Create both test files with `_pump` and `_pumpLight` helpers matching the pattern in `scaffold_badge_test.dart` and plan 08-03. Write the eighteen tests above against the (not-yet-existing) `ScaffoldChip` and `ScaffoldChipGroup` classes — importing from `package:frontend_scaffold/components/scaffold_chip.dart` and `package:frontend_scaffold/components/scaffold_chip_group.dart`. For Test 7, wrap the constructor invocation in `expect(..., throwsAssertionError)` and ensure the test runs in a debug build context (assertions enabled — `flutter test` enables them by default). For Test 16, locate the `Wrap` via `tester.widget<Wrap>(find.byType(Wrap))` and assert on its `spacing` and `runSpacing` properties. Run `flutter test test/components/scaffold_chip_test.dart test/components/scaffold_chip_group_test.dart` — the run MUST fail because the imported files do not yet exist (RED state).</action>
  <verify>
    <automated>flutter test test/components/scaffold_chip_test.dart test/components/scaffold_chip_group_test.dart 2>&amp;1 | grep -E "(Error|Could not find|Target of URI)" &amp;&amp; echo "RED confirmed"</automated>
  </verify>
  <done>Both test files exist with the eighteen tests, and `flutter test` reports compile/import errors because the implementations do not yet exist (RED state).</done>
  <acceptance_criteria>
    - `test/components/scaffold_chip_test.dart` exists
    - `test/components/scaffold_chip_group_test.dart` exists
    - `grep -c "testWidgets" test/components/scaffold_chip_test.dart` returns ≥ 10
    - `grep -c "testWidgets" test/components/scaffold_chip_group_test.dart` returns ≥ 8
    - `grep -c "throwsAssertionError" test/components/scaffold_chip_test.dart` returns ≥ 1
    - `flutter test test/components/scaffold_chip_test.dart test/components/scaffold_chip_group_test.dart` exits non-zero (RED)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ScaffoldChip as pill surface + pressable + slots</name>
  <files>lib/components/scaffold_chip.dart</files>
  <read_first>
    - lib/components/scaffold_chip.dart (will not exist — file is being created)
    - lib/components/scaffold_selectable_surface.dart (selected-surface contract — pattern analog for selected state)
    - lib/components/scaffold_badge.dart (pill geometry + slot-composition pattern)
    - lib/components/scaffold_pressable.dart (the interaction wrapper)
    - lib/components/scaffold_surface.dart (the pill container)
    - lib/components/scaffold_status_indicator.dart (the trailing status slot)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (section "scaffold_chip.dart" — the full implementation shape including the a11y assert)
    - test/components/scaffold_chip_test.dart (the failing tests from Task 1 — they are the contract)
  </read_first>
  <behavior>
    - All ten chip tests from Task 1 flip from RED to GREEN.
    - `dart analyze --fatal-infos lib/components/scaffold_chip.dart` is clean.
  </behavior>
  <action>Create `lib/components/scaffold_chip.dart` as a StatelessWidget. Open with the standard library-doc header (`/// ScaffoldChip — M3 pill-shaped pressable atom.` plus a paragraph describing composition of ScaffoldSurface (radiusPill) + ScaffoldPressable + ScaffoldTouchTarget (48px min via pressable) and the selected-state contract (2px lightGreenPrimary BORDER, not fill); then `library;`). Constructor takes optional `String? label`, `IconData? icon`, `StatusVariant? status`, `bool selected = false`, `bool disabled = false`, `String? semanticLabel`, `VoidCallback? onPressed`. Add a constructor assert: `assert(label != null || semanticLabel != null, 'Icon-only ScaffoldChip requires semanticLabel (WCAG 4.1.2)')`. In `build`, resolve `palette`, `dimens`, `textTheme`. Build the internal row children list: if `icon != null` add `Icon(icon, size: 16)`; if `label != null` add (with `SizedBox(width: dimens.space2)` separator if not first) `Text(label!, style: textTheme.labelMedium)`; if `status != null` add (with separator if not first) `ScaffoldStatusIndicator(status: status!, dotSize: 8)`. Wrap the row in `Row(mainAxisSize: MainAxisSize.min, children: rowChildren)`. Build the pill surface: `ScaffoldSurface(color: palette.deepBlueCardColor, borderRadius: BorderRadius.circular(dimens.radiusPill), border: selected ? Border.all(color: palette.lightGreenPrimary, width: 2.0) : null, padding: EdgeInsets.symmetric(horizontal: dimens.space4, vertical: dimens.space4), child: row)`. Wrap the surface in `ScaffoldPressable(onPressed: onPressed, disabled: disabled, semanticLabel: semanticLabel, child: surface)`. Wrap that in `Semantics(selected: selected, child: ...)` to add the selected flag (the pressable already adds `button: true`). Re-run the chip tests — all ten MUST pass.</action>
  <verify>
    <automated>flutter test test/components/scaffold_chip_test.dart &amp;&amp; dart analyze --fatal-infos lib/components/scaffold_chip.dart</automated>
  </verify>
  <done>All ten chip tests pass; `dart analyze --fatal-infos` is clean; the file consumes only `Theme.of(context)` via `context.palette`/`context.dimens` (no hardcoded colors or dims).</done>
  <acceptance_criteria>
    - `lib/components/scaffold_chip.dart` exists
    - `grep -c "ScaffoldPressable(" lib/components/scaffold_chip.dart` returns ≥ 1
    - `grep -c "ScaffoldSurface(" lib/components/scaffold_chip.dart` returns ≥ 1
    - `grep -c "radiusPill" lib/components/scaffold_chip.dart` returns ≥ 1
    - `grep -c "Icon-only ScaffoldChip requires semanticLabel" lib/components/scaffold_chip.dart` returns ≥ 1 (the D-03 assert)
    - `grep -c "Border.all(color: palette.lightGreenPrimary, width: 2.0)" lib/components/scaffold_chip.dart` returns ≥ 1
    - `grep -nE "Colors\.(white|black|grey|blue|red)" lib/components/scaffold_chip.dart` returns no matches
    - `grep -nE "EdgeInsets\.(all|symmetric|only)\([0-9]" lib/components/scaffold_chip.dart` returns no matches
    - `flutter test test/components/scaffold_chip_test.dart` exits zero
    - `dart analyze --fatal-infos lib/components/scaffold_chip.dart` exits zero
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ScaffoldChipGroup with consumer-owned selection dispatch</name>
  <files>lib/components/scaffold_chip_group.dart</files>
  <read_first>
    - lib/components/scaffold_chip_group.dart (will not exist — file is being created)
    - lib/components/scaffold_chip.dart (the per-item atom composed by the group — created in Task 2)
    - lib/components/responsive_grid.dart (layout-only composite pattern analog)
    - lib/components/scaffold_selection_indicator_radio.dart (radio semantics pattern analog)
    - lib/components/scaffold_selection_indicator_checkbox.dart (group semantics pattern analog)
    - .planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-PATTERNS.md (section "scaffold_chip_group.dart" — the Wrap layout and selection dispatch patterns)
    - test/components/scaffold_chip_group_test.dart (the failing tests from Task 1 — they are the contract)
  </read_first>
  <behavior>
    - All eight chip-group tests from Task 1 flip from RED to GREEN.
    - `dart analyze --fatal-infos lib/components/scaffold_chip_group.dart` is clean.
  </behavior>
  <action>Create `lib/components/scaffold_chip_group.dart` as a StatelessWidget. Open with the standard library-doc header (`/// ScaffoldChipGroup — M3 chip-set layout with consumer-owned selection.` plus a paragraph; then `library;`). Import `dart:ui` for `SemanticsRole`. Constructor takes `required List<ScaffoldChip> chips`, `Set<int> selected = const <int>{}`, `ValueChanged<Set<int>>? onSelectionChanged`, `bool multiSelect = false`. In `build`, return `const SizedBox.shrink()` when `chips.isEmpty`. Otherwise build `Wrap(spacing: dimens.space8, runSpacing: dimens.space4, children: [for (int i = 0; i < chips.length; i++) _wrapChipAtIndex(i, chips[i])])`. The `_wrapChipAtIndex` helper constructs a new `ScaffoldChip` forwarding the source chip's `label`, `icon`, `status`, `disabled`, `semanticLabel`, but with `selected: selected.contains(index)` and `onPressed: chip.onPressed == null ? null : () => _handleChipTap(index)`. The `_handleChipTap` method copies `selected` into a new `Set<int>`, then for multi-select toggles the index (add if absent, remove if present); for single-select clears the set and adds the tapped index. Calls `onSelectionChanged?.call(next)` with the new set. Wrap the `Wrap` in `Semantics(role: multiSelect ? SemanticsRole.group : SemanticsRole.radiogroup, child: wrap)`. Re-run the chip-group tests — all eight MUST pass.</action>
  <verify>
    <automated>flutter test test/components/scaffold_chip_group_test.dart &amp;&amp; dart analyze --fatal-infos lib/components/scaffold_chip_group.dart</automated>
  </verify>
  <done>All eight chip-group tests pass; `dart analyze --fatal-infos` is clean; the file consumes only `Theme.of(context)` via `context.dimens` (no hardcoded colors or dims).</done>
  <acceptance_criteria>
    - `lib/components/scaffold_chip_group.dart` exists
    - `grep -c "Wrap(" lib/components/scaffold_chip_group.dart` returns ≥ 1
    - `grep -c "spacing: dimens.space8" lib/components/scaffold_chip_group.dart` returns ≥ 1
    - `grep -c "runSpacing: dimens.space4" lib/components/scaffold_chip_group.dart` returns ≥ 1
    - `grep -c "SizedBox.shrink" lib/components/scaffold_chip_group.dart` returns ≥ 1 (empty state)
    - `grep -c "SemanticsRole.radiogroup\|SemanticsRole.group" lib/components/scaffold_chip_group.dart` returns ≥ 2
    - `grep -c "onSelectionChanged" lib/components/scaffold_chip_group.dart` returns ≥ 2 (field + call site)
    - `grep -nE "Colors\.(white|black|grey|blue|red)" lib/components/scaffold_chip_group.dart` returns no matches
    - `flutter test test/components/scaffold_chip_group_test.dart` exits zero
    - `dart analyze --fatal-infos lib/components/scaffold_chip_group.dart` exits zero
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Author chip_demo.dart</name>
  <files>example/lib/demos/chip_demo.dart</files>
  <read_first>
    - example/lib/demos/media_card_demo.dart (pattern analog — sectioned vertical layout)
    - example/lib/demos/wallet_connect_sheet_demo.dart (pattern analog for interactive demos with local state)
    - lib/components/scaffold_chip.dart (the widget being demonstrated)
    - lib/components/scaffold_chip_group.dart (the widget being demonstrated)
  </read_first>
  <action>Create `example/lib/demos/chip_demo.dart` exposing a `ScaffoldChipDemo` StatelessWidget. Render a `Scaffold(appBar: AppBar(title: const Text('ScaffoldChip')), body: SingleChildScrollView(padding: EdgeInsets.all(dimens.itemSpacing), child: Column(...)))`. Sections: (1) "Default" — `ScaffoldChip(label: 'Chip', onPressed: () {})`; (2) "Selected" — same with `selected: true`; (3) "Disabled" — same with `disabled: true`; (4) "With leading icon" — `ScaffoldChip(label: 'Add', icon: Icons.add, onPressed: () {})`; (5) "With status" — `ScaffoldChip(label: 'Active', status: StatusVariant.success, onPressed: () {})`; (6) "Icon only" — `ScaffoldChip(icon: Icons.close, semanticLabel: 'Close', onPressed: () {})`; (7) "ChipGroup (single-select)" — a `StatefulBuilder` holding a local `Set<int> _selected` driving a `ScaffoldChipGroup(chips: [ScaffoldChip(label: 'One'), ScaffoldChip(label: 'Two'), ScaffoldChip(label: 'Three')], selected: _selected, onSelectionChanged: (next) => setState(() => _selected = next))`; (8) "ChipGroup (multi-select)" — same with `multiSelect: true`; (9) "Empty group" — `ScaffoldChipGroup(chips: const [])`. Import from `package:frontend_scaffold/components/scaffold_chip.dart`, `package:frontend_scaffold/components/scaffold_chip_group.dart`, and `package:frontend_scaffold/components/scaffold_status_indicator.dart` directly. Do NOT register the demo in `example/lib/main.dart` — that lands in plan 08-06.</action>
  <verify>
    <automated>dart analyze --fatal-infos example/lib/demos/chip_demo.dart</automated>
  </verify>
  <done>The demo file exists, compiles cleanly under `dart analyze --fatal-infos`, and is NOT yet registered in `example/lib/main.dart`.</done>
  <acceptance_criteria>
    - `example/lib/demos/chip_demo.dart` exists
    - `grep -c "ScaffoldChipDemo" example/lib/demos/chip_demo.dart` returns ≥ 1
    - `grep -c "ScaffoldChip(" example/lib/demos/chip_demo.dart` returns ≥ 6 (six single-chip sections)
    - `grep -c "ScaffoldChipGroup(" example/lib/demos/chip_demo.dart` returns ≥ 3 (single-select, multi-select, empty)
    - `dart analyze --fatal-infos example/lib/demos/chip_demo.dart` exits zero
    - `grep -c "chip_demo" example/lib/main.dart` returns 0 (registration deferred to plan 08-06)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → ScaffoldChip/ScaffoldChipGroup | consumer supplies labels, icons, status, callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Consumer-supplied selection Set | accept | In-process API contract — consumer owns selection truth per D-02 |
| T-08-04-02 | Denial of Service | Huge chips list | accept | Consumer responsibility — Wrap lays out unbounded children; no virtualization in atom |
| T-08-04-03 | Information Disclosure | Semantics labels | mitigate | D-03 enforced via constructor assert: icon-only chips REQUIRE semanticLabel — accessibility spoofing prevented at API level |
| T-08-04-04 | Elevation of Privilege | None — atoms hold no secrets/state | accept | Stateless render + private transient state only — no privilege surface |
</threat_model>

<verification>
- `flutter test test/components/scaffold_chip_test.dart test/components/scaffold_chip_group_test.dart` exits zero.
- `dart analyze --fatal-infos lib/components/scaffold_chip.dart lib/components/scaffold_chip_group.dart test/components/scaffold_chip_test.dart test/components/scaffold_chip_group_test.dart example/lib/demos/chip_demo.dart` exits zero.
- No hardcoded colors or dims in either new component file.
- The demo file exists and is NOT yet registered in `example/lib/main.dart` (deferred to plan 08-06).
</verification>

<success_criteria>
- `ScaffoldChip` renders a pressable token with icon/text and optional status indicator; selected state is a 2px lightGreenPrimary BORDER; icon-only chips REQUIRE semanticLabel.
- `ScaffoldChipGroup` lays out chips via Wrap with the locked spacing, holds no selection truth, and dispatches single- vs multi-select changes via `onSelectionChanged`.
- Both atoms render correctly under both palettes, have widget tests, and a demo file (registration deferred to plan 08-06).
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/08-supporting-atoms-table-cells-light-palette/08-04-SUMMARY.md` when done.
</output>
