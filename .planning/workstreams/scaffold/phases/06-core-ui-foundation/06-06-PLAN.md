---
phase: 06-core-ui-foundation
plan: "06"
type: execute
wave: 6
depends_on:
  - 06-05
files_modified:
  - lib/components/scaffold_card.dart
  - lib/components/scaffold_state_view.dart
  - lib/components/scaffold_search_bar.dart
  - lib/frontend_scaffold.dart
  - example/lib/demos/kitchen_sink_demo.dart
autonomous: true
requirements:
  - WIDG-26
  - WIDG-27
  - WIDG-28
user_setup: []

must_haves:
  truths:
    - "ScaffoldCard renders elevated/outlined/filled variants composing ScaffoldSurface + ScaffoldPressable (when onTap non-null) with header/body/actions slots"
    - "ScaffoldCard elevated: elevation=4 with ScaffoldElevation.card shadow, background=deepBlueCardColor; outlined: elevation=0, 1px borderSubtle border; filled: elevation=0, surfaceElevated bg"
    - "ScaffoldStateView renders loading/empty/error/unavailable/success states composing Skeleton+StatusIndicator (loading), icon+headline+body+optional action (empty/error/unavailable/success)"
    - "ScaffoldSearchBar renders pill-shaped Surface with search icon, TextField, clear/loading/filtered/resultCount trailing slots, FocusOutline, Semantics search + LiveRegion (per D-02)"
    - "All 3 composites consume only atoms shipped in Plans 01-05, Theme.of(context), and context.palette/dimens — no Riverpod, no GeniusTheme, no app-specific logic (per D-04)"
    - "Kitchen-sink demo renders all 28 atoms + ScaffoldMotion in a scrollable gallery with reducedMotion toggle"
  artifacts:
    - lib/components/scaffold_card.dart
    - lib/components/scaffold_state_view.dart
    - lib/components/scaffold_search_bar.dart
    - example/lib/demos/kitchen_sink_demo.dart
  key_links:
    - "ScaffoldCard → Surface(01)+Pressable(03)+Elevation(Phase5)+Motion(01)"
    - "ScaffoldStateView → Skeleton(03)+StatusIndicator(03)+Pressable(03)"
    - "ScaffoldSearchBar → Surface(01)+Pressable(03)+StatusIndicator(03)+Badge(03)+FocusOutline(01)+LiveRegion(02)+Motion(01)"
    - "Kitchen sink → all 28 atoms + ScaffoldMotion from Plans 01-05"
---

<objective>
Ship the 3 Wave 3 template-generated composites (ScaffoldCard, ScaffoldStateView, ScaffoldSearchBar) that compose atoms from Waves 0-2 into high-level consumer-ready widgets. Create the final kitchen-sink demo gallery proving all 28 atoms + ScaffoldMotion compose and render correctly.

Purpose: Deliver the consumer-facing composite widgets and prove the full library works end-to-end. This plan completes Phase 6.

Output: 3 composite widgets, kitchen-sink demo, barrel exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md
@.planning/workstreams/scaffold/ROADMAP.md
@src/scaffold/templates/components/card.dart.jinja2
@src/scaffold/templates/components/state.dart.jinja2
@src/scaffold/templates/components/search_bar.dart.jinja2
@src/scaffold/lib/theme/scaffold_theme.dart
@src/scaffold/lib/theme/scaffold_palette.dart
@src/scaffold/lib/theme/scaffold_dimens.dart
@src/scaffold/lib/theme/scaffold_elevation.dart
@src/scaffold/lib/components/scaffold_surface.dart
@src/scaffold/lib/components/scaffold_pressable.dart
@src/scaffold/lib/components/scaffold_focus_outline.dart
@src/scaffold/lib/components/scaffold_live_region.dart
@src/scaffold/lib/components/scaffold_skeleton.dart
@src/scaffold/lib/components/scaffold_badge.dart
@src/scaffold/lib/components/scaffold_status_indicator.dart
@src/scaffold/lib/components/scaffold_motion.dart
@src/scaffold/lib/frontend_scaffold.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScaffoldCard — elevated/outlined/filled card with header/body/actions slots</name>
  <files>lib/components/scaffold_card.dart, test/components/scaffold_card_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/templates/components/card.dart.jinja2, src/scaffold/lib/components/scaffold_surface.dart, src/scaffold/lib/components/scaffold_pressable.dart, src/scaffold/lib/theme/scaffold_elevation.dart, src/scaffold/lib/theme/scaffold_palette.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldCard(variant: elevated, header: H, body: B) renders with ScaffoldElevation.card shadow on deepBlueCardColor surface, 16px padding
    - Test 2: ScaffoldCard(variant: outlined) renders elevation=0, 1px palette.borderSubtle border
    - Test 3: ScaffoldCard(variant: filled) renders elevation=0, palette.surfaceElevated background
    - Test 4: ScaffoldCard with onTap wraps in ScaffoldPressable; tap fires onTap
    - Test 5: ScaffoldCard without onTap renders non-interactive Surface only
    - Test 6: ScaffoldCard disabled applies DisabledOverlay and blocks onTap
    - Test 7: ScaffoldCard with header+body+actions renders all slots with correct spacing
  </behavior>
  <action>
    Create lib/components/scaffold_card.dart. StatelessWidget. Constructor: CardVariant variant=CardVariant.elevated, Widget? header, Widget? body, List<Widget> actions=const [], VoidCallback? onTap, bool disabled=false. Define enum CardVariant { elevated, outlined, filled }.

    Per UI-SPEC: variant selects the ScaffoldSurface treatment. elevated: elevation=4, shadow=ScaffoldElevation.card, color=palette.deepBlueCardColor. outlined: elevation=0, border=Border.all(palette.borderSubtle,1), color=Colors.transparent. filled: elevation=0, color=palette.surfaceElevated. BorderRadius: dimens.borderRadiusCard.

    Interior slots: Padding(EdgeInsets.all(dimens.space8)) wrapping Column(crossAxisAlignment: stretch, mainAxisSize: min) containing header, SizedBox(dimens.space8), body, SizedBox(dimens.space8), actions Row (right-aligned, gap=dimens.space4). Omit spacer when adjacent slot is null. Empty fallback: SizedBox(height:48, Center(Text("No content", style:textTheme.bodyMedium))).

    Interactive: when onTap non-null, wrap in ScaffoldPressable(child: cardSurface, onPressed: onTap, disabled:disabled). Per D-04: composition, not baked-in logic. No Cubit/hydrated_bloc in this implementation. Add barrel export. No hardcoded hex values.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_card_test.dart && dart analyze --fatal-infos lib/components/scaffold_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_card_test.dart has 7 test cases
    - All 7 tests pass; dart analyze --fatal-infos clean
    - Elevated uses ScaffoldElevation.card (grep confirms import)
    - Filled uses palette.surfaceElevated (grep confirms)
    - Interactive variant imports ScaffoldPressable (grep confirms)
    - Barrel exports scaffold_card.dart; no hardcoded hex values
  </acceptance_criteria>
  <done>ScaffoldCard created with elevated/outlined/filled variants, 7 tests green, barrel-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScaffoldStateView + ScaffoldSearchBar — state display + pill search composite</name>
  <files>lib/components/scaffold_state_view.dart, lib/components/scaffold_search_bar.dart, test/components/scaffold_state_view_test.dart, test/components/scaffold_search_bar_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/templates/components/state.dart.jinja2, src/scaffold/templates/components/search_bar.dart.jinja2, src/scaffold/lib/components/scaffold_skeleton.dart, src/scaffold/lib/components/scaffold_status_indicator.dart, src/scaffold/lib/components/scaffold_pressable.dart, src/scaffold/lib/components/scaffold_surface.dart, src/scaffold/lib/components/scaffold_focus_outline.dart, src/scaffold/lib/components/scaffold_live_region.dart, src/scaffold/lib/components/scaffold_badge.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldStateView(state: loading) renders centered Skeleton + StatusIndicator
    - Test 2: ScaffoldStateView(state: empty) renders Icons.inbox_outlined 64px + headline + body + optional action
    - Test 3: ScaffoldStateView(state: error) renders Icons.error_outline 64px statusError + headline + message + retry
    - Test 4: ScaffoldStateView(state: unavailable) renders Icons.cloud_off 64px + headline + message
    - Test 5: ScaffoldStateView(state: success) renders Icons.check_circle 64px statusSuccess + headline + message
    - Test 6: onRetry callback fires and re-triggers loading state
    - Test 7: ScaffoldSearchBar idle renders pill Surface with search icon + TextField + hintText "Search..."
    - Test 8: ScaffoldSearchBar with query shows Icons.close clear button; clear empties query
    - Test 9: ScaffoldSearchBar isLoading=true shows StatusIndicator in trailing slot
    - Test 10: ScaffoldSearchBar isFiltered=true shows Icons.filter_list in lightGreenPrimary tint
    - Test 11: ScaffoldSearchBar resultCount>0 shows ScaffoldBadge(count: resultCount)
  </behavior>
  <action>
    **scaffold_state_view.dart:** StatelessWidget. Constructor: required StateViewState state, Widget? loadingWidget, IconData emptyIcon=Icons.inbox_outlined, String? emptyHeadline, String? emptyBody, Widget? emptyAction, String? errorHeadline, String? errorBody, VoidCallback? onRetry, String? unavailableHeadline, String? unavailableBody, IconData successIcon=Icons.check_circle, String? successHeadline, String? successBody, Widget? successAction. Define enum StateViewState { loading, empty, error, unavailable, success }.

    Switch on state per UI-SPEC contract: loading→centered Skeleton(200x200, dimens.borderRadiusCard)+StatusIndicator, empty→64px icon(palette.textSecondary)+headline(textTheme.headlineSmall)+body(textTheme.bodyLarge)+optional action, error→64px Icons.error_outline(palette.statusError)+headline+body+retry Pressable, unavailable→64px Icons.cloud_off+headline+body, success→64px successIcon(palette.statusSuccess)+headline+body+optional action. Headlines=textTheme.headlineSmall, body=textTheme.bodyLarge. Semantics role status (D-02). Consumer-configurable defaults match UI-SPEC copywriting contract.

    **scaffold_search_bar.dart:** StatefulWidget. Constructor: String? query, ValueChanged<String>? onQueryChanged, VoidCallback? onClear, VoidCallback? onFilterTap, bool isFiltered=false, int resultCount=0, String hintText="Search...", ValueChanged<String>? onSubmitted, bool isLoading=false. Internal TextEditingController+FocusNode.

    Build: ScaffoldSurface(borderRadius: dimens.radiusPill) wrapping Row: leading Icons.search(24px, palette.textSecondary), Expanded TextField(no border, textTheme.bodyLarge, hintText, controller), trailing Row: clear IconButton(Icons.close, shown when query non-empty), StatusIndicator(status:neutral) when isLoading, filter IconButton(Icons.filter_list, color: isFiltered?lightGreenPrimary:textSecondary) when onFilterTap non-null, ScaffoldBadge(count:resultCount) when resultCount>0. Focus: wrap in ScaffoldFocusOutline. A11y: Semantics search + LiveRegion for result count (D-02). ConstrainedBox(minWidth:200). Plain Dart — no Cubit. Add barrel exports. No hardcoded hex values.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_state_view_test.dart test/components/scaffold_search_bar_test.dart && dart analyze --fatal-infos lib/components/scaffold_state_view.dart lib/components/scaffold_search_bar.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_state_view_test.dart has 6 test cases
    - test/components/scaffold_search_bar_test.dart has 5 test cases
    - All 11 tests pass; dart analyze --fatal-infos clean on both files
    - StateView imports Skeleton, StatusIndicator, Pressable (grep confirms)
    - SearchBar imports Surface, FocusOutline, Badge, StatusIndicator, LiveRegion (grep confirms)
    - SearchBar minWidth 200 enforced (grep confirms ConstrainedBox)
    - SearchBar Semantics role search (grep confirms)
    - Barrel exports both files; no hardcoded hex values
  </acceptance_criteria>
  <done>ScaffoldStateView and ScaffoldSearchBar created, 11 tests green, barrel-exported.</done>
</task>

<task type="auto">
  <name>Task 3: Kitchen-sink demo — render all 28 atoms + ScaffoldMotion in scrollable gallery</name>
  <files>example/lib/demos/kitchen_sink_demo.dart</files>
  <read_first>src/scaffold/lib/frontend_scaffold.dart (all 28 atom exports), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md (all visual contracts)</read_first>
  <action>
    Create example/lib/demos/kitchen_sink_demo.dart. StatefulWidget wrapping ScaffoldMotion(reducedMotion: _reduced) as ancestor. Scaffold with AppBar("Core UI Foundation — Kitchen Sink") and ListView body. Each section: labeled header + ScaffoldSurface card containing the atom demo. Include a Switch at top to toggle reducedMotion. Render every atom:

    Wave 0: Motion info card (durations/curves table), Surface (colored rect), TouchTarget (small icon in 48px box), FocusOutline (text field with focus), LiveRegion (label), OverflowFade (long text), ScrollEdgeIndicator (scroll list), ResponsiveVisibility (toggle), FormattedValue (number/money/date), ColorSwatch (3 colors)

    Wave 1: Skeleton (animated), Badge (dot/count/icon/text), StatusIndicator (5 colors), SelectionIndicator (radio/checkbox/toggle), ImagePlaceholder (loading/failed), AnimatedDisplay (fade+pulse), Pressable (tap counter), DisabledOverlay (dimmed), DragHandle, ResizeHandle (corner), NumericInput (0-100)

    Wave 2: SelectableSurface (toggle), Draggable (chip+handle), DropTarget (zone), FileInputSurface (drop area)

    Wave 3: ScaffoldCard (elevated/outlined), StateView (loading/empty), SearchBar (interactive)

    The demo must import the frontend_scaffold barrel (or all individual atoms). Add note in demo file header on how to wire into example app's main.dart.
  </action>
  <verify>
    <automated>cd src/scaffold && dart analyze --fatal-infos example/lib/demos/kitchen_sink_demo.dart</automated>
  </verify>
  <acceptance_criteria>
    - kitchen_sink_demo.dart exists and dart analyze --fatal-infos returns zero issues
    - Demo imports all 28 widget atoms (grep confirms frontend_scaffold import)
    - Demo renders inside ScaffoldMotion ancestor (grep confirms)
    - Demo includes reducedMotion Switch toggle (grep confirms)
    - Demo groups widgets by wave with labeled sections
    - Demo verifiable by running `cd src/scaffold/example && flutter run` and seeing no red-screen errors
  </acceptance_criteria>
  <done>Kitchen-sink demo created, dart analyze clean, all 28 atoms + ScaffoldMotion rendered in gallery. Phase 6 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller text input → SearchBar TextField | SearchBar sanitizes before onQueryChanged callback; no XSS in Flutter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Denial of Service | SearchBar TextEditingController leak | low | mitigate | Controller+FocusNode disposed in widget dispose() |
</threat_model>

<verification>
- `cd src/scaffold && flutter test test/components/scaffold_card_test.dart test/components/scaffold_state_view_test.dart test/components/scaffold_search_bar_test.dart` — all green (18 tests)
- `cd src/scaffold && dart analyze --fatal-infos lib/components/scaffold_card.dart lib/components/scaffold_state_view.dart lib/components/scaffold_search_bar.dart example/lib/demos/kitchen_sink_demo.dart` — zero issues
- `cd src/scaffold && flutter test` — full suite confirmation, no regressions from barrel updates
- Grep barrel for all 3 export lines
</verification>

<success_criteria>
1. ScaffoldCard renders elevated/outlined/filled variants; interactive when onTap non-null; disabled state blocks interaction (D-04)
2. ScaffoldStateView renders loading/empty/error/unavailable/success states composing Skeleton, StatusIndicator, Pressable with consumer-configurable copy (D-02)
3. ScaffoldSearchBar renders pill-shaped Surface with search icon, TextField, clear/loading/filtered/resultCount trailing slots, FocusOutline, LiveRegion (D-02)
4. Kitchen-sink demo renders all 28 atoms + ScaffoldMotion in scrollable gallery with reducedMotion toggle
5. dart analyze --fatal-infos clean across all new files; all tests green; barrel exports updated
6. Phase 6 complete — 28 atoms + ScaffoldMotion shipped, exported, tested, demoed
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Classes
- CardVariant (enum: elevated, outlined, filled) — lib/components/scaffold_card.dart
- ScaffoldCard (StatelessWidget) — lib/components/scaffold_card.dart
- StateViewState (enum: loading, empty, error, unavailable, success) — lib/components/scaffold_state_view.dart
- ScaffoldStateView (StatelessWidget) — lib/components/scaffold_state_view.dart
- ScaffoldSearchBar (StatefulWidget) — lib/components/scaffold_search_bar.dart
- KitchenSinkDemo (StatefulWidget) — example/lib/demos/kitchen_sink_demo.dart
</parameter>
