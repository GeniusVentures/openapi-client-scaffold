---
phase: 06-core-ui-foundation
plan: "05"
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - lib/components/scaffold_selectable_surface.dart
  - lib/components/scaffold_draggable.dart
  - lib/components/scaffold_drop_target.dart
  - lib/components/scaffold_file_input_surface.dart
  - lib/frontend_scaffold.dart
  - test/components/scaffold_selectable_surface_test.dart
  - test/components/scaffold_draggable_test.dart
  - test/components/scaffold_drop_target_test.dart
  - test/components/scaffold_file_input_surface_test.dart
autonomous: true
requirements:
  - WIDG-22
  - WIDG-23
  - WIDG-24
  - WIDG-25
user_setup: []

must_haves:
  truths:
    - "ScaffoldSelectableSurface composes Surface+Pressable: unselected=deepBlueCardColor, selected=lightGreenPrimary at 12% opacity overlay + 1px lightGreenPrimary border, transitions use ScaffoldMotion.durations.medium/standard"
    - "ScaffoldDraggable wraps child in LongPressDraggable: feedback=1.05x scale with dragFeedbackBackground + card shadow, original fades to 40%, composes Pressable+optional DragHandle"
    - "ScaffoldDropTarget composes Surface+StatusIndicator: idle=dashed borderSubtle border, over(accepted)=dropZoneHighlight tint+lightGreenPrimary border, over(rejected)=dropZoneRejected tint+statusError border, dropped=accepted 500ms→idle, Semantics label"
    - "ScaffoldFileInputSurface composes Surface+DropTarget+StatusIndicator: idle=dashed+upload icon, valid=statusSuccess, invalid=statusError, disabled=DisabledOverlay"
  artifacts:
    - lib/components/scaffold_selectable_surface.dart
    - lib/components/scaffold_draggable.dart
    - lib/components/scaffold_drop_target.dart
    - lib/components/scaffold_file_input_surface.dart
  key_links:
    - "SelectableSurface → Surface(Plan01)+Pressable(Plan03)+FocusOutline(Plan01)+DisabledOverlay(Plan03)"
    - "Draggable → Pressable(Plan03)+DragHandle(Plan04)+Motion(Plan01)"
    - "DropTarget → Surface(Plan01)+StatusIndicator(Plan03)+Motion(Plan01)"
    - "FileInputSurface → Surface(Plan01)+DropTarget(Plan05)+StatusIndicator(Plan03)+DisabledOverlay(Plan03)"
---

<objective>
Ship the 4 Wave 2 multi-dependency atoms: SelectableSurface, Draggable, DropTarget, FileInputSurface. Each composes 2+ atoms from Waves 0-1 into higher-level interaction primitives.

Purpose: Deliver the multi-touch interaction surface layer — selection, drag-and-drop, and file input.

Output: 4 new widget atoms, widget tests, barrel exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md
@src/scaffold/lib/theme/scaffold_theme.dart
@src/scaffold/lib/theme/scaffold_palette.dart
@src/scaffold/lib/theme/scaffold_dimens.dart
@src/scaffold/lib/components/scaffold_motion.dart
@src/scaffold/lib/components/scaffold_surface.dart
@src/scaffold/lib/components/scaffold_pressable.dart
@src/scaffold/lib/components/scaffold_disabled_overlay.dart
@src/scaffold/lib/components/scaffold_status_indicator.dart
@src/scaffold/lib/components/scaffold_drag_handle.dart
@src/scaffold/lib/frontend_scaffold.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScaffoldSelectableSurface + ScaffoldDraggable — selected surface + drag wrapper</name>
  <files>lib/components/scaffold_selectable_surface.dart, lib/components/scaffold_draggable.dart, test/components/scaffold_selectable_surface_test.dart, test/components/scaffold_draggable_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/components/scaffold_surface.dart, src/scaffold/lib/components/scaffold_pressable.dart, src/scaffold/lib/components/scaffold_focus_outline.dart, src/scaffold/lib/components/scaffold_disabled_overlay.dart, src/scaffold/lib/components/scaffold_drag_handle.dart, src/scaffold/lib/components/scaffold_motion.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: SelectableSurface unselected renders deepBlueCardColor Surface
    - Test 2: Selected shows lightGreenPrimary at 12% opacity overlay + 1px lightGreenPrimary border
    - Test 3: Focused shows FocusOutline ring (inherited from Pressable's FocusNode)
    - Test 4: onTap fires; disabled blocks interaction via DisabledOverlay
    - Test 5: State transitions use ScaffoldMotion.durations.medium/standard
    - Test 6: Draggable idle renders child normally
    - Test 7: Long-press activates drag — feedback scales 1.05x with dragFeedbackBackground + card shadow, original fades 0.4 opacity
    - Test 8: Draggable composes optional DragHandle (when dragHandle is true or Widget)
    - Test 9: onDragStarted/onDragEnd/onDraggableCanceled fire correctly
  </behavior>
  <action>
    **scaffold_selectable_surface.dart:** StatelessWidget. Constructor: Widget? child, bool selected=false, VoidCallback? onTap, VoidCallback? onLongPress, bool disabled=false. Compose ScaffoldSurface with ScaffoldPressable. Selected: AnimatedContainer with lightGreenPrimary 12% opacity overlay (Stack: deepBlueCardColor base + Positioned.fill Container(color: palette.lightGreenPrimary.withOpacity(0.12))) + Border.all(color: palette.lightGreenPrimary, width: 1). Duration=ScaffoldMotion.durations.medium, curve=standard. Focused: inherited from Pressable's FocusNode→FocusOutline. Disabled: DisabledOverlay. Per UI-SPEC.

    **scaffold_draggable.dart:** StatelessWidget wrapping LongPressDraggable. Constructor: Widget? child, dynamic data, Widget? feedback, Widget? childWhenDragging, dynamic dragHandle, VoidCallback? onDragStarted, VoidCallback? onDragEnd, VoidCallback? onDraggableCanceled. Build: if dragHandle is true, prepend ScaffoldDragHandle above child in Column. LongPressDraggable wraps. feedback default: Transform.scale(scale: 1.05, child: Material(elevation:4, shadowColor: Colors.black38, color: palette.dragFeedbackBackground, child: child)). childWhenDragging default: Opacity(opacity: 0.4, child: child). Elevation anim uses ScaffoldMotion.durations.short.

    Add barrel exports. No hardcoded hex values. Per D-04: no convenience constructors composing extra atoms.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_selectable_surface_test.dart test/components/scaffold_draggable_test.dart && dart analyze --fatal-infos lib/components/scaffold_selectable_surface.dart lib/components/scaffold_draggable.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_selectable_surface_test.dart has 5 test cases
    - test/components/scaffold_draggable_test.dart has 4 test cases
    - All 9 tests pass; dart analyze --fatal-infos clean on both files
    - SelectableSurface imports Surface, Pressable (grep confirms)
    - Draggable uses palette.dragFeedbackBackground (grep confirms)
    - Barrel exports both files; no hardcoded hex values
  </acceptance_criteria>
  <done>ScaffoldSelectableSurface and ScaffoldDraggable created, 9 tests green, barrel-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScaffoldDropTarget + ScaffoldFileInputSurface — drop zone + file input composable</name>
  <files>lib/components/scaffold_drop_target.dart, lib/components/scaffold_file_input_surface.dart, test/components/scaffold_drop_target_test.dart, test/components/scaffold_file_input_surface_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/components/scaffold_surface.dart, src/scaffold/lib/components/scaffold_status_indicator.dart, src/scaffold/lib/components/scaffold_disabled_overlay.dart, src/scaffold/lib/components/scaffold_motion.dart, src/scaffold/lib/theme/scaffold_palette.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: DropTarget idle renders dashed borderSubtle border on Surface
    - Test 2: DropTarget over(accepted) shows dropZoneHighlight tint + lightGreenPrimary solid border
    - Test 3: DropTarget over(rejected) shows dropZoneRejected tint + statusError solid border
    - Test 4: DropTarget dropped shows accepted tint 500ms then returns idle
    - Test 5: DropTarget Semantics label "Drop zone" + state label
    - Test 6: FileInputSurface idle renders dashed border + upload icon + label text
    - Test 7: FileInputSurface valid file shows statusSuccess border + check circle + filename
    - Test 8: FileInputSurface invalid file shows statusError border + error icon + validation message
    - Test 9: FileInputSurface disabled applies DisabledOverlay
  </behavior>
  <action>
    **scaffold_drop_target.dart:** StatefulWidget. Constructor: Widget? child, bool Function(dynamic data)? acceptCondition, VoidCallback? onAccept, VoidCallback? onReject, Type? acceptType. Track state: _DropState { idle, overAccepted, overRejected, dropped } via setState. Wrap child in DragTarget. onWillAcceptWithDetails checks acceptType/acceptCondition. Render ScaffoldSurface with state-dependent styling using AnimatedContainer(duration: ScaffoldMotion.durations.medium, curve: standard). idle: dashed border (use Container with Border.all + custom dash pattern or simply Border.all with borderSubtle), overAccepted: Solid border palette.lightGreenPrimary + palette.dropZoneHighlight overlay, overRejected: Solid border palette.statusError + palette.dropZoneRejected overlay, dropped: overAccepted 500ms timer then reset. Semantics: "Drop zone. {state}" (D-02).

    **scaffold_file_input_surface.dart:** StatefulWidget. Constructor: required ValueChanged<File> onFileSelected, String? Function(File)? validate, List<String>? acceptTypes, int? maxSize, bool disabled=false, String idleLabel="Choose a file or drag here". State: File? _selectedFile, String? _validationError, bool _isDragOver. Build composes ScaffoldSurface with state-dependent decoration. idle: dashed border, centered Column(Icon(Icons.upload_file, size:48, color: palette.textSecondary), Text(idleLabel)). valid: border=palette.statusSuccess 2px solid, Icon(Icons.check_circle, color: palette.statusSuccess), filename Text. invalid: border=palette.statusError 2px solid, Icon(Icons.error, color: palette.statusError), validation message Text. Drag-over: DropTarget over state. Disabled: DisabledOverlay. File selection: tap handler opens platform picker; if file_picker not in deps, implement as drop-only with a clear note in the widget doc comment. Add barrel exports. No hardcoded hex values.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_drop_target_test.dart test/components/scaffold_file_input_surface_test.dart && dart analyze --fatal-infos lib/components/scaffold_drop_target.dart lib/components/scaffold_file_input_surface.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_drop_target_test.dart has 5 test cases
    - test/components/scaffold_file_input_surface_test.dart has 4 test cases
    - All 9 tests pass; dart analyze --fatal-infos clean on both files
    - DropTarget uses palette.dropZoneHighlight/dropZoneRejected (grep confirms)
    - FileInputSurface imports Surface, DisabledOverlay (grep confirms)
    - FileInputSurface valid uses palette.statusSuccess; invalid uses palette.statusError (grep confirms)
    - Barrel exports both files; no hardcoded hex values
  </acceptance_criteria>
  <done>ScaffoldDropTarget and ScaffoldFileInputSurface created, 9 tests green, barrel-exported. Wave 2 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller file data → Widget state | FileInputSurface receives file data via drop/pick; validate callback screens. Drag data in-process only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Denial of Service | DropTarget rapid state transitions | low | mitigate | 500ms dropped→idle timer prevents rapid re-trigger; mounted guard |
| T-06-10 | Denial of Service | FileInputSurface large file | low | mitigate | maxSize bounds; validate before accept |
</threat_model>

<verification>
- `cd src/scaffold && flutter test` on all 4 test files — all green (18 tests)
- `cd src/scaffold && dart analyze --fatal-infos` on all 4 widget files — zero issues
- Grep barrel for all 4 export lines
</verification>

<success_criteria>
1. ScaffoldSelectableSurface composes Surface+Pressable for selected/focused/pressed/disabled states with animated transitions
2. ScaffoldDraggable wraps child in LongPressDraggable with 1.05x feedback, optional DragHandle, lifecycle callbacks
3. ScaffoldDropTarget renders idle/accepted/rejected/dropped visual states with palette tints and Semantics label
4. ScaffoldFileInputSurface composes Surface+DropTarget+StatusIndicator for file selection with validation
5. dart analyze --fatal-infos clean; all 18 tests green; barrel exports updated
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Classes
- ScaffoldSelectableSurface (StatelessWidget) — lib/components/scaffold_selectable_surface.dart
- ScaffoldDraggable (StatelessWidget) — lib/components/scaffold_draggable.dart
- ScaffoldDropTarget (StatefulWidget) — lib/components/scaffold_drop_target.dart
- ScaffoldFileInputSurface (StatefulWidget) — lib/components/scaffold_file_input_surface.dart
</parameter>
