---
phase: 06-core-ui-foundation
plan: "02"
type: execute
wave: 2
# Exec wave 2/6 → dependency layer Wave 0. The 6 exec waves realize 4 canonical layers: Wave 0 = plans 01+02, Wave 1 = 03+04, Wave 2 = 05, Wave 3 = 06.
depends_on:
  - 06-01
files_modified:
  - lib/components/scaffold_live_region.dart
  - lib/components/scaffold_overflow_fade.dart
  - lib/components/scaffold_scroll_edge_indicator.dart
  - lib/components/scaffold_responsive_visibility.dart
  - templates/components/formatted_value.dart.jinja2
  - templates/components/formatted_value_vars.json
  - lib/components/scaffold_formatted_value_number.dart
  - lib/components/scaffold_formatted_value_money.dart
  - lib/components/scaffold_formatted_value_percentage.dart
  - lib/components/scaffold_formatted_value_date.dart
  - lib/components/scaffold_formatted_value_time.dart
  - lib/components/scaffold_formatted_value_duration.dart
  - lib/components/scaffold_color_swatch.dart
  - lib/frontend_scaffold.dart
  - test/components/scaffold_live_region_test.dart
  - test/components/scaffold_overflow_fade_test.dart
  - test/components/scaffold_scroll_edge_indicator_test.dart
  - test/components/scaffold_responsive_visibility_test.dart
  - test/components/scaffold_formatted_value_test.dart
  - test/components/scaffold_color_swatch_test.dart
autonomous: true
requirements:
  - WIDG-05
  - WIDG-06
  - WIDG-07
  - WIDG-08
  - WIDG-09
  - WIDG-10
user_setup: []

must_haves:
  truths:
    - "ScaffoldLiveRegion wraps Semantics(liveRegion: true) with configurable label/value — screen readers announce when label/value change (per D-02)"
    - "ScaffoldOverflowFade renders a ShaderMask with linear gradient fading from transparent (center) to backgroundColor (edges), configurable fadeDirection and fadeExtent (default 24px)"
    - "ScaffoldScrollEdgeIndicator connects to a ScrollController and renders 1px hairlines (palette.borderSubtle) at scrollable edges when content overflows"
    - "ScaffoldResponsiveVisibility hides/shows/replaces child based on ScaffoldBreakpoints thresholds with configurable operator and breakpoint"
    - "ScaffoldFormattedValue is template-generated from formatted_value.dart.jinja2 into 6 per-variant files (ScaffoldFormattedValueNumber/Money/Percentage/Date/Time/Duration), each rendering its variant's formatted text via textTheme.bodyLarge — null renders configured nullPlaceholder (default '--'); no runtime enum/switch couples variants (per STATE.md template boundary)"
    - "ScaffoldColorSwatch renders configurable-size color dots in a grid/row, each wrapped in a 48px touch target — selected shows focusRingWidth ring in lightGreenPrimary, disabled shows disabledOverlayColor at disabledOverlayOpacity"
    - "Every atom uses only context.palette/context.dimens/Theme.of(context).textTheme for visual properties — no hardcoded hex or numeric literals (per D-04)"
    - "long-text: generated ScaffoldFormattedValue* variants use maxLines:1 with TextOverflow.ellipsis — verification: backstop"
    - "zero-one-many: ScaffoldColorSwatch handles 0 colors (SizedBox.shrink), 1 color (single swatch), many (scrollable row/grid) — verification: backstop"
  artifacts:
    - lib/components/scaffold_live_region.dart
    - lib/components/scaffold_overflow_fade.dart
    - lib/components/scaffold_scroll_edge_indicator.dart
    - lib/components/scaffold_responsive_visibility.dart
    - templates/components/formatted_value.dart.jinja2
    - templates/components/formatted_value_vars.json
    - lib/components/scaffold_formatted_value_number.dart
    - lib/components/scaffold_formatted_value_money.dart
    - lib/components/scaffold_formatted_value_percentage.dart
    - lib/components/scaffold_formatted_value_date.dart
    - lib/components/scaffold_formatted_value_time.dart
    - lib/components/scaffold_formatted_value_duration.dart
    - lib/components/scaffold_color_swatch.dart
    - test/components/scaffold_live_region_test.dart
    - test/components/scaffold_overflow_fade_test.dart
    - test/components/scaffold_scroll_edge_indicator_test.dart
    - test/components/scaffold_responsive_visibility_test.dart
    - test/components/scaffold_formatted_value_test.dart
    - test/components/scaffold_color_swatch_test.dart
  key_links:
    - "ScaffoldColorSwatch → ScaffoldTouchTarget (Plan 01) for 48px hit area on each swatch dot"
    - "formatted_value.dart.jinja2 → engine.py (Jinja2 StrictUndefined) → 6 per-variant lib/components/scaffold_formatted_value_*.dart files — drift gate regenerates + diffs (template is source-of-truth, no hand-edited enum switch)"
    - "Generated ScaffoldFormattedValue* null-value loading placeholder → simple Container shimmer using palette.skeletonBaseColor/skeletonShimmerColor (zero-dep, per D-03); FormattedValue variants are Wave 0 zero-dependency and do not depend on Wave 1's ScaffoldSkeleton"
    - "ScaffoldResponsiveVisibility → ScaffoldBreakpoints (existing, Phase 5) for width threshold comparisons"
---

<objective>
Ship the 6 remaining Wave 0 zero-dependency atoms: LiveRegion, OverflowFade, ScrollEdgeIndicator, ResponsiveVisibility, FormattedValue, ColorSwatch. These complete the foundation layer before Wave 1 atoms begin composing primitives. ScaffoldFormattedValue ships as a template-generated atom (formatted_value.dart.jinja2 → 6 per-variant generated files, drift-gated) per the locked STATE.md/ROADMAP decision that its number/money/percentage/date/time/duration variants change widget-tree structure and imports; ColorSwatch composes the ScaffoldTouchTarget shipped in Plan 01 and stays plain Dart.

Purpose: Complete the zero-dependency foundation layer so Wave 1 atoms can depend on a stable, complete set of primitives.

Output: 5 plain Dart widget atoms, 1 template-generated atom (6 generated variant files), widget tests, barrel exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-01-PLAN.md
@src/scaffold/engine.py
@src/scaffold/design_tokens.json
@src/scaffold/templates/components/card.dart.jinja2
@src/scaffold/templates/components/card_vars.json
@src/scaffold/lib/theme/scaffold_theme.dart
@src/scaffold/lib/theme/scaffold_palette.dart
@src/scaffold/lib/theme/scaffold_dimens.dart
@src/scaffold/lib/utils/breakpoints.dart
@src/scaffold/lib/components/scaffold_touch_target.dart
@src/scaffold/lib/components/scaffold_motion.dart
@src/scaffold/lib/frontend_scaffold.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScaffoldLiveRegion + ScaffoldOverflowFade + ScaffoldScrollEdgeIndicator — a11y announcement, edge fade, scroll boundary</name>
  <files>lib/components/scaffold_live_region.dart, lib/components/scaffold_overflow_fade.dart, lib/components/scaffold_scroll_edge_indicator.dart, test/components/scaffold_live_region_test.dart, test/components/scaffold_overflow_fade_test.dart, test/components/scaffold_scroll_edge_indicator_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/theme/scaffold_theme.dart, src/scaffold/lib/components/scaffold_surface.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: ScaffoldLiveRegion(label: "Volume") registers Semantics(liveRegion: true) with label "Volume" — verify via tester.getSemantics
    - Test 2: ScaffoldLiveRegion updates Semantics label when label parameter changes
    - Test 3: ScaffoldOverflowFade renders a ShaderMask widget when fadeDirection is horizontal
    - Test 4: ScaffoldOverflowFade with fadeExtent=32 creates a gradient with 32px edge stops
    - Test 5: ScaffoldScrollEdgeIndicator shows atStart edge line when ScrollController is at 0.0 and maxScrollExtent > 0
    - Test 6: ScaffoldScrollEdgeIndicator hides both edge lines when content fits (maxScrollExtent == 0)
  </behavior>
  <action>
    Create three widget files:

    **scaffold_live_region.dart:** StatelessWidget. Constructor: String? label, String? value, Widget? child. In build(): return Semantics(liveRegion: true, label: label, value: value, child: child ?? const SizedBox.shrink()). Per UI-SPEC: "A zero-size widget that wraps Semantics(liveRegion: true). Used by atoms that report changing values." Add library frontend_scaffold; at top.

    **scaffold_overflow_fade.dart:** StatelessWidget. Constructor: Widget? child, FadeDirection fadeDirection=FadeDirection.horizontal, double fadeExtent=24.0, Color? backgroundColor. Define enum FadeDirection { horizontal, vertical, both }. In build(): resolve palette, compute gradient stops. Create a LinearGradient with transparent stops at the edges and opaque backgroundColor stops in the center. Wrap child in ShaderMask(blendMode: BlendMode.dstOut, shaderCallback: ...) using the gradient. Per UI-SPEC: fade from transparent (edges) to backgroundColor (center).

    **scaffold_scroll_edge_indicator.dart:** StatefulWidget. Constructor: required ScrollController scrollController, Color? edgeColor, double edgeWidth=1.0. In initState: add listener on scrollController. In build(): check hasClients and position.extentBefore/After to determine which edges show. Render Positioned Containers at top/bottom with AnimatedOpacity(duration: ScaffoldMotion.durations.short) for fade transitions. Edge color defaults to palette.borderSubtle. Per UI-SPEC: "1px hairlines at scrollable edges."

    Add barrel exports. Per D-04: each atom does one thing. No hardcoded hex values.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_live_region_test.dart test/components/scaffold_overflow_fade_test.dart test/components/scaffold_scroll_edge_indicator_test.dart && dart analyze --fatal-infos lib/components/scaffold_live_region.dart lib/components/scaffold_overflow_fade.dart lib/components/scaffold_scroll_edge_indicator.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test cases pass
    - dart analyze --fatal-infos on all 3 widget files returns zero issues
    - Barrel exports all 3 files (grep 'export.*scaffold_live_region' lib/frontend_scaffold.dart returns one match, same for overflow_fade and scroll_edge_indicator)
    - ScaffoldLiveRegion registers Semantics(liveRegion: true)
    - ScaffoldScrollEdgeIndicator uses ScaffoldMotion.durations.short (grep confirms import and usage)
    - No hardcoded 0xFF color values in widget .dart files
  </acceptance_criteria>
  <done>ScaffoldLiveRegion, ScaffoldOverflowFade, ScaffoldScrollEdgeIndicator live in lib/components/, exported from barrel, 6 tests green, analyze clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScaffoldResponsiveVisibility — breakpoint-aware show/hide/replace</name>
  <files>lib/components/scaffold_responsive_visibility.dart, test/components/scaffold_responsive_visibility_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/utils/breakpoints.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldResponsiveVisibility with showAt=760, operator=>= renders child at screen width 800
    - Test 2: ScaffoldResponsiveVisibility with showAt=760, operator=>= renders SizedBox.shrink() at screen width 600
    - Test 3: ScaffoldResponsiveVisibility with hideAt=760, operator=<= hides child at 600, renders at 800
    - Test 4: ScaffoldResponsiveVisibility with replacement widget shows replacement when hide condition met
    - Test 5: Widget responds to MediaQuery size change via setScreenSize in test
  </behavior>
  <action>
    Create lib/components/scaffold_responsive_visibility.dart. StatelessWidget. Constructor: Widget? child, double? showAt, double? hideAt, ComparisonOperator operator=ComparisonOperator.greaterThanOrEqual, double? breakpoint, Widget? replacement. Define enum ComparisonOperator { greaterThan, lessThan, greaterThanOrEqual, lessThanOrEqual }.

    In build(): resolve screenWidth from MediaQuery.of(context).size.width. Use breakpoint value if provided, else showAt or hideAt. Apply the comparison. Show child when condition is met, replacement or SizedBox.shrink() when not. Per UI-SPEC: uses ScaffoldBreakpoints (small=760, tablet=1200, medium=1644, large=1920) as reference thresholds. Add library declaration and barrel export.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_responsive_visibility_test.dart && dart analyze --fatal-infos lib/components/scaffold_responsive_visibility.dart</automated>
  </verify>
  <acceptance_criteria>
    - 5 test cases pass
    - dart analyze --fatal-infos clean on scaffold_responsive_visibility.dart
    - Barrel exports scaffold_responsive_visibility.dart
    - Widget imports ScaffoldBreakpoints (grep confirms)
  </acceptance_criteria>
  <done>ScaffoldResponsiveVisibility exported, 5 tests green, analyze clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScaffoldFormattedValue (template-generated) + ScaffoldColorSwatch — format display + color picker</name>
  <files>templates/components/formatted_value.dart.jinja2, templates/components/formatted_value_vars.json, lib/components/scaffold_formatted_value_number.dart, lib/components/scaffold_formatted_value_money.dart, lib/components/scaffold_formatted_value_percentage.dart, lib/components/scaffold_formatted_value_date.dart, lib/components/scaffold_formatted_value_time.dart, lib/components/scaffold_formatted_value_duration.dart, lib/components/scaffold_color_swatch.dart, test/components/scaffold_formatted_value_test.dart, test/components/scaffold_color_swatch_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/engine.py, src/scaffold/templates/components/card.dart.jinja2, src/scaffold/templates/components/card_vars.json, src/scaffold/lib/theme/scaffold_theme.dart, src/scaffold/lib/theme/scaffold_palette.dart, src/scaffold/lib/theme/scaffold_dimens.dart, src/scaffold/lib/components/scaffold_live_region.dart, src/scaffold/lib/components/scaffold_touch_target.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldFormattedValueNumber(value: 1234) renders "1,234" in textTheme.bodyLarge (generated variant=number)
    - Test 2: ScaffoldFormattedValueMoney(value: 42.5, currencySymbol: "$") renders "$42.50" (generated variant=money)
    - Test 3: ScaffoldFormattedValuePercentage(value: 0.425) renders "42.5%" (generated variant=percentage)
    - Test 4: ScaffoldFormattedValueDate(value: DateTime(2026, 8, 11)) renders a locale-aware date (generated variant=date)
    - Test 5: Generated ScaffoldFormattedValue* with null value renders nullPlaceholder (default "--")
    - Test 6: ScaffoldFormattedValueNumber with a long number renders with TextOverflow.ellipsis and maxLines:1
    - Test 7: ScaffoldColorSwatch(colors: [Colors.red, Colors.green], selectedIndex: 1) renders 2 circles with green having focus ring
    - Test 8: ScaffoldColorSwatch with 0 colors renders SizedBox.shrink()
    - Test 9: ScaffoldColorSwatch with 1 color renders single swatch in 48px touch target
    - Test 10: ScaffoldColorSwatch with 10 colors renders in scrollable row
    - Test 11: ScaffoldColorSwatch onSelected fires when non-selected color is tapped
    - Test 12: ScaffoldColorSwatch disabled swatches pass dim overlay + IgnorePointer
  </behavior>
  <action>
    **ScaffoldFormattedValue — template-generated (per WIDG-09, NOT enum-switch).** Create templates/components/formatted_value.dart.jinja2 with per-variant branches: `{% if variant == "number" %}…{% elif variant == "money" %}…{% elif variant == "percentage" %}…{% elif variant == "date" %}…{% elif variant == "time" %}…{% elif variant == "duration" %}…{% endif %}`. Each branch emits ONLY that variant's widget tree — a StatelessWidget class `ScaffoldFormattedValue{{ Variant }}` (Number / Money / Percentage / Date / Time / Duration) whose build() returns a ScaffoldLiveRegion wrapping Text(formatted, style: style ?? textTheme.bodyLarge, maxLines: 1, overflow: TextOverflow.ellipsis). Formatting logic is variant-specific and generated inline per branch: number → group separators; money → currencySymbol + group separators + decimalPlaces; percentage → percent sign + decimalPlaces; date → locale pattern; time → 12h/24h; duration → H:MM:SS. Null value → Text(nullPlaceholder, default '--'). No intl dependency. The template header carries the source-schema + generator-version "do not edit by hand" comment (same convention as card.dart.jinja2). Create templates/components/formatted_value_vars.json with a `_comment`, `widget_class_name`, and `variant` field (a single representative variant for standalone render; the build renders once per variant by overriding `variant` + `widget_class_name`).

    Generate the 6 variant files via engine.py (Jinja2 StrictUndefined) — do NOT hand-write them:
      python3 engine.py --template components/formatted_value.dart.jinja2 --output lib/components/scaffold_formatted_value_number.dart --template-dir templates/ --tokens design_tokens.json --vars templates/components/formatted_value_vars.json
    Repeat for money / percentage / date / time / duration, overriding `variant` and `widget_class_name` per render so each output file declares exactly one variant class (e.g. ScaffoldFormattedValueMoney in scaffold_formatted_value_money.dart). Each generated file imports scaffold_live_region.dart; no generated file contains a cross-variant runtime enum/switch — each file holds only its own variant's tree.

    **scaffold_color_swatch.dart (plain Dart, unchanged).** StatelessWidget. Constructor: required List<Color> colors, int? selectedIndex, ValueChanged<int>? onSelected, double dotSize=24.0, bool disabled=false. In build(): if colors.isEmpty → SizedBox.shrink(). Render SingleChildScrollView(scrollDirection: Axis.horizontal) containing Row of circles. Each circle: GestureDetector wrapping a Container(diameter=dotSize, decoration: BoxDecoration(shape: BoxShape.circle, color: colors[i], border: i==selectedIndex ? Border.all(color: palette.lightGreenPrimary, width: dimens.focusRingWidth) : null)). Wrap each circle in SizedBox(width: dimens.minTouchTarget, height: dimens.minTouchTarget) with Center(child: circle) for 48px hit area. Disabled: apply palette.disabledOverlayColor at dimens.disabledOverlayOpacity overlay + IgnorePointer. Per UI-SPEC: zero-one-many backstop — 0 colors → shrink, 1 → single swatch, many → scrollable row.

    Add barrel export entries for all 6 generated variant files + scaffold_color_swatch.dart. Add a drift gate: regenerate each variant via engine.py into a temp file and diff against the committed lib/components/ file — zero difference required (template is source-of-truth); wire regenerate-and-diff into CI so any hand-edit to a generated file, or template/output divergence, fails the build. Per D-04: no convenience constructors. No hardcoded hex values.
  </action>
  <verify>
    <automated>cd src/scaffold && python3 engine.py --template components/formatted_value.dart.jinja2 --output /tmp/06_fv_number.dart --template-dir templates/ --tokens design_tokens.json --vars templates/components/formatted_value_vars.json && diff -q /tmp/06_fv_number.dart lib/components/scaffold_formatted_value_number.dart && flutter test test/components/scaffold_formatted_value_test.dart test/components/scaffold_color_swatch_test.dart && dart analyze --fatal-infos lib/components/scaffold_formatted_value_number.dart lib/components/scaffold_formatted_value_money.dart lib/components/scaffold_formatted_value_percentage.dart lib/components/scaffold_formatted_value_date.dart lib/components/scaffold_formatted_value_time.dart lib/components/scaffold_formatted_value_duration.dart lib/components/scaffold_color_swatch.dart</automated>
  </verify>
  <acceptance_criteria>
    - templates/components/formatted_value.dart.jinja2 + formatted_value_vars.json exist; template renders via engine.py (Jinja2 StrictUndefined) with zero UndefinedError
    - 6 generated variant files present in lib/components/ (scaffold_formatted_value_{number,money,percentage,date,time,duration}.dart), each declaring a distinct ScaffoldFormattedValue* class with the source-schema + generator-version header
    - test/components/scaffold_formatted_value_test.dart has 6 test cases (number, money, percentage, date, null placeholder, long-text overflow) exercising the generated classes
    - test/components/scaffold_color_swatch_test.dart has 6 test cases (selection ring, zero shrink, one swatch, many scroll, onSelected, disabled)
    - All 12 tests pass
    - dart analyze --fatal-infos on all 6 generated files + scaffold_color_swatch.dart returns zero issues
    - Barrel exports all 6 generated files + scaffold_color_swatch.dart (grep confirms export lines)
    - Generated ScaffoldFormattedValue* files import ScaffoldLiveRegion (grep confirms) and contain no runtime enum/switch spanning variants (each file contains only its own variant's tree)
    - Drift gate passes: regenerating each variant via engine.py and diffing against the committed file produces zero difference (template is source-of-truth)
    - ScaffoldColorSwatch wraps each dot in dimens.minTouchTarget ConstrainedBox (grep confirms)
    - No hardcoded 0xFF color values in generated variant files or scaffold_color_swatch.dart
  </acceptance_criteria>
  <done>ScaffoldFormattedValue generated from formatted_value.dart.jinja2 into 6 per-variant files (drift-gated), ScaffoldColorSwatch created as plain Dart, 12 tests green, barrel-exported, analyze clean. Wave 0 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller parameters → Widget internals | FormattedValue dynamic value requires type-check before formatting. ColorSwatch color list may be empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Denial of Service | FormattedValue dynamic cast | low | mitigate | Type-check value before formatting; render nullPlaceholder on invalid type — verified by null-value test |
| T-06-04 | Information Disclosure | LiveRegion Semantics label | low | accept | Semantics labels are by-design user-visible for screen readers; no secrets in atom labels |
</threat_model>

<verification>
- `cd src/scaffold && flutter test test/components/scaffold_live_region_test.dart test/components/scaffold_overflow_fade_test.dart test/components/scaffold_scroll_edge_indicator_test.dart test/components/scaffold_responsive_visibility_test.dart test/components/scaffold_formatted_value_test.dart test/components/scaffold_color_swatch_test.dart` — all green
- `cd src/scaffold && dart analyze --fatal-infos lib/components/scaffold_live_region.dart lib/components/scaffold_overflow_fade.dart lib/components/scaffold_scroll_edge_indicator.dart lib/components/scaffold_responsive_visibility.dart lib/components/scaffold_formatted_value_number.dart lib/components/scaffold_formatted_value_money.dart lib/components/scaffold_formatted_value_percentage.dart lib/components/scaffold_formatted_value_date.dart lib/components/scaffold_formatted_value_time.dart lib/components/scaffold_formatted_value_duration.dart lib/components/scaffold_color_swatch.dart` — zero issues
- Grep barrel for the 5 plain Dart atom export lines + 6 generated FormattedValue export lines
- Drift gate: regenerate each FormattedValue variant via engine.py and diff against the committed file — zero difference
</verification>

<success_criteria>
1. ScaffoldLiveRegion wraps Semantics(liveRegion: true); screen readers announce label/value changes (per D-02)
2. ScaffoldOverflowFade renders edge-fade ShaderMask with configurable direction and extent
3. ScaffoldScrollEdgeIndicator shows/hides 1px edge hairlines based on ScrollController position
4. ScaffoldResponsiveVisibility shows/hides/replaces at ScaffoldBreakpoints thresholds
5. ScaffoldFormattedValue is generated from formatted_value.dart.jinja2 into 6 per-variant files (ScaffoldFormattedValueNumber/Money/Percentage/Date/Time/Duration) rendering number/money/percentage/date/time/duration with null placeholder and long-text ellipsis; drift gate regenerates + diffs clean; zero-dep loading uses palette.skeletonBaseColor
6. ScaffoldColorSwatch renders selectable color dots in 48px touch targets with selection ring, disabled state; handles 0/1/many colors
7. dart analyze --fatal-infos clean; all tests green; barrel exports updated; drift gate passes
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Classes
- ScaffoldLiveRegion (StatelessWidget) — lib/components/scaffold_live_region.dart
- FadeDirection (enum: horizontal, vertical, both) — lib/components/scaffold_overflow_fade.dart
- ScaffoldOverflowFade (StatelessWidget) — lib/components/scaffold_overflow_fade.dart
- ScaffoldScrollEdgeIndicator (StatefulWidget) — lib/components/scaffold_scroll_edge_indicator.dart
- ComparisonOperator (enum) — lib/components/scaffold_responsive_visibility.dart
- ScaffoldResponsiveVisibility (StatelessWidget) — lib/components/scaffold_responsive_visibility.dart
- ScaffoldFormattedValueNumber / Money / Percentage / Date / Time / Duration (generated StatelessWidgets) — lib/components/scaffold_formatted_value_{number,money,percentage,date,time,duration}.dart (generated from templates/components/formatted_value.dart.jinja2, drift-gated)
- ScaffoldColorSwatch (StatelessWidget) — lib/components/scaffold_color_swatch.dart

### Templates (source-of-truth)
- formatted_value.dart.jinja2 + formatted_value_vars.json — per-variant branches, rendered once per variant via engine.py (Jinja2 StrictUndefined); drift gate regenerates + diffs

### Test classes
- ScaffoldLiveRegionTest, ScaffoldOverflowFadeTest, ScaffoldScrollEdgeIndicatorTest, ScaffoldResponsiveVisibilityTest, ScaffoldFormattedValueTest, ScaffoldColorSwatchTest
</parameter>
