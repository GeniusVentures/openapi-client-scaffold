---
phase: 06-core-ui-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - lib/components/scaffold_skeleton.dart
  - lib/components/scaffold_badge.dart
  - lib/components/scaffold_status_indicator.dart
  - lib/components/scaffold_pressable.dart
  - lib/components/scaffold_disabled_overlay.dart
  - lib/frontend_scaffold.dart
  - test/components/scaffold_skeleton_test.dart
  - test/components/scaffold_badge_test.dart
  - test/components/scaffold_status_indicator_test.dart
  - test/components/scaffold_pressable_test.dart
  - test/components/scaffold_disabled_overlay_test.dart
autonomous: true
requirements:
  - WIDG-11
  - WIDG-12
  - WIDG-15
  - WIDG-17
  - WIDG-18
user_setup: []

must_haves:
  truths:
    - "ScaffoldSkeleton renders animated placeholder with shimmer gradient using palette.skeletonBaseColor/skeletonShimmerColor and dimens.skeletonCornerRadius — when ScaffoldMotion.reducedMotion is true, uses static pulse opacity instead of horizontal sweep (per D-01)"
    - "ScaffoldBadge renders dot (8px circle in palette.lightGreenPrimary), count (pill with palette.badgeColor background, max 2 digits truncating to '99+'), icon (16px icon in 24px circle), text (custom label pill), and empty (SizedBox.shrink when count is 0) — disabled state applies 0.4 opacity (per D-04 pure composability: no positioning — consumer uses Stack+Positioned)"
    - "ScaffoldStatusIndicator renders a configurable-size circle (default 12px) with color mapped to 5 status variants (success=statusSuccess, warning=statusWarningText, error=statusError, info=blue500, neutral=textSecondary) — Semantics role: status with configurable accessible label, disabled state 0.4 opacity (per D-02)"
    - "ScaffoldPressable wraps child with M3 state layers: hovered=textPrimary 8% opacity, pressed=textPrimary 12% opacity, focused=FocusOutline ring, disabled=DisabledOverlay — composes TouchTarget for 48px hit area, responds to Enter/Space keys (per D-02)"
    - "ScaffoldDisabledOverlay renders semi-transparent dim (palette.disabledOverlayColor at dimens.disabledOverlayOpacity) + IgnorePointer + optional reason tooltip via Semantics — enabled state renders child with zero-opacity cost"
  artifacts:
    - lib/components/scaffold_skeleton.dart
    - lib/components/scaffold_badge.dart
    - lib/components/scaffold_status_indicator.dart
    - lib/components/scaffold_pressable.dart
    - lib/components/scaffold_disabled_overlay.dart
    - test/components/scaffold_skeleton_test.dart
    - test/components/scaffold_badge_test.dart
    - test/components/scaffold_status_indicator_test.dart
    - test/components/scaffold_pressable_test.dart
    - test/components/scaffold_disabled_overlay_test.dart
  key_links:
    - "ScaffoldPressable → ScaffoldTouchTarget (Plan 01) for 48px hit area"
    - "ScaffoldPressable.pressed/hovered → ScaffoldMotion.durations (Plan 01) for state layer transition timing"
    - "ScaffoldSkeleton.shimmer → ScaffoldMotion.reducedMotion (Plan 01) → static pulse when true"
    - "ScaffoldSkeleton colors → palette.skeletonBaseColor/skeletonShimmerColor (Plan 01 D-03 tokens)"
---

<objective>
Ship the first batch of Wave 1 single-dependency atoms: ScaffoldSkeleton, ScaffoldBadge, ScaffoldStatusIndicator, ScaffoldPressable, ScaffoldDisabledOverlay. These 5 widgets each depend on exactly one Wave 0 atom (TouchTarget, Motion, or FocusOutline) and form the interaction/feedback foundation that Wave 2 multi-dependency atoms compose.

Purpose: Establish the animation (Skeleton), affordance (Badge/StatusIndicator), interaction (Pressable), and state-blocking (DisabledOverlay) patterns before Wave 2's composite widgets.

Output: 5 new widget atoms, widget tests, barrel exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md
@src/scaffold/lib/theme/scaffold_theme.dart
@src/scaffold/lib/theme/scaffold_palette.dart
@src/scaffold/lib/theme/scaffold_dimens.dart
@src/scaffold/lib/components/scaffold_motion.dart
@src/scaffold/lib/components/scaffold_touch_target.dart
@src/scaffold/lib/components/scaffold_focus_outline.dart
@src/scaffold/lib/frontend_scaffold.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScaffoldSkeleton + ScaffoldDisabledOverlay — animated placeholder + interaction blocker</name>
  <files>lib/components/scaffold_skeleton.dart, lib/components/scaffold_disabled_overlay.dart, test/components/scaffold_skeleton_test.dart, test/components/scaffold_disabled_overlay_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/components/scaffold_motion.dart, src/scaffold/lib/theme/scaffold_palette.dart, src/scaffold/lib/theme/scaffold_dimens.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldSkeleton renders a rounded-rect Container with palette.skeletonBaseColor and dimens.skeletonCornerRadius
    - Test 2: ScaffoldSkeleton shimmer animation has a LinearGradient sweep with palette.skeletonShimmerColor — verify the AnimationController is running
    - Test 3: ScaffoldSkeleton with reducedMotion=true uses static pulse (opacity oscillation 0.6→1.0) with no horizontal sweep — verify via ScaffoldMotion(reducedMotion: true) ancestor
    - Test 4: ScaffoldSkeleton with explicit width=200, height=20, borderRadius=8 overrides default values
    - Test 5: ScaffoldDisabledOverlay renders child normally when disabled=false
    - Test 6: ScaffoldDisabledOverlay renders dim overlay + IgnorePointer when disabled=true
    - Test 7: ScaffoldDisabledOverlay with reason explicitly set applies Semantics(tooltip: reason)
  </behavior>
  <action>
    **scaffold_skeleton.dart:** StatefulWidget with SingleTickerProviderStateMixin. Constructor: double? width, double? height, double? borderRadius, Widget? child. In build(): read ScaffoldMotion.of(context).reducedMotion. When reducedMotion is false: create AnimationController with duration=ScaffoldMotion.durations.medium, drive a shimmer AnimationBuilder that sweeps a LinearGradient (colors: [skeletonBaseColor, skeletonShimmerColor, skeletonBaseColor], stops: [0.0, 0.5, 1.0]) via an AnimatedBuilder + ShaderMask or a ClipRRect with a sliding gradient. When reducedMotion is true: use AnimationController with duration=ScaffoldMotion.durations.medium for opacity pulse (0.6→1.0), repeat(reverse: true). Per UI-SPEC: ExcludeSemantics(true). Configurable width/height/borderRadius override dimens defaults. Animation respects ScaffoldMotion.durations/curves.

    **scaffold_disabled_overlay.dart:** StatelessWidget. Constructor: required Widget child, bool disabled=false, String? reason. In build(): if disabled is false, return child. If disabled: wrap in Stack with child (opacity: 1.0 - dimens.disabledOverlayOpacity) behind a Positioned.fill Container with color=palette.disabledOverlayColor, and IgnorePointer. If reason is non-null, wrap in Semantics(tooltip: reason). Per D-02: "Semantics tooltip for reason." Per UI-SPEC: enabled state renders child with zero-opacity cost. Add barrel exports.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_skeleton_test.dart test/components/scaffold_disabled_overlay_test.dart && dart analyze --fatal-infos lib/components/scaffold_skeleton.dart lib/components/scaffold_disabled_overlay.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_skeleton_test.dart exists with 4 test cases
    - test/components/scaffold_disabled_overlay_test.dart exists with 3 test cases
    - All 7 tests pass
    - dart analyze --fatal-infos clean on both widget files
    - ScaffoldSkeleton reads ScaffoldMotion.of(context).reducedMotion (grep confirms)
    - ScaffoldDisabledOverlay.dim color references palette.disabledOverlayColor + dimens.disabledOverlayOpacity (grep confirms)
    - Barrel exports both files
  </acceptance_criteria>
  <done>ScaffoldSkeleton and ScaffoldDisabledOverlay created, 7 tests green, barrel-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScaffoldBadge — dot/count/icon/text indicator with empty state</name>
  <files>lib/components/scaffold_badge.dart, test/components/scaffold_badge_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/theme/scaffold_theme.dart, src/scaffold/lib/theme/scaffold_palette.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldBadge(variant: dot) renders an 8px circle in lightGreenPrimary
    - Test 2: ScaffoldBadge(variant: count, count: 5) renders pill with "5" in textTheme.labelSmall, white text on badgeColor background
    - Test 3: ScaffoldBadge(variant: count, count: 150, maxDigits: 2) renders "99+" (truncation)
    - Test 4: ScaffoldBadge(variant: count, count: 0) renders SizedBox.shrink() (empty state per UI-SPEC covered:empty)
    - Test 5: ScaffoldBadge(variant: icon, icon: Icons.star) renders 16px icon in 24px colored circle
    - Test 6: ScaffoldBadge(variant: text, text: "New") renders custom label pill
    - Test 7: ScaffoldBadge with disabled=true applies 0.4 opacity
    - Test 8: ScaffoldBadge with badgeColor override uses the provided color instead of default lightGreenPrimary
  </behavior>
  <action>
    Create lib/components/scaffold_badge.dart. StatelessWidget. Constructor: BadgeVariant variant=BadgeVariant.dot, int count=0, String? text, IconData? icon, Color? badgeColor (defaults to palette.lightGreenPrimary), Widget? child, bool disabled=false, int maxDigits=2. Define enum BadgeVariant { dot, count, icon, text }.

    In build(): if disabled, wrap in Opacity(opacity: 0.4). Switch on variant:
    - dot: 8px Container with BoxShape.circle, color=badgeColor.
    - count: if count==0 → SizedBox.shrink(). Otherwise pill-shaped Container with badgeColor fill, borderRadius=dimens.radiusPill, padding=EdgeInsets.symmetric(horizontal: 6, vertical: 2), child=Text(count > 99 && maxDigits==2 ? "99+" : count.toString(), style: textTheme.labelSmall?.copyWith(color: Colors.white)). minWidth=20.
    - icon: 24px circle Container with badgeColor, child=Icon(icon, size: 16, color: Colors.white).
    - text: pill Container with badgeColor fill, borderRadius=dimens.radiusPill, padding=EdgeInsets.symmetric(horizontal: 8, vertical: 2), child=Text(text ?? '', style: textTheme.labelSmall?.copyWith(color: Colors.white)).

    Per D-04: "NO positioning — consumer uses Stack+Positioned." The badge only renders itself; no child wrapping or positioning. Per D-02: no explicit Semantics role specified (badge is supplementary visual), but label text is accessible via the Text widget's own default semantics.

    Add barrel export.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_badge_test.dart && dart analyze --fatal-infos lib/components/scaffold_badge.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_badge_test.dart has 8 test cases
    - All 8 tests pass
    - dart analyze --fatal-infos clean on scaffold_badge.dart
    - Badge count=0 renders SizedBox.shrink() (verified by test)
    - Badge uses palette.lightGreenPrimary as default badgeColor (grep confirms fallback)
    - No hardcoded hex values in widget code
    - Barrel exports scaffold_badge.dart
  </acceptance_criteria>
  <done>ScaffoldBadge created, 8 tests green, barrel-exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScaffoldStatusIndicator + ScaffoldPressable — status dot + interaction wrapper</name>
  <files>lib/components/scaffold_status_indicator.dart, lib/components/scaffold_pressable.dart, test/components/scaffold_status_indicator_test.dart, test/components/scaffold_pressable_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/theme/scaffold_theme.dart, src/scaffold/lib/theme/scaffold_palette.dart, src/scaffold/lib/components/scaffold_touch_target.dart, src/scaffold/lib/components/scaffold_focus_outline.dart, src/scaffold/lib/components/scaffold_disabled_overlay.dart, src/scaffold/lib/components/scaffold_motion.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: ScaffoldStatusIndicator(status: success) renders 12px circle in palette.statusSuccess
    - Test 2: ScaffoldStatusIndicator(status: warning) renders in palette.statusWarningText
    - Test 3: ScaffoldStatusIndicator(status: error) renders in palette.statusError
    - Test 4: ScaffoldStatusIndicator(status: info) renders in palette.blue500
    - Test 5: ScaffoldStatusIndicator(status: neutral) renders in palette.textSecondary
    - Test 6: ScaffoldStatusIndicator registers SemanticsRole.status with label from constructor label parameter
    - Test 7: ScaffoldPressable renders child wrapped in TouchTarget + hover/press state layers
    - Test 8: ScaffoldPressable onPressed fires when tapped
    - Test 9: ScaffoldPressable registers SemanticsRole.button and responds to Enter key
    - Test 10: ScaffoldPressable disabled state applies DisabledOverlay and blocks onPressed
  </behavior>
  <action>
    **scaffold_status_indicator.dart:** StatelessWidget. Constructor: required StatusVariant status, double dotSize=12.0, String? label, bool disabled=false. Define enum StatusVariant { success, warning, error, info, neutral }. In build(): resolve color from variant → palette mapping (success=palette.statusSuccess, warning=palette.statusWarningText, error=palette.statusError, info=palette.blue500, neutral=palette.textSecondary). Render Container(width: dotSize, height: dotSize, decoration: BoxDecoration(shape: BoxShape.circle, color: color)). Wrap in Semantics(role: SemanticsRole.status, label: label) if label non-null. If disabled, apply Opacity(0.4). Per D-02: "Semantics role: status. label announced by screen readers."

    **scaffold_pressable.dart:** StatefulWidget. Constructor: Widget? child, VoidCallback? onPressed, VoidCallback? onLongPress, ValueChanged<bool>? onHoverChanged, FocusNode? focusNode, bool disabled=false. In build(): compose ScaffoldTouchTarget + ScaffoldFocusOutline + state layers. Create or use FocusNode. Track hover state via MouseRegion or InkWell. State layers: hover → Container with textPrimary at 8% opacity; pressed → Container with textPrimary at 12% opacity. Use ScaffoldMotion.durations.short for hover fade, ScaffoldMotion.durations.medium for press resolution. Disabled: wrap in ScaffoldDisabledOverlay. Keyboard: respond to Enter/Space via FocusNode onKeyEvent. Semantics: role button. Per UI-SPEC: "Composes ScaffoldTouchTarget internally to guarantee 48x48 hit area."

    Add barrel exports. Per D-04: Pressable serves as the canonical interaction wrapper — other interactive atoms compose it.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_status_indicator_test.dart test/components/scaffold_pressable_test.dart && dart analyze --fatal-infos lib/components/scaffold_status_indicator.dart lib/components/scaffold_pressable.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_status_indicator_test.dart has 6 test cases
    - test/components/scaffold_pressable_test.dart has 4 test cases
    - All 10 tests pass
    - dart analyze --fatal-infos clean on both widget files
    - ScaffoldPressable imports and composes ScaffoldTouchTarget (grep confirms import)
    - ScaffoldPressable imports ScaffoldFocusOutline (grep confirms import)
    - ScaffoldStatusIndicator uses SemanticsRole.status (grep confirms)
    - Barrel exports both files
    - No hardcoded hex values in widget .dart files
  </acceptance_criteria>
  <done>ScaffoldStatusIndicator and ScaffoldPressable created, 10 tests green, barrel-exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller callbacks → Widget state | ScaffoldPressable fires onPressed/onLongPress; caller owns callback safety. Skeleton animation state isolated to widget lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Denial of Service | Skeleton AnimationController leak | low | mitigate | AnimationController disposed in widget dispose(); SingleTickerProviderStateMixin ensures single ticker; test verifies no memory leak |
| T-06-06 | Denial of Service | Pressable callback after disposal | low | mitigate | Check mounted before calling onPressed; FocusNode disposed in widget dispose() |
</threat_model>

<verification>
- `cd src/scaffold && flutter test test/components/scaffold_skeleton_test.dart test/components/scaffold_disabled_overlay_test.dart test/components/scaffold_badge_test.dart test/components/scaffold_status_indicator_test.dart test/components/scaffold_pressable_test.dart` — all green
- `cd src/scaffold && dart analyze --fatal-infos lib/components/scaffold_skeleton.dart lib/components/scaffold_disabled_overlay.dart lib/components/scaffold_badge.dart lib/components/scaffold_status_indicator.dart lib/components/scaffold_pressable.dart` — zero issues
- Grep barrel for all 5 export lines
</verification>

<success_criteria>
1. ScaffoldSkeleton renders animated shimmer; respects reducedMotion → static pulse (per D-01)
2. ScaffoldDisabledOverlay renders dim + IgnorePointer when disabled, transparent when enabled
3. ScaffoldBadge renders dot/count/icon/text variants; count=0 renders empty (SizedBox.shrink); count overflow truncates to "99+"
4. ScaffoldStatusIndicator renders 5 color-coded status states with Semantics role status
5. ScaffoldPressable composes TouchTarget + FocusOutline + state layers for press/hover/focus/disable; keyboard Enter/Space fire onPressed
6. dart analyze --fatal-infos clean; all 25 tests green; barrel exports updated
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Classes
- ScaffoldSkeleton (StatefulWidget, SingleTickerProviderStateMixin) — lib/components/scaffold_skeleton.dart
- ScaffoldDisabledOverlay (StatelessWidget) — lib/components/scaffold_disabled_overlay.dart
- BadgeVariant (enum: dot, count, icon, text) — lib/components/scaffold_badge.dart
- ScaffoldBadge (StatelessWidget) — lib/components/scaffold_badge.dart
- StatusVariant (enum: success, warning, error, info, neutral) — lib/components/scaffold_status_indicator.dart
- ScaffoldStatusIndicator (StatelessWidget) — lib/components/scaffold_status_indicator.dart
- ScaffoldPressable (StatefulWidget) — lib/components/scaffold_pressable.dart
- _ScaffoldPressableState (State) — lib/components/scaffold_pressable.dart
</parameter>
