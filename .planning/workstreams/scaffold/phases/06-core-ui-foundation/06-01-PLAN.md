---
phase: 06-core-ui-foundation
plan: "01"
type: tracer
wave: 1
depends_on: []
files_modified:
  - lib/theme/scaffold_palette.dart
  - lib/theme/scaffold_dimens.dart
  - lib/components/scaffold_motion.dart
  - lib/components/scaffold_surface.dart
  - lib/components/scaffold_touch_target.dart
  - lib/components/scaffold_focus_outline.dart
  - lib/frontend_scaffold.dart
  - test/components/scaffold_motion_test.dart
  - test/components/scaffold_surface_test.dart
  - test/components/scaffold_touch_target_test.dart
  - test/components/scaffold_focus_outline_test.dart
  - example/lib/demos/tracer_demo.dart
autonomous: true
requirements:
  - WIDG-01
  - WIDG-02
  - WIDG-03
  - WIDG-04
user_setup: []

must_haves:
  truths:
    - "ScaffoldPalette exposes 7 new color tokens: focusRingColor, skeletonBaseColor, skeletonShimmerColor, disabledOverlayColor, dragFeedbackBackground, dropZoneHighlight, dropZoneRejected (per D-03)"
    - "ScaffoldDimens exposes 6 new dimension tokens: focusRingWidth, skeletonCornerRadius, disabledOverlayOpacity, dragHandleSize, minTouchTarget, touchTargetPadding (per D-03)"
    - "Existing ScaffoldPalette.copyWith and lerp cover all 7 new tokens; existing ScaffoldDimens.copyWith and lerp cover all 6 new tokens"
    - "ScaffoldMotion.of(context) returns an InheritedWidget-backed value with a reducedMotion boolean field (per D-01)"
    - "ScaffoldMotion.durations exposes static const shorts (150ms), medium (300ms), long (500ms)"
    - "ScaffoldMotion.curves exposes static const standard (easeInOut), decelerate (easeOut), emphasized (easeInOut with slight overshoot)"
    - "ScaffoldSurface renders a Container with configurable color (defaults to context.palette.deepBlueCardColor), borderRadius (defaults to context.dimens.borderRadiusCard), border, elevation, shape, padding, and child — no content knowledge"
    - "ScaffoldTouchTarget wraps child in a transparent Padding to guarantee min 48x48 hit area using context.dimens.minTouchTarget, registering Semantics(container: true)"
    - "ScaffoldFocusOutline renders a 2px CustomPaint rounded-rect border in palette.focusRingColor when FocusNode.hasFocus AND (keyboard focus active OR accessibleNavigation true) (per D-02 focus rule)"
  artifacts:
    - lib/theme/scaffold_palette.dart (+7 fields, copyWith, lerp, defaultPalette entries)
    - lib/theme/scaffold_dimens.dart (+6 fields, copyWith, lerp, defaultDimens entries)
    - lib/components/scaffold_motion.dart (ScaffoldMotion InheritedWidget + static duration/curve constants)
    - lib/components/scaffold_surface.dart (ScaffoldSurface StatelessWidget)
    - lib/components/scaffold_touch_target.dart (ScaffoldTouchTarget StatelessWidget)
    - lib/components/scaffold_focus_outline.dart (ScaffoldFocusOutline StatefulWidget with FocusNode listener)
    - test/components/scaffold_motion_test.dart
    - test/components/scaffold_surface_test.dart
    - test/components/scaffold_touch_target_test.dart
    - test/components/scaffold_focus_outline_test.dart
    - example/lib/demos/tracer_demo.dart
  key_links:
    - "ScaffoldMotion InheritedWidget → ScaffoldMotion.of(context) → reducedMotion boolean — consumed by every animated atom"
    - "ScaffoldPalette/ScaffoldDimens ThemeExtension registration via scaffoldThemeExtensions → context.palette/context.dimens → atom build() lookups"
    - "ScaffoldTouchTarget Padding wrapping pattern → all subsequent interactive atoms that guarantee 48x48 hit area"
    - "ScaffoldFocusOutline accessibleNavigation check → D-02 requirement that focus ring is visible when TalkBack/VoiceOver is active"
---

<objective>
Wire the foundational layer of the Core UI Foundation: expand theme tokens per D-03, ship the ScaffoldMotion motion/animation utility with reduced-motion support (D-01), and deliver the three zero-dependency foundation atoms (Surface, TouchTarget, FocusOutline) that every subsequent atom builds on. Prove the thin slice works end-to-end with a tracer demo rendering a focusable, motion-aware, touch-target-enforced surface with correct palette colors.

Purpose: Establish the theme token contract and the motion/focus/touch primitives before any dependent widgets are built. The tracer demo proves the architectural pattern (ThemeExtension → context.palette/dimens → widget build, InheritedWidget → of(context) → reduced motion) is correct end-to-end.

Output: Expanded token classes, 4 new widget atoms, widget tests for each, and a tracer demo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md
@.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/STATE.md
@src/scaffold/lib/theme/scaffold_palette.dart
@src/scaffold/lib/theme/scaffold_dimens.dart
@src/scaffold/lib/theme/scaffold_theme.dart
@src/scaffold/lib/frontend_scaffold.dart
@src/scaffold/lib/components/action_button.dart
@src/scaffold/lib/utils/breakpoints.dart
@.planning/workstreams/scaffold/phases/05-scaffold-submodule-consolidation/05-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Expand theme tokens (D-03) — 7 palette + 6 dimens fields with full ThemeExtension contract</name>
  <files>lib/theme/scaffold_palette.dart, lib/theme/scaffold_dimens.dart</files>
  <read_first>src/scaffold/lib/theme/scaffold_palette.dart, src/scaffold/lib/theme/scaffold_dimens.dart, src/scaffold/lib/theme/scaffold_theme.dart, .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md (Color section + Spacing section for exact token names, dark defaults, and usage), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md (D-03)</read_first>
  <behavior>
    - Test 1: ScaffoldPalette.defaultPalette.focusRingColor equals lightGreenPrimary (0xFF00EAAE) — focus ring inherits primary accent by default
    - Test 2: ScaffoldPalette.defaultPalette.skeletonBaseColor equals surfaceElevated (0xFF0C0E14) — skeleton base is the elevated surface tone
    - Test 3: ScaffoldPalette.defaultPalette.skeletonShimmerColor equals deepBlueCardColor (0xFF0A121F) — shimmer contrasts against base
    - Test 4: ScaffoldPalette.defaultPalette.disabledOverlayColor equals Color(0x66000000) — 40% black overlay for dark theme
    - Test 5: ScaffoldPalette.defaultPalette.dragFeedbackBackground equals surfaceElevated (0xFF0C0E14)
    - Test 6: ScaffoldPalette.defaultPalette.dropZoneHighlight equals lightGreenPrimary at 20% opacity (Color(0x3300EAAE))
    - Test 7: ScaffoldPalette.defaultPalette.dropZoneRejected equals statusError at 20% opacity (Color(0x33FF4D4D))
    - Test 8: ScaffoldPalette.copyWith(focusRingColor: Colors.red).focusRingColor == Colors.red and all other fields unchanged
    - Test 9: ScaffoldPalette.lerp between identical palettes returns same values; lerp to different palette at t=0.5 returns midpoint colors
    - Test 10: ScaffoldDimens.defaultDimens.focusRingWidth equals 2.0
    - Test 11: ScaffoldDimens.defaultDimens.skeletonCornerRadius equals 4.0
    - Test 12: ScaffoldDimens.defaultDimens.disabledOverlayOpacity equals 0.40
    - Test 13: ScaffoldDimens.defaultDimens.dragHandleSize equals 24.0
    - Test 14: ScaffoldDimens.defaultDimens.minTouchTarget equals 48.0
    - Test 15: ScaffoldDimens.defaultDimens.touchTargetPadding equals 12.0
    - Test 16: ScaffoldDimens.copyWith(minTouchTarget: 56.0).minTouchTarget == 56.0 and all other fields unchanged
    - Test 17: ScaffoldDimens.lerp between identical dimens returns same values; lerp to different dimens at t=0.5 returns midpoint values
  </behavior>
  <action>
    Add 7 Color fields to ScaffoldPalette: focusRingColor, skeletonBaseColor, skeletonShimmerColor, disabledOverlayColor, dragFeedbackBackground, dropZoneHighlight, dropZoneRejected. Place them after the existing 15 fields, before the constructor. Each field follows the existing pattern: `final Color tokenName;` with a doc comment stating its purpose. Update the const constructor to accept all 7 new fields as required named parameters. Update defaultPalette to provide the dark-mode defaults per D-03/UI-SPEC (focusRingColor: ScaffoldColors.lightGreenPrimary, skeletonBaseColor: 0xFF0C0E14, skeletonShimmerColor: 0xFF0A121F, disabledOverlayColor: 0x66000000, dragFeedbackBackground: 0xFF0C0E14, dropZoneHighlight: 0x3300EAAE, dropZoneRejected: 0x33FF4D4D). Update copyWith to accept all 7 new optional Color? parameters and pass them through. Update lerp to interpolate all 7 new fields using Color.lerp.

    Add 6 double fields to ScaffoldDimens: focusRingWidth, skeletonCornerRadius, disabledOverlayOpacity, dragHandleSize, minTouchTarget, touchTargetPadding. Place them after the existing 17 fields, before the constructor. Each field follows the existing pattern: `final double tokenName;` with a doc comment. Update the const constructor to accept all 6 new fields as required named parameters. Update defaultDimens to provide values per D-03/UI-SPEC (focusRingWidth: 2.0, skeletonCornerRadius: 4.0, disabledOverlayOpacity: 0.40, dragHandleSize: 24.0, minTouchTarget: 48.0, touchTargetPadding: 12.0). Update copyWith to accept all 6 new optional double? parameters. Update lerp to interpolate all 6 new fields using the existing lerpDouble helper pattern.

    Per D-03: "Tokens use copyWith/lerp to match the existing ThemeExtension pattern". The existing fields before the additions must remain unchanged in name, default value, and position — consumers from Phase 5 depend on them. dart analyze --fatal-infos must pass on the modified theme files.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_palette_token_test.dart test/components/scaffold_dimens_token_test.dart && dart analyze --fatal-infos lib/theme/scaffold_palette.dart lib/theme/scaffold_dimens.dart</automated>
  </verify>
  <acceptance_criteria>
    - Token test file test/components/scaffold_palette_token_test.dart exists with 9 test cases covering focusRingColor/skeletonBaseColor/skeletonShimmerColor/disabledOverlayColor/dragFeedbackBackground/dropZoneHighlight/dropZoneRejected default values, copyWith propagation, and lerp midpoint interpolation
    - Token test file test/components/scaffold_dimens_token_test.dart exists with 7 test cases covering focusRingWidth/skeletonCornerRadius/disabledOverlayOpacity/dragHandleSize/minTouchTarget/touchTargetPadding default values, copyWith propagation, and lerp midpoint interpolation
    - All 16 tests pass
    - dart analyze --fatal-infos on palette.dart and dimens.dart returns zero issues
    - Grep confirms no field was removed: `grep -c 'final Color' lib/theme/scaffold_palette.dart` returns 22 (15 existing + 7 new)
    - Grep confirms no field was removed: `grep -c 'final double' lib/theme/scaffold_dimens.dart` returns 23 (17 existing + 6 new)
  </acceptance_criteria>
  <done>ScaffoldPalette has 22 Color fields (15 + 7) with full copyWith/lerp coverage. ScaffoldDimens has 23 double fields (17 + 6) with full copyWith/lerp coverage. All tests green, analyze clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScaffoldMotion — InheritedWidget + static animation constants (D-01)</name>
  <files>lib/components/scaffold_motion.dart, test/components/scaffold_motion_test.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/theme/scaffold_theme.dart (context.palette pattern), src/scaffold/lib/components/action_button.dart (SingleTickerProviderStateMixin + AnimationController pattern), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md (ScaffoldMotion visual contract section), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md (D-01)</read_first>
  <behavior>
    - Test 1: ScaffoldMotion.durations.short equals Duration(milliseconds: 150)
    - Test 2: ScaffoldMotion.durations.medium equals Duration(milliseconds: 300)
    - Test 3: ScaffoldMotion.durations.long equals Duration(milliseconds: 500)
    - Test 4: ScaffoldMotion.curves.standard equals Curves.easeInOut
    - Test 5: ScaffoldMotion.curves.decelerate equals Curves.easeOut
    - Test 6: ScaffoldMotion.curves.emphasized is a custom curve that overshoots (test its transform returns value > 1.0 at some t < 1.0)
    - Test 7: ScaffoldMotion(reducedMotion: true).of(context).reducedMotion is true
    - Test 8: ScaffoldMotion(reducedMotion: false).of(context).reducedMotion is false
    - Test 9: ScaffoldMotion.of(context) throws FlutterError when no ScaffoldMotion ancestor exists
    - Test 10: ScaffoldMotion(reducedMotion: true) ancestor → ScaffoldMotion.of(context).reducedMotion is true in nested child
  </behavior>
  <action>
    Create lib/components/scaffold_motion.dart. This file exposes two things per D-01:

    1. A `ScaffoldMotionDurations` class with three static const Duration fields: short=150ms, medium=300ms, long=500ms. Follow the existing ScaffoldElevation pattern (class with private constructor + static consts).

    2. A `ScaffoldMotionCurves` class with three static const Curve fields: standard=Curves.easeInOut, decelerate=Curves.easeOut, emphasized= custom Cubic(0.2, 0.0, 0.0, 1.0) with a slight overshoot — define as a static const with a doc comment noting it is the M3 emphasized decelerate curve.

    3. A `ScaffoldMotion` InheritedWidget: takes `required bool reducedMotion` and `required Widget child`. Exposes static `ScaffoldMotionData? of(BuildContext context)` returning `context.dependOnInheritedWidgetOfExactType<ScaffoldMotion>()` (typed as ScaffoldMotion so callers read `.reducedMotion`). `updateShouldNotify` returns `oldWidget.reducedMotion != reducedMotion`.

    The public API is `ScaffoldMotion.of(context).reducedMotion` — every atom reads this boolean. Per D-01: "Widgets read ScaffoldMotion.of(context).reducedMotion for accessibility preferences; animations respect this by substituting zero-duration or fading instead of motion."

    Add `library frontend_scaffold;` and `export 'scaffold_motion.dart';` to the barrel. No linter suppressions.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_motion_test.dart && dart analyze --fatal-infos lib/components/scaffold_motion.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_motion_test.dart exists with 10 test cases per behavior block
    - All 10 tests pass
    - dart analyze --fatal-infos on scaffold_motion.dart returns zero issues
    - grep 'export.*scaffold_motion' lib/frontend_scaffold.dart returns exactly one matching line
    - ScaffoldMotion class uses InheritedWidget as its superclass (grep confirms)
  </acceptance_criteria>
  <done>ScaffoldMotionDurations, ScaffoldMotionCurves, and ScaffoldMotion InheritedWidget live in lib/components/scaffold_motion.dart. Barrel exports the file. 10 tests pass, analyze clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScaffoldSurface + ScaffoldTouchTarget + ScaffoldFocusOutline + tracer demo — wire three zero-dep foundation atoms</name>
  <files>lib/components/scaffold_surface.dart, lib/components/scaffold_touch_target.dart, lib/components/scaffold_focus_outline.dart, test/components/scaffold_surface_test.dart, test/components/scaffold_touch_target_test.dart, test/components/scaffold_focus_outline_test.dart, example/lib/demos/tracer_demo.dart, lib/frontend_scaffold.dart</files>
  <read_first>src/scaffold/lib/components/action_button.dart (widget pattern: StatefulWidget, context.palette/context.dimens resolution, textTheme usage), src/scaffold/lib/theme/scaffold_palette.dart (updated token names), src/scaffold/lib/theme/scaffold_dimens.dart (updated token names), src/scaffold/lib/theme/scaffold_theme.dart (ScaffoldThemeX extension), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-UI-SPEC.md (ScaffoldSurface, ScaffoldTouchTarget, ScaffoldFocusOutline sections), .planning/workstreams/scaffold/phases/06-core-ui-foundation/06-CONTEXT.md (D-04 pure composability, D-02 accessibility)</read_first>
  <behavior>
    - Test 1: ScaffoldSurface with default constructor renders a Container with borderRadius=dimens.borderRadiusCard and color=palette.deepBlueCardColor (pump, find by type Container, verify decoration properties)
    - Test 2: ScaffoldSurface with explicit color, borderRadius, border, padding, and elevation applies all overrides correctly
    - Test 3: ScaffoldSurface renders child widget passed via child parameter
    - Test 4: ScaffoldSurface with shape=BoxShape.circle renders a circular container
    - Test 5: ScaffoldTouchTarget wrapper adds Padding so child smaller than 48x48 renders at 48x48 hit-testable area
    - Test 6: ScaffoldTouchTarget registers Semantics(container: true) — verify with tester.getSemantics
    - Test 7: ScaffoldTouchTarget with explicit minWidth=56, minHeight=56 enforces 56x56
    - Test 8: ScaffoldFocusOutline renders a CustomPaint when FocusNode has focus and keyboard focus is active
    - Test 9: ScaffoldFocusOutline does NOT render CustomPaint when FocusNode does not have focus
    - Test 10: ScaffoldFocusOutline renders CustomPaint when accessibleNavigation is true even without keyboard focus (per D-02: "visible in all states — not just keyboard-focused but also when TalkBack/VoiceOver is active")
    - Test 11: ScaffoldFocusOutline uses palette.focusRingColor (the new token from Task 1) for the paint color
    - Test 12: ScaffoldFocusOutline uses dimens.focusRingWidth (2.0) for the stroke width
    - Test 13: Tracer demo renders a ScaffoldMotion → ScaffoldSurface → ScaffoldTouchTarget → ScaffoldFocusOutline composition and reducedMotion toggle updates the display
  </behavior>
  <action>
    Create three widget files in lib/components/:

    **scaffold_surface.dart:** A StatelessWidget. Constructor parameters: Color? color (default: null → resolves to palette.deepBlueCardColor), BorderRadiusGeometry? borderRadius (default: null → resolves to dimens.borderRadiusCard), BoxBorder? border, double? elevation, EdgeInsetsGeometry? padding, BoxShape shape (default: BoxShape.rectangle), Widget? child. In build(): resolve palette/dimens via ScaffoldThemeX, compute final color/borderRadius with ?? fallbacks. If shape is circle, wrap in ClipOval. Return Container/Material (for elevation ink effects) or DecoratedBox with the resolved properties wrapping child. Follow the pattern: `final palette = context.palette; final dimens = context.dimens;` at the top of build(). Per D-04: "NO content knowledge — renders whatever child is passed."
    
    **scaffold_touch_target.dart:** A StatelessWidget. Constructor parameters: Widget? child, double minWidth=48 (reads dimens.minTouchTarget at build time), double minHeight=48. In build(): compute `final hPad = max(0, (dimens.minTouchTarget - childWidth) / 2)` — but since child size is unknown pre-layout, use `ConstrainedBox(constraints: BoxConstraints(minWidth: dimens.minTouchTarget, minHeight: dimens.minTouchTarget))` with `EdgeInsets.all(dimens.touchTargetPadding)` Padding when child is smaller. Register `Semantics(container: true)`. Per D-02: "Registers Semantics with container: true so screen readers identify the touch region."

    **scaffold_focus_outline.dart:** A StatefulWidget. Constructor parameters: Widget? child, FocusNode? focusNode, BorderRadiusGeometry? borderRadius, Color? ringColor, double? ringWidth. Creates an internal FocusNode if none provided. Listens to focus changes. In build(): resolve palette.focusRingColor and dimens.focusRingWidth with ?? fallbacks. Compute `showRing` = `(focusNode.hasFocus) && (FocusManager.instance.keyboardFocus || MediaQuery.of(context).accessibleNavigation)`. When showRing is true, wrap child in a Stack with a Positioned.fill CustomPaint painter that draws a rounded-rect border at the resolved ringWidth using the resolved ringColor, inset by ringWidth/2 from the outer edge. Per D-02: "Focus ring MUST be visible when TalkBack/VoiceOver is active (via accessibleNavigation), not just keyboard focus."

    **tracer_demo.dart:** A StatefulWidget in example/lib/demos/. Renders a ScaffoldMotion(reducedMotion: _reduced) wrapping a Column containing: (1) a Switch to toggle _reduced, (2) a ScaffoldSurface containing a ScaffoldTouchTarget containing a ScaffoldFocusOutline child — a Text("Focus me") with an associated FocusNode. The demo proves: palette tokens render, touch target enforces size, focus ring shows on keyboard focus and accessibleNavigation, and reducedMotion propagates from ScaffoldMotion to any consumer.

    Add library declarations and export lines to frontend_scaffold.dart barrel for each new widget file. Per D-04 pure composability: atoms do exactly one thing. No convenience constructors.
  </action>
  <verify>
    <automated>cd src/scaffold && flutter test test/components/scaffold_surface_test.dart test/components/scaffold_touch_target_test.dart test/components/scaffold_focus_outline_test.dart && dart analyze --fatal-infos lib/components/scaffold_surface.dart lib/components/scaffold_touch_target.dart lib/components/scaffold_focus_outline.dart example/lib/demos/tracer_demo.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/components/scaffold_surface_test.dart exists with 4 test cases
    - test/components/scaffold_touch_target_test.dart exists with 3 test cases
    - test/components/scaffold_focus_outline_test.dart exists with 4 test cases
    - All 11 tests pass (plus tracer demo loads without crash when run in example)
    - dart analyze --fatal-infos returns zero issues across all 3 widget files + demo
    - Barrel frontend_scaffold.dart exports all 3 new files (grep confirms export lines)
    - No widget file contains hardcoded hex color values (grep for '0xFF' in widget .dart files returns zero matches)
    - No widget file contains hardcoded numeric dimension values (grep-regex for regex like `(48|\.0)[^a-zA-Z]` may have false positives; instead confirm dimens.* is used via grep for 'context.dimens')
  </acceptance_criteria>
  <done>ScaffoldSurface, ScaffoldTouchTarget, ScaffoldFocusOutline all live in lib/components/, exported from barrel, with full test coverage. Tracer demo renders the thin slice end-to-end. All tests green, analyze clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller parameters → Widget internals | Caller-supplied values (colors, children, callbacks) enter widget build via constructor parameters. Widgets must handle null/default gracefully. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Denial of Service | All widget atoms | low | mitigate | Widget build methods use ?? fallbacks for all theme token lookups; null child returns SizedBox.shrink(); tests verify no crash on null/missing theme data |
| T-06-02 | Tampering | InheritedWidget propagation | low | accept | ScaffoldMotion.reducedMotion is a boolean; a malicious ancestor could set it to false to force animations on accessibility users — but this requires control of the widget tree, which is in the consumer app's trust domain |
| T-06-SC | Tampering | npm/pip/cargo installs | N/A | N/A | No package-manager installs in this phase; Flutter/Dart pub dependencies already vetted in Phase 5 (toast sync 05-05-SUMMARY) |
</threat_model>

<verification>
- `cd src/scaffold && dart analyze --fatal-infos lib/ test/` returns zero issues across all new and modified files
- `cd src/scaffold && flutter test test/components/scaffold_palette_token_test.dart test/components/scaffold_dimens_token_test.dart test/components/scaffold_motion_test.dart test/components/scaffold_surface_test.dart test/components/scaffold_touch_target_test.dart test/components/scaffold_focus_outline_test.dart` — all tests green
- Grep confirms no hardcoded 0xFF colors in widget .dart files (themes exempted)
- Grep confirms no hardcoded numeric dimensions in widget .dart files that should use dimens tokens
- Tracer demo loads in the example app (flutter run on example) without red-screen errors
</verification>

<success_criteria>
1. ScaffoldPalette.defaultPalette exposes 22 Color fields (15 existing + 7 new) with correct dark-mode defaults
2. ScaffoldDimens.defaultDimens exposes 23 double fields (17 existing + 6 new) with correct values
3. ScaffoldMotion.of(context).reducedMotion returns the inherited value; static durations and curves match the UI-SPEC contract
4. ScaffoldSurface renders child with background/border/shape/elevation using context.palette tokens
5. ScaffoldTouchTarget wraps any child in 48x48 minimum hit area with Semantics container
6. ScaffoldFocusOutline shows focus ring on keyboard focus AND accessibleNavigation
7. Tracer demo proves the Motion → Surface → TouchTarget → FocusOutline composition renders without error
8. dart analyze --fatal-infos clean across all new files; all new tests green; barrel exports updated
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/06-core-ui-foundation/06-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Classes
- ScaffoldMotion (InheritedWidget) — lib/components/scaffold_motion.dart
- ScaffoldMotionDurations (static utility class) — lib/components/scaffold_motion.dart
- ScaffoldMotionCurves (static utility class) — lib/components/scaffold_motion.dart
- ScaffoldSurface (StatelessWidget) — lib/components/scaffold_surface.dart
- ScaffoldTouchTarget (StatelessWidget) — lib/components/scaffold_touch_target.dart
- ScaffoldFocusOutline (StatefulWidget) — lib/components/scaffold_focus_outline.dart
- _FocusRingPainter (CustomPainter) — lib/components/scaffold_focus_outline.dart
- TracerDemo (StatefulWidget) — example/lib/demos/tracer_demo.dart

### Modified types (new fields added)
- ScaffoldPalette (ThemeExtension) — 7 new Color fields: focusRingColor, skeletonBaseColor, skeletonShimmerColor, disabledOverlayColor, dragFeedbackBackground, dropZoneHighlight, dropZoneRejected
- ScaffoldDimens (ThemeExtension) — 6 new double fields: focusRingWidth, skeletonCornerRadius, disabledOverlayOpacity, dragHandleSize, minTouchTarget, touchTargetPadding

### Test classes
- ScaffoldPaletteTokenTest — test/components/scaffold_palette_token_test.dart
- ScaffoldDimensTokenTest — test/components/scaffold_dimens_token_test.dart
- ScaffoldMotionTest — test/components/scaffold_motion_test.dart
- ScaffoldSurfaceTest — test/components/scaffold_surface_test.dart
- ScaffoldTouchTargetTest — test/components/scaffold_touch_target_test.dart
- ScaffoldFocusOutlineTest — test/components/scaffold_focus_outline_test.dart
</parameter>
