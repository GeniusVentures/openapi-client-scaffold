---
phase: 07-media-integration-widgets
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - lib/frontend_scaffold.dart
  - example/lib/main.dart
autonomous: true
requirements: [WIDG-29, WIDG-30, WIDG-31]

must_haves:
  truths:
    - "A consumer app importing only package:frontend_scaffold/frontend_scaffold.dart can reference MediaCard, MediaControls, and WalletConnectSheet"
    - "The example app's main.dart registers all three new demos and the app still compiles"
    - "dart analyze --fatal-infos exits 0 across the entire package"
    - "flutter test exits 0 across the entire test suite (Phase 6 + Phase 7 tests, 154 + 31 expected)"
  artifacts:
    - path: "lib/frontend_scaffold.dart"
      provides: "Barrel export updated with three new component exports in sorted position"
      contains: "components/media_card.dart"
    - path: "example/lib/main.dart"
      provides: "Demo registration for MediaCardDemo, MediaControlsDemo, WalletConnectSheetDemo"
      contains: "MediaCardDemo"
  key_links:
    - from: "lib/frontend_scaffold.dart"
      to: "lib/components/media_card.dart"
      via: "export 'components/media_card.dart' in sorted position within the scaffold_* cluster"
      pattern: "export 'components/media_card\\.dart'"
    - from: "lib/frontend_scaffold.dart"
      to: "lib/components/media_controls.dart"
      via: "export 'components/media_controls.dart' adjacent to media_card"
      pattern: "export 'components/media_controls\\.dart'"
    - from: "lib/frontend_scaffold.dart"
      to: "lib/components/wallet_connect_sheet.dart"
      via: "export 'components/wallet_connect_sheet.dart' after the scaffold_* cluster, before desktop_body_container"
      pattern: "export 'components/wallet_connect_sheet\\.dart'"
---

<objective>
Wire Phase 7 widgets into the public surface and run the final gate. Append three export lines to `lib/frontend_scaffold.dart` in sorted position, register the three new demos in `example/lib/main.dart`, then run the full `dart analyze --fatal-infos` + `flutter test` gates across the package.

Purpose: Without the barrel export, Plans 01-03 are unreachable to consumers. Without the demo registration, the new demos are orphaned. Without the final gate, the phase has no ship signal.
Output: 2 modified files, full-package analyze + test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/scaffold/STATE.md
@.planning/workstreams/scaffold/ROADMAP.md
@.planning/workstreams/scaffold/REQUIREMENTS.md
@.planning/workstreams/scaffold/phases/07-media-integration-widgets/07-CONTEXT.md
@.planning/workstreams/scaffold/phases/07-media-integration-widgets/07-PATTERNS.md
@.planning/workstreams/scaffold/phases/07-media-integration-widgets/07-01-SUMMARY.md
@.planning/workstreams/scaffold/phases/07-media-integration-widgets/07-02-SUMMARY.md
@.planning/workstreams/scaffold/phases/07-media-integration-widgets/07-03-SUMMARY.md
@lib/frontend_scaffold.dart
@example/lib/main.dart

<interfaces>
<!-- Barrel export insertion points (from 07-PATTERNS.md, lines 412-428). -->

Current barrel (lib/frontend_scaffold.dart) has `scaffold_*` exports sorted alphabetically, then un-prefixed widgets (desktop_body_container, responsive_grid, sliding_drawer_button, string_button, text_entry_field_widget, text_form_field_logic), then directory-grouped exports (animation/, bottom_drawer/, loading/, toast/), then theme/, then utils/.

Insert (in order):
- After line 44 (`export 'components/scaffold_live_region.dart';`), before line 45 (`export 'components/scaffold_motion.dart';`):
  - `export 'components/media_card.dart';`
  - `export 'components/media_controls.dart';`

  (Alphabetically `media_*` < `scaffold_*`, but the existing barrel groups all `scaffold_*` together; the patterns doc explicitly instructs inserting after scaffold_live_region to keep the cluster intact. Follow the patterns doc.)

- After line 65 (`export 'components/scaffold_touch_target.dart';`), before line 66 (`export 'components/desktop_body_container.dart';`):
  - `export 'components/wallet_connect_sheet.dart';`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append three exports to lib/frontend_scaffold.dart</name>
  <files>lib/frontend_scaffold.dart</files>
  <read_first>
    - lib/frontend_scaffold.dart (current export ordering)
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-PATTERNS.md (barrel section, lines 412-428)
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-01-SUMMARY.md (confirms media_card.dart exists)
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-02-SUMMARY.md (confirms media_controls.dart exists)
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-03-SUMMARY.md (confirms wallet_connect_sheet.dart exists)
  </read_first>
  <action>
    Edit `lib/frontend_scaffold.dart`. Insert two export lines — `export 'components/media_card.dart';` and `export 'components/media_controls.dart';` — immediately after the existing line `export 'components/scaffold_live_region.dart';` and before `export 'components/scaffold_motion.dart';`. Insert one more export line — `export 'components/wallet_connect_sheet.dart';` — immediately after the existing line `export 'components/scaffold_touch_target.dart';` and before `export 'components/desktop_body_container.dart';`. Do not reorder any existing line. Do not change any other file. Run `dart analyze --fatal-infos lib/frontend_scaffold.dart` and confirm zero warnings.
  </action>
  <verify>
    <automated>dart analyze --fatal-infos lib/frontend_scaffold.dart && grep -c "export 'components/media_card.dart'" lib/frontend_scaffold.dart && grep -c "export 'components/media_controls.dart'" lib/frontend_scaffold.dart && grep -c "export 'components/wallet_connect_sheet.dart'" lib/frontend_scaffold.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/frontend_scaffold.dart` contains `export 'components/media_card.dart';` exactly once
    - File contains `export 'components/media_controls.dart';` exactly once
    - File contains `export 'components/wallet_connect_sheet.dart';` exactly once
    - All three new export lines are positioned per the patterns doc (media_* after scaffold_live_region; wallet_connect_sheet after scaffold_touch_target, before desktop_body_container)
    - `dart analyze --fatal-infos lib/frontend_scaffold.dart` exits 0
  </acceptance_criteria>
  <done>Barrel updated; analyzer clean.</done>
</task>

<task type="auto">
  <name>Task 2: Register demos in example/lib/main.dart</name>
  <files>example/lib/main.dart</files>
  <read_first>
    - example/lib/main.dart (current demo registration pattern)
    - example/lib/demos/media_card_demo.dart (from Plan 01)
    - example/lib/demos/media_controls_demo.dart (from Plan 02)
    - example/lib/demos/wallet_connect_sheet_demo.dart (from Plan 03)
    - example/lib/demos/bottom_drawer_demo.dart (single-purpose demo registration analog)
  </read_first>
  <action>
    Edit `example/lib/main.dart`. Add three imports — `import 'demos/media_card_demo.dart';`, `import 'demos/media_controls_demo.dart';`, `import 'demos/wallet_connect_sheet_demo.dart';` — in alphabetical position within the existing demos import block (after `loading_demo.dart`, before `page_chrome_demo.dart`). Register three new entries in the HomePage demo list (or equivalent registration structure used by existing demos) — one each for `MediaCardDemo`, `MediaControlsDemo`, `WalletConnectSheetDemo` — with titles 'Media card', 'Media controls', 'Wallet connect sheet'. Follow the exact registration shape used by the existing demos (e.g., bottom_drawer_demo). Run `dart analyze --fatal-infos example/lib/main.dart` and confirm zero warnings.
  </action>
  <verify>
    <automated>dart analyze --fatal-infos example/lib/main.dart && grep -c "media_card_demo.dart" example/lib/main.dart && grep -c "media_controls_demo.dart" example/lib/main.dart && grep -c "wallet_connect_sheet_demo.dart" example/lib/main.dart && grep -cE "MediaCardDemo|MediaControlsDemo|WalletConnectSheetDemo" example/lib/main.dart</automated>
  </verify>
  <acceptance_criteria>
    - `example/lib/main.dart` contains imports for `media_card_demo.dart`, `media_controls_demo.dart`, `wallet_connect_sheet_demo.dart`
    - File contains `MediaCardDemo`, `MediaControlsDemo`, `WalletConnectSheetDemo` references (in the registration list)
    - `dart analyze --fatal-infos example/lib/main.dart` exits 0
  </acceptance_criteria>
  <done>All three demos registered; analyzer clean.</done>
</task>

<task type="auto">
  <name>Task 3: Final phase gate — full analyze + test</name>
  <files>(none — read-only validation)</files>
  <read_first>
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-CONTEXT.md (the analyze/test gate rule)
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-01-SUMMARY.md
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-02-SUMMARY.md
    - .planning/workstreams/scaffold/phases/07-media-integration-widgets/07-03-SUMMARY.md
  </read_first>
  <action>
    From the scaffold package root, run `dart analyze --fatal-infos` (no path argument — analyzes the whole package). Confirm exit code 0 and zero issues. Then run `flutter test` (whole suite). Confirm exit code 0. Phase 6 shipped 154 tests; this phase adds 31 (10 media_card + 12 media_controls + 9 wallet_connect_sheet) for an expected total of 185 passing. If the actual count differs, record the actual count in the SUMMARY — do not modify tests to hit the number.
  </action>
  <verify>
    <automated>dart analyze --fatal-infos && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `dart analyze --fatal-infos` exits 0 with zero issues reported
    - `flutter test` exits 0 with all tests passing
    - Test count is recorded in the SUMMARY (expected ~185: 154 from Phase 6 + 31 from Phase 7)
  </acceptance_criteria>
  <done>Full package green; phase ready for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan wires exports and demo registrations. There is no auth, data-handling, network, persistence, or supply-chain surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | n/a | lib/frontend_scaffold.dart, example/lib/main.dart | accept | Pure wiring — three export lines and three demo registrations. No new inputs, no new code paths beyond what Plans 01-03 already shipped. |

Phase 7 is PURE PRESENTATIONAL UI. There are no high/medium/low security threats. The gate is satisfied by explicit statement, not by invented threats.
</threat_model>

<verification>
- `dart analyze --fatal-infos` exits 0 across the entire package
- `flutter test` exits 0 across the entire test suite
- All three widgets importable from the barrel (verified by analyzer resolving demo imports in main.dart)
</verification>

<success_criteria>
- Barrel exports three new components in sorted position
- Example app registers three new demos and compiles
- Full package analyzer clean
- Full test suite green
</success_criteria>

<output>
Create `.planning/workstreams/scaffold/phases/07-media-integration-widgets/07-04-SUMMARY.md` when done
</output>
