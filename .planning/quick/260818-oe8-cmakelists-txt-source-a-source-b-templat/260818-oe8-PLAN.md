---
quick_id: 260818-oe8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scaffold/CMakeLists.txt
autonomous: true
requirements: []
must_haves:
  truths:
    - "Source A custom command creates template_gen/scaffold/base/ before touching its stamp"
    - "Source B custom command creates template_gen/shared/base/ before touching its stamp"
    - "scaffold_generate_templates target succeeds on a fresh build tree"
  artifacts:
    - path: "src/scaffold/CMakeLists.txt"
      provides: "make_directory COMMAND lines added to Source A and Source B custom commands"
      contains: "make_directory"
  key_links:
    - from: "src/scaffold/CMakeLists.txt (Source A add_custom_command)"
      to: "${CMAKE_CURRENT_BINARY_DIR}/template_gen/scaffold/base/"
      via: "cmake -E make_directory before cmake -E touch"
      pattern: "make_directory.*template_gen/scaffold"
    - from: "src/scaffold/CMakeLists.txt (Source B add_custom_command)"
      to: "${CMAKE_CURRENT_BINARY_DIR}/template_gen/shared/base/"
      via: "cmake -E make_directory before cmake -E touch"
      pattern: "make_directory.*template_gen/shared"
---

<objective>
Fix fresh-build failure in `scaffold_generate_templates`: Source A (scaffold's own `base/` templates) and Source B (parent M3 templates from `TEMPLATES_DIR`) each end their `add_custom_command` with `cmake -E touch <stamp>` where the stamp lives under a nested directory (`template_gen/scaffold/base/...` and `template_gen/shared/base/...`) that is never created. On a fresh build tree, `cmake -E touch` fails because the parent directory does not exist.

The component targets (widget / cubit / state) already do the right thing — they run `${CMAKE_COMMAND} -E make_directory "${CMAKE_CURRENT_BINARY_DIR}/template_gen"` before the engine invocation. This fix brings Source A and Source B in line with that pattern, extended to the nested `base/` subdirectory that their stamps actually live in.

Verified against `tools/scaffold_codegen/engine.py` line 143 (`out.parent.mkdir(parents=True, exist_ok=True)`): the engine creates output directories itself, so ONLY the stamp directories need to be created by CMake.

Purpose: Make `scaffold_generate_templates` work on a clean configure+build with no manual `mkdir` step.
Output: Two `make_directory` COMMAND lines inserted into the existing `add_custom_command` blocks — no other edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/scaffold/CLAUDE.md
@src/scaffold/.planning/workstreams/scaffold/STATE.md
@src/scaffold/CMakeLists.txt
@src/scaffold/tools/scaffold_codegen/engine.py

<!-- Interface the executor needs. Extracted from the codebase. -->
The two custom commands being fixed (current state, lines 109-121 and 144-156 of
`src/scaffold/CMakeLists.txt`):

Source A (scaffold's own base/ templates):
```cmake
set(_stamp "${CMAKE_CURRENT_BINARY_DIR}/template_gen/scaffold/${_out}.stamp")
set(_output "${GENERATED_DIR}/scaffold/${_out}")
add_custom_command(
    OUTPUT ${_stamp}
    COMMAND ${PYTHON3_EXECUTABLE} ${ENGINE_SCRIPT}
        --template ${_rel}
        --tokens ${DESIGN_TOKENS}
        --output ${_output}
        --template-dir ${_scaffold_templates_dir}
        ${_parent_template_dir_flag}
    COMMAND ${CMAKE_COMMAND} -E touch ${_stamp}
    ...
)
```
For Source A, `${_out}` = `base/<stem>` (from `file(RELATIVE_PATH ... templates ... )`
on `templates/base/*.jinja2`), so the stamp path is
`.../template_gen/scaffold/base/<stem>.stamp` and the parent directory to create is
`${CMAKE_CURRENT_BINARY_DIR}/template_gen/scaffold/base`.

Source B (parent M3 templates from TEMPLATES_DIR):
```cmake
set(_stamp "${CMAKE_CURRENT_BINARY_DIR}/template_gen/shared/${_out}.stamp")
set(_output "${GENERATED_DIR}/shared/${_out}")
add_custom_command(
    OUTPUT ${_stamp}
    COMMAND ${PYTHON3_EXECUTABLE} ${ENGINE_SCRIPT}
        --template ${_rel}
        ...
    COMMAND ${CMAKE_COMMAND} -E touch ${_stamp}
    ...
)
```
For Source B, `${_out}` = `base/<stem>` (from `file(RELATIVE_PATH ... ${TEMPLATES_DIR} ...)`),
so the stamp path is `.../template_gen/shared/base/<stem>.stamp` and the parent directory
to create is `${CMAKE_CURRENT_BINARY_DIR}/template_gen/shared/base`.

Component-target precedent (line 216, already correct — match this style):
```cmake
COMMAND ${CMAKE_COMMAND} -E make_directory
    "${CMAKE_CURRENT_BINARY_DIR}/template_gen"
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add make_directory to Source A and Source B custom commands</name>
  <files>src/scaffold/CMakeLists.txt</files>
  <action>
    In `src/scaffold/CMakeLists.txt`, make TWO minimal insertions — no other edits,
    no refactoring, no reordering of the existing COMMAND lines.

    1. Source A (the `add_custom_command` block that currently starts at line 109,
       immediately after `set(_stamp "${CMAKE_CURRENT_BINARY_DIR}/template_gen/scaffold/${_out}.stamp")`
       and `set(_output "${GENERATED_DIR}/scaffold/${_out}")`):
       Insert as the FIRST `COMMAND` inside the `add_custom_command` (before the
       existing `COMMAND ${PYTHON3_EXECUTABLE} ${ENGINE_SCRIPT} ...` line):

           COMMAND ${CMAKE_COMMAND} -E make_directory
               "${CMAKE_CURRENT_BINARY_DIR}/template_gen/scaffold/base"

       This matches the component-target style at line 216 (same `make_directory`
       invocation shape, multi-line continuation, quoted path). The nested
       `scaffold/base` subdirectory is required because `${_out}` is `base/<stem>`
       for every file globbed from `templates/base/*.jinja2`, so the touch at the
       end writes to `template_gen/scaffold/base/<stem>.stamp`.

    2. Source B (the `add_custom_command` block that currently starts at line 144,
       immediately after `set(_stamp "${CMAKE_CURRENT_BINARY_DIR}/template_gen/shared/${_out}.stamp")`
       and `set(_output "${GENERATED_DIR}/shared/${_out}")`):
       Insert as the FIRST `COMMAND` inside that `add_custom_command`:

           COMMAND ${CMAKE_COMMAND} -E make_directory
               "${CMAKE_CURRENT_BINARY_DIR}/template_gen/shared/base"

       Same reasoning as Source A, with `shared` replacing `scaffold`.

    Do NOT add make_directory for the output directories (`${GENERATED_DIR}/...`) —
    `tools/scaffold_codegen/engine.py` line 143 already does
    `out.parent.mkdir(parents=True, exist_ok=True)` on the output path, so CMake
    does not need to create them. Only the stamp directories are CMake's
    responsibility (the `cmake -E touch` at the end of each custom command is the
    operation that actually fails on a fresh tree).

    Do NOT touch the component targets (lines ~214, ~240, ~270) — they already have
    their own `make_directory` calls and are not affected by this bug. Do NOT touch
    the `FRONTEND_TARGET=html` block (lines ~311+) — per
    `src/scaffold/CLAUDE.md` "Known broken", those targets fail under
    `StrictUndefined` for unrelated reasons and are out of scope.
  </action>
  <verify>
    <automated>cd /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/apps/genius-tube/src/scaffold && rm -rf /tmp/fresh-build-260818-oe8 && cmake -B /tmp/fresh-build-260818-oe8 -S . -DPYTHON3_EXECUTABLE=$(which python3) && cmake --build /tmp/fresh-build-260818-oe8 --target scaffold_generate_templates && ls /tmp/fresh-build-260818-oe8/template_gen/scaffold/base/*.stamp</automated>
  </verify>
  <done>
    Fresh configure + `scaffold_generate_templates` build succeeds with no
    `cmake -E touch` directory-not-found error; at least one stamp file exists
    under `/tmp/fresh-build-260818-oe8/template_gen/scaffold/base/`. The diff to
    `src/scaffold/CMakeLists.txt` consists of exactly two inserted `make_directory`
    COMMAND blocks (four added lines total) — no other lines changed.
  </done>
</task>

</tasks>

<verification>
- `git diff src/scaffold/CMakeLists.txt` shows exactly two inserted `COMMAND ${CMAKE_COMMAND} -E make_directory ...` blocks and zero other modifications.
- `cmake -B /tmp/fresh-build-260818-oe8 -S . -DPYTHON3_EXECUTABLE=$(which python3)` configures cleanly.
- `cmake --build /tmp/fresh-build-260818-oe8 --target scaffold_generate_templates` completes successfully.
- Re-running `cmake --build /tmp/fresh-build-260818-oe8 --target scaffold_generate_templates` is a no-op (stamps up to date), proving the incremental-stamp contract still holds.
- `generate_all_components` still builds (regression check — component targets share the same `template_gen/` root and must not break).
</verification>

<success_criteria>
- Fresh-tree `scaffold_generate_templates` build passes.
- Incremental rebuild still skips unchanged templates.
- No changes outside `src/scaffold/CMakeLists.txt`.
- Commit message follows the repo convention: `fix(cmake): create template_gen/{scaffold,shared}/base dirs before stamp touch in Source A/B`.
</success_criteria>

<output>
Create `.planning/quick/260818-oe8-cmakelists-txt-source-a-source-b-templat/260818-oe8-SUMMARY.md` when done
</output>
